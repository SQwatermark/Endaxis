# 参与开发

这份说明适用于准备修改 Endaxis 的开发者。启动与构建命令见[开发指南](docs/development/README.md)，模块分工见[架构概览](docs/architecture/README.md)。

## 开始修改

1. 检查工作树，保留已有未提交内容。
2. 阅读[代码规范](docs/development/code-style.md)，找到负责该行为的模块和已有测试。
3. 描述要解决的问题、触发方式和预期结果。涉及游戏规则时，先核对同版本数据与原生证据；旧版模拟结果只用于发现差异。
4. 完成修改并运行相关检查。涉及界面时，实际打开页面验证，不以构建成功代替交互验收。
5. 检查差异，更新受影响的使用说明或架构说明，再提交代码。

小改动不需要另建方案文档。若一个局部功能需要反复穿过多个模块补字段，先检查状态归属和公共入口，不要继续机械转发。

## 如何验证

[开发指南](docs/development/README.md#验证修改)列出了应用、生成器、契约及离线工具的检查命令。根据改变的行为选择范围，报告实际运行的命令、结果和未覆盖部分。

修复模拟问题，应保留能复现错误的输入，并检查顺序、资源、状态和伤害。总伤害接近旧版不能证明修复正确。数据生成问题必须修改生成器或配置并重新生成，不能手改产物。

## 提交内容

提交说明应回答：原来哪里不对、现在如何表现、怎样验证。涉及数据更新时，还要交代来源版本和未支持范围。

可以提交有明确用途的测试夹具、生成定义和必要资源。不要提交个人存档、原始解包目录、内存转储、账户信息、临时报告、日志或本机路径。原始研究材料保存在仓库外；公开文档只保留可核验的结论、必要片段与来源。

文档使用中文，直接说明用途、步骤和结果。具体编排规则见[文档维护](docs/development/documentation.md)。
