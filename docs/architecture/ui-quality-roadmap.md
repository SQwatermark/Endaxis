# UI 设计与实现质量中期计划

## 目标与依据

提升整体设计质量和实现质量，同时复刻旧版合理的使用体验。不是通用表单重写，
不是以拆文件、测试数量或源码相似度作为完成指标。

旧版行为参照 `D:/Projects/Endaxis-upstream-main`；先读源码解释规则，再在相同
数据、视口和状态下验证效果。游戏模拟事实仍来自游戏证据，不由旧版展示反推。
用户明确调整过的体验优先；新版模型必然差异和旧版明显缺陷需要单独记录。
深色主题优先，浅色主题留专项，不把浅色未完成伪装成全主题验收。

## 当前架构判断

- 已有不可变文档命令、编辑会话、显示模型、手势租约和逻辑输入区域，不推倒重来。
- TimelineEditor 仍集中协调大量选择、弹窗、持久化配置、手势和模拟反馈；按功能
  边界逐步迁出，而不是仅把连续代码搬到 composable。
- 输入区域与 hasModalPanel、旧优先级、DOM 保护共存。完整迁移与回归前保留保护，
  完成后移除重复判断，避免长期维护两套事实来源。
- 之前的独立组件保存测试不等于完整项目保存验收；源码结构测试不等于行为测试。

## 阶段与验收

| 阶段             | 范围                                                   | 完成证据                                                                      |
| ---------------- | ------------------------------------------------------ | ----------------------------------------------------------------------------- |
| 1 输入与浮层归属 | 拖拽、键盘、剪贴板、焦点、确认框生命周期               | 跨控件拖动、取消、模态叠加、卸载恢复；原生文本操作不受损；实际浏览器回归      |
| 2 主面板分区复刻 | 顶栏与标尺、轨道身份区、技能区、监控区、侧栏与相关入口 | 每次只专注一区；读旧版源码；正常/悬停/选择/拖动/折叠/滚动的行为及深色视觉对照 |
| 3 编辑流程       | 选择→实例编辑→模板编辑，技能/能力实体/Buff/配装        | 编辑范围、引用影响明确；取消不污染原对象；保存与项目持久化、撤销重做走通      |
| 4 结构与反馈收整 | 命令入口、状态归属、模拟反馈、共享视觉约束             | 同操作同命令；无过期结果误导；合理复用而不压平异构内容；回归覆盖真实用户流程  |

阶段 4 的必要重构随前面切片实施，不等所有功能完成才做，也不先开展无边界重构。
第 2 阶段具体次序结合现有逐块核对文档与缺陷确定，已验收切片不重复重做。

## 设计约束

1. 自由编辑与严格模拟并行。告警不是编辑权限；不得隐式替换技能或抹去用户意图。
2. 选择、查看、编辑不同；不得因为选择而突然开新面板。
3. 实例/模板、草稿/已保存数据的范围必须明确；定义编辑器不照搬旧版技能表单。
4. 菜单和快捷键共享操作语义、事务与可用条件；不各自改写文档。
5. 视觉层级、命中层级、键盘归属分别设计并保持一致，不能只提高 z-index。
6. 布局变化不得改变游戏效果持续时间、输入时刻或模拟中断规则。
7. 编辑即时反馈；模拟待计算、计算中、过期、失败和告警需要可辨识，不能用旧结果
   冒充当前结果。具体状态实现先查现有代码，不预设必须引入某框架或 worker。
8. 同类控件共享尺寸、语义和反馈；不同任务保留不同表达，不追求所有编辑器一致外形。

## 每个切片的交付

- 记录旧版源码依据、目标行为、必要差异和未验收项。
- 分别记录纯逻辑测试、组件行为、真实浏览器及视觉证据，不互相替代。
- 验证完成后提交代码与文档；临时项目、截图和测试生成物不提交 tmp/。
- 中期目标未完成前不宣称整体完成，避免频繁汇报微小进度。

当前阶段：1。程序式确认、选择器、构筑和模板编辑器已经接入区域；工作台手势
屏障跟随同一逻辑路径，已移除对应 11 项根枚举。剩余详情、分析、帮助及局部
浮层仍需迁移与验收。阶段 3 的已复现草稿反馈缺陷随当前切片修复，不代表阶段 3 完成。

## UI 阶段完成后的真实旧轴对比任务

用户指定下一阶段从 https://www.end-axis.com/shares 选择若干真实旧版示例轴，
尽量覆盖更多不同干员，优先选择旧版无报错的轴。此任务在 UI 质量目标完成之后
开展，不能提前替换当前主线，也不能以少数示例轴通过宣称 UI 质量目标完成。

每条轴记录来源、旧版可用性、复现条件、旧/新版本证据，区分 UI 展示、编辑行为、
模拟输出差异。依旧版源码和游戏事实分别判断：新版缺陷及时修复回归；新版合理
差异、旧版问题和未知也全部记录，不因为未修代码而略过。尚未选样或执行对比。
