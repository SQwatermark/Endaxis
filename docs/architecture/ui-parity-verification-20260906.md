# 主面板 UI 逐块核对

唯一旧版参照：`D:/Projects/Endaxis-upstream-main` 的上游 main 工作树。
旧版源码用于解释实际行为；新版按自身结构实现相同体验。不能用类名一致或源码字符串测试代替浏览器验收。

## 已验收切片：右上角四个按钮

2026-09-06，在独立 Chrome 无头会话中同时运行旧版 5174 和当前版 5173，
viewport 1920×1080、deviceScaleFactor 1，不读写用户浏览器项目。

- 修前：当前 78×35px、继承 16px 字号；旧版 70×28px、12px/12px、600 字重。
- 原因：`TimelineHeaderToolbar` scoped `button { font: inherit; color: inherit }`
  覆盖共享按钮定义。双方 padding 均为 6px 12px，之前将变大归因于 padding 的结论错误。
- 修复：仅非共享按钮保留字体继承，使共享按钮使用自己的完整字体与颜色契约。
- 修后：四个按钮均为 70×28px；字号、字重、行高、padding、图文间距和默认颜色一致。
  深/浅主题悬停均保持 28px 高和向上 1px 位移；实际打开“更多”和“显示”正常。
- “更多”弹层两版均为 280×280.171875px。新版额外语言 RU、项目导出与旧版接收分享码
  的内容差异保留，不据此宣称分享功能已经复刻完成。
- 临时测量脚本和截图在忽略的 `tmp/ui-browser/`，不提交生成物。

## 待逐块核对

### SCALE 和标尺已修正的切片

- 对照旧版 `TimelineGrid`、`timelineStore`：100% = 50px/现实秒，范围 15–1200px/秒
  （30%–2400%），滑块每步 1px/秒。新版维持 30Hz 帧协议，仅在 UI 几何换算中恢复此映射。
- 加减按钮按当前秒宽约 10% 变化；原先固定增减 10 个百分点和 50%–200% 范围不是必要差异。
- 角落纵向容器中的子控件此前被 `flex: 1 0 0` 撑满，导致内容贴顶；改成自然内容高度。
  Chrome 测量两版内容均为 169×46px、滑轨 137×2px，内部位置一致，深浅主题截图已检查。
- 标尺低亮秒刻度与逐帧刻度恢复其原本颜色，去掉重复 opacity；保留新版现实时间投影。
- 修复折叠准备区后轴末端拖动仍减完整准备时长的错误，改用共用逆投影。
  Chrome 实际拖动验证：5 秒准备区折叠，60s 轴末端左拖 50px，结果为 59s，无页面异常。
- 类型检查通过；时间轴 121 文件 / 707 项通过。测试中的既有 Element Plus SSR 注入警告仍存在。
- 后续补齐顶栏 50px 内容加 1px 边框后，两版角落坐标完全一致：外框 (249,51)、
  控件 (257,57.5)、滑轨 (273,97.5)。
- 准备时长/轴长输入恢复弹窗位置、背景、边框、阴影、字体和自动聚焦；统一充能右键输入
  同样恢复完整弹窗，保留可键盘操作的新版按钮实现。
- Chrome 测量准备时长弹窗两版均为 (687,57)、116.609375×38px；输入框均为 86×24px，
  打开立即获得焦点，Escape 关闭。旧版五秒显示 300f、新版显示 150f 是 60Hz/30Hz 协议差异，
  不修改新版帧协议来伪装为旧版。
- 1920×800 下实际纵向滚动后，SCALE 和标尺命中测试仍命中各自区域，轨道内容没有覆盖顶部。
- 折叠准备区时不再向 18px 窄区投影全部负时间刻度，避免标签重叠；旧版源码同样从 0 秒开始显示刻度。
- 滑块恢复只调整比例、保持水平滚动位置；加减按钮的居中锚点排除 180px 干员栏。
  浏览器从初始位置将滑块拉到最大，当前 scrollLeft 保持 0。
- 滚轮改为按秒宽取整，避免实际比例与原生 range 步长不同步；Chrome 对照两版
  100%→116%→98%，指针下时间锚点差异低于 1px。边界比例和正负取整有独立回归。
- 同时刻按键帽保留轨道/动作输入顺序；此前额外按实例 ID 排序会打乱上下层。新增对应回归。
- 剩余验收：多按键帽的实际密集重叠截图、极端缩放下的准备区与横向拖拽。

### 轨道左侧布局切片

- 使用独立 Chrome 中相同的佩丽卡轨道，对照相对轨道中心的盒模型。
  两版头像实际均为 48×48px，中心对齐；没有重新裁剪头像资源。
- 修正充能行 y=-52→-54、属性详情 x=87→91/y=-27→-29。
- 配装行从 x=35→29；设置实际 22px 行高后，32px 武器槽向上伸出 10px，
  武器槽顶部从中心+33 恢复到+23，四件装备的底边同样恢复。套装提示同步左移。
- 修正空装备槽虚线颜色、头像边框及浅色属性详情 hover；仍使用新版的可访问按钮与领域命令。
- 浅色实测发现组件仍依赖已不存在的 `.timeline-editor` 祖先，导致主题规则全部失效。
  改为真实 `.track-header` 根，浏览器确认浅色充能与输入框恢复、属性详情 hover 的文字/背景/边框
  计算颜色均与旧版相同。加入现有主题作用域门禁，防止 Vue `:global` 改写意外污染 html 根。
- 充能上限 68/80 的不同来自不同版本的定义/构筑，不为视觉一致而覆写数据。
- 尚未验收轨道整体高度分配、所有非空配装图标、拖动换位与各入口；不标记为整个左侧完成。

### 轨道高度与入口续验

- 紧凑模式由固定四行像素高改为按当前视口及存储比例分配；保留用户明确指定的上下无额外留白，
  不照搬上游的上下各 20px padding。Buff 所需空间仍能扩展实际轨道和滚动画布。
- 修正轨道边框被额外计算、头部最小 160px 导致缩短后头部越过行边界的几何不一致。
- Chrome 1920×1080：可用 716px，四轨各179px；分隔线拖动30px后为209/149/179/179。
  缩到1920×800后为154/110/132/132，合计528px，与可用区域一致。
- 双击分隔线恢复全部轨道等权；Escape与pointercancel还原调整前比例，取消不遗留本地配置。
  Chrome实测Escape还原、双击回132/132/132/132。
- 实页逐一打开武器、护甲、护手、两个配件、干员选择和属性详情，均打开对应面板；
  下移轨道后身份交换正确，再下移还原；无页面异常。测试在独立会话内，不修改用户浏览器项目。
- 非空配装图标、原生拖动换位和极窄轨道下控件布局仍需继续验收。

### 配装与原生换位续验

- 上游 `TimelineGrid.vue:onReorderDragStart` 使用整个身份区作为原生拖动预览；
  新版补回整块预览及鼠标抓取偏移，不再只带着小把手移动。
- 对照上游 `.track-info.is-reorder-target/source` 恢复白色半透明目标提示及 0.5 源透明度，
  去掉自行添加的金色外框；上下箭头恢复 10px，拖动把手保留 12px。
- 上游 `timelineStore.moveTrack` 实际为两轨交换；新版同样交换，不改成插入排序。
- 独立 Chrome 会话通过原生拖放交换并还原两轨，结束后无残留目标/源状态；
  截获的 `setDragImage` 指向完整头部，保留抓取点 (17,89)。无页面异常。
- 经选择器装入武器及四件装备，五张原生 ID WebP 均成功解码；武器图像内容区 28px、
  装备 18px，包含边框分别 32px/22px，与上游槽位规则一致。已查看实际截图。
- 该验证只覆盖实际选择的五个资源，不等于全图标资源审计。
- 双页面实际拖至最小 66px：修正新版 CSS Grid 的自动最小行高导致控件偏下 6px，
  修后充能 top=-21px、配装 top=66px 与上游相同。上游此时同样有控件越出轨道的缺陷，
  未擅自隐藏控件或改变最小高度；后续应单独处理其可用性，不能标记为无问题。

### 极窄轨道的可用性修正

- 上一轮发现的共同越界缺陷已作明确偏离上游的修正：最小轨道高度由只容纳技能行的
  66px 改为 160px。沿用上游控件位置、尺寸及套装提示；提示底部在中心下方 79px，
  再加边框，160px 是完整身份区不越界的下限。空轨使用同一下限，避免选择干员后跳动。
- 不隐藏、裁切或缩小控件。短窗口保留纵向滚动，不再强行把四轨压进不足的空间。
  Buff 过量仍可继续扩展行高；普通高度及分隔线交换空间的行为保持不变。
- Chrome 实测完整五槽配装并向上拖动分隔线：最小 160px；充能 top=26px，
  配装底部=135px，套装提示底部=159px，全部位于本轨边界内。已查看截图。
  1920×700 窗口出现正常纵向滚动，第四轨头像仍能滚入视口并打开选择器；无页面异常。
- 此项是对上游明确可用性缺陷的修正，不属于新版模拟协议导致的必然差异。

### 名称与空头像反馈

- 对照上游 `trigger-name__main` 修正行内名称无法约束宽度的问题；保留 14px 字号及
  18px 行高，过长名称在自身区域内省略。只有实际显示转换提示时才保留其 20px 占位，
  正常名称不再浪费这段空间。转换支持提示属于新版必要差异，不因复刻删去。
- 补齐上游空头像悬停的边框、背景及加号变色。独立 Chrome 双页面测得暗色三项颜色、
  浅色三项颜色分别完全一致；无页面异常。
- 实页正常名称可用宽度 80px、右侧占位 0；独立页面注入长文本作为纯布局夹具，
  内容宽 238px，显示宽 80px，省略且不越出头部。已查看浅色截图，不写入项目或定义。

### 轨道选择与选择器入口分离

- 上游头部空白区只调用 `selectTrack`；空头像/空名称才打开选择器。新版头部与
  `useTimelineLoadoutEditor.selectTrack` 两层均有空轨自动弹窗，已同时移除。
- 名称点击只选中轨道，空名称额外显式打开干员选择；头像保持独立更换入口。
  头部点击阻止冒泡到时间轴。选择空轨和已有干员轨道均清除技能选择，符合上游 store。
- 独立 Chrome 新旧页面分别验证空白区、空名称、已有名称、头像四条路径；
  弹窗与选中结果一致，无页面异常。新增状态层测试覆盖不弹窗、不写项目历史及清除选择。

### 充能输入验收 / 敌人读数起步

- 轨道充能实页验证：输入25、步进26、撤销25、重做26；999被约束到定义上限，
  负值被约束到0，相应箭头禁用，无意外弹窗。无需为此更改已工作的输入逻辑。
- 开始仅核对底部敌人状态读数。对照上游 `ResourceMonitor.vue` 的 readout 规则，
  修正字体栈、标题11px行高、数值1.1行高和血条160ms ease过渡。
- Chrome双页面实测：标题(13,8)、数值(13,25)、血条(13,44.1875)，血条156×5，
  修后与上游相同；已查看两张读数截图。该比较不包括模块外壳和折叠布局。
- 底部原有未提交的Buff摘要、投影、公共网格及失衡/技力改动仍未整体验收，
  不随本次局部字体修正提交，也不标记底部完成。

### 最后命中 Buff 摘要规则审查

- 上游 lastDamageSnapshot 取最后 DAMAGE_HIT 时刻，以显示键归并、保留最高层数，
  最多8项，其余显示溢出数。新版以 Buff 定义 ID 归并，避免不同原生定义因同名被合并。
- 抽出纯 UI 摘要函数并测试：无命中为空，包含结束端点，排除零时长与区间外段，
  同定义取最高层数、稳定排序并报告8项以外的数量。包含端点只复刻摘要读数，
  不改变模拟生命周期或其他实时状态判定。
- 敌人效果组件已接入该函数，并在工作区修正摘要18px图标、单层角标及血条左对齐。
  组件尚与前述未验收的底部网格改动交织，留待实页验收后整体提交；本次只提交纯函数与测试。

### 敌人 Buff 摘要实页验收

- 使用独立浏览器渲染正式组件，注入10个可见段的布局夹具（不写入项目或游戏定义）。
  验证8个图标及+2溢出提示、单层角标；第一个图标(13,57.1875)，18×18，
  第八个自动换到(13,79.1875)。已查看截图，横向滚动350px后摘要位置不动且命中测试正常。
- 摘要改为敌人读数组件的内容插槽，跟随血条正常排版，不用固定top定位。
  有一/两行图标时最低高度84/106px，保证图标与角标完整显示；不挤压或裁切第二行。
- 提交范围为敌人摘要、最后命中取样接线和所需的公共背景网格；不包含失衡/技力曲线、
  底部外壳的其他在途改动。本验证是组件边界夹具，不等价于全干员或真实战斗回归。

### 佩丽卡真实模拟与摘要容器高度

- 独立 Chrome 会话从技能库原生拖入佩丽卡战技，实际模拟产生电磁附着；
  敌人轴和最后命中摘要均显示，层数1。详情原生 ID 为
  `buff_common_energy_shard_attached_pulse`，本次放置对应66–666帧、持续20秒。
  折叠/展开后图标与详情入口保留，无页面异常。
- 发现实际外壳可能裁切已增高的子组件：现在由敌人效果组件报告最低内容高度，
  外壳仅对展开的敌人行应用。实页组件事件验证106px传递成功；折叠恢复14px。
  空间不足时采用安全底对齐，保证上方溢出内容可滚动访问，三行DOM顺序不变。

### 敌人持续条与隐式横向滚动

- 对照上游 ResourceMonitor 的 anomaly-icon-box / anomaly-stacks：轴上单层效果也显示1，
  图标禁止收缩；正式佩丽卡战技实测图标20×20、持续条高16，悬停亮度分别1.18与1.16，
  后者饱和度1.08，与上游规则一致。已查看实际持续条截图。
- 悬停长持续条时发现隐式横向滚动：原根元素宽度为整条时间轴，浏览器将祖先隐藏容器
  scrollIntoView后，敌人摘要从x249移至x71。修为可视区宽度100%与overflow:clip，
  超宽坐标仅由内部层绘制，不让摘要参与隐式横向滚动。
- 修后真实战技路径再次验证，悬停和时间轴scrollLeft=600后摘要X保持不变，
  摘要区域命中测试正常；折叠、展开与详情入口回归通过，无页面异常。

### 多行与浅色效果显示

- 对照上游 afflictionLayout 的 icon=20、gap=4，敌人持续效果行距由误用干员轨道的22px
  改为24px，并同步最低内容高度。依用户要求，不照搬上游空间不足时将图标压至14px的行为。
- 独立Chrome正式组件十行夹具：每行图标20px、行距24px；切换浅色后已截图检查，
  第十行和摘要换行完整可见。佩丽卡真实战技路径、层数、悬停、滚动、折叠与详情再次通过。
- 爆发/反应瞬时标记和持续Buff目前仍有共行重叠风险，下一步需单独核对避让；
  不将本次十行持续Buff验证表述为所有敌人效果已完成。

### 瞬时事件标记避让

- 上游按physical/attachment/anomaly/status分区，同刻标记另分slot。新版没有这套旧版
  效果对象分类，仍以原生可见Buff作为持续状态；瞬时回执独立放在持续段之后的行。
- 新增纯UI像素避让：保持事件真实水平锚点不变，重叠图标向下分行，间隔24px；
  可容纳时复用行。滚动不改变分行，缩放按实际图标宽度重排，不改模拟时间或事件。
- 渲染键包含回执投影序号，避免同种同帧标记重复键丢失。三项测试覆盖同刻重复、
  间隔复用、输入顺序及滚动/缩放。
- 正式组件独立Chrome夹具：10个持续段之后3个同刻瞬时事件，X均920，Y为406/430/454，
  无覆盖，已查看截图；佩丽卡真实模拟显示、滚动、折叠和详情路径再次通过。

### 准备期边界验收 / 失衡读数语义

- 敌人持续条实页夹具验证12组准备区展开/折叠、三种缩放、两种滚动组合。
  跨零秒起点、原生durationEndFrame终点及零秒网格均对齐；没有为此修改生产坐标逻辑。
- 开始失衡行审查：原生模拟保存剩余抗性，恢复时回到最大值；上游ResourceMonitor展示
  已积累失衡。此前直接显示剩余值导致初始满条，方向相反。
- UI投影改为maxValue减remaining，保留所有帧、时间、sequence，不修改原始曲线或模拟。
  单测覆盖初始0、积累、满失衡、恢复归0和原始数据不变。
- Chrome实际佩丽卡战技前显示0/300，战技后10/300，已查看读数截图。失衡行其余样式、
  曲线填充与恢复区间仍待继续验收，不随本次语义修正一起提交。

### 失衡读数尺寸与最小高度

- 新旧双页面实测标题与数值：修后位置(13,8)/(13,25)，行高11/13.2px，字体栈与上游一致。
  进度条位于(13,44.1875)，宽156px。
- 发现上游默认较矮的失衡行会把5px进度条压至0px；新版不复刻该缺陷。
  展开失衡行至少60px，进度条禁止收缩并使用160ms ease过渡；折叠仍14px。
- Chrome真实佩丽卡战技回归：0/300到10/300，进度条高5px，折叠展开高度检查通过；
  已查看读数截图。曲线生成点是否需要保持值/阶跃，须核对上游staggerSeries生成过程，
  本轮不凭视觉猜测修改曲线点。

1. 标尺与 SCALE：控制区垂直对齐、滑块映射、准备区/结束边界、按键帽、滚动遮挡。
2. 轨道左侧：头像、充能、配装入口、轨道高度、横向滚动与选中效果遮挡。
3. 底部：敌人状态、失衡、技力分别验收；折叠、曲线、图标、来源和交互均在范围内。
4. 技能库与轴上编辑：保持已确定的类型主标题，不因复刻重新改写技能文本；逐项验证拖放与撤销。
5. 选择器、详情和功能入口：同功能恢复旧版体验；新版定义编辑等必要差异独立说明。

### 失衡曲线瞬时变化与末值保持

- 核对上游 `simulation/projection/projectStaggerSeries.ts`：每次 STAGGER 事件在同一时刻记录旧值、新值，末值延伸至显示终点，因此不是逐渐增长的斜线。
- 新版仅在 UI 绘图补充保持点；保留原始结算点、命中标记与模拟数据，其他资源曲线不受影响。
- 单测覆盖瞬时变化、同帧连续变化、恢复归零、末值延伸、空数据和输入不变。
- 独立 Chrome 实际佩丽卡战技验证 SVG：先从 (180,56) 水平到 (540,56)，同一 X 跳至 Y=54.1333，再保持到显示终点。读数为 10/300，电磁附着、详情、折叠及横向遮挡回归通过，无页面异常。

### 准备区按钮与轨道分隔线命中顺序

- 上游 `TimelineGrid.vue` 准备区展开/收起控件层级为 6，分隔线 overlay 为 1。新版控件 12 低于分隔线 40，导致初始重叠处误触拖动。
- 控件层级调整为 41，保留容器 pointer-events:none、按钮 pointer-events:auto；仍低于固定轨道头与标尺。
- Chrome 实页中展开/收起两种按钮中心 Y=469.5，位于第二条分隔线 Y=464 的 10px 命中区内。elementFromPoint、真实鼠标点击均优先命中按钮，旁边分隔线仍能拖动。
- 验证时发现上一轮部分暂存提交恢复未暂存补丁后，ResourceCurves 的 pattern、rect、警告 span 和 CSS 声明错位，导致 Invalid end tag。已恢复合法结构，新增组件编译回归测试；此类提交必须在提交后重新加载实页验证，不能只验证提交前状态。

### 失衡状态区间与面板高度

- 核对上游 ResourceMonitor 的 lockZones、stun-pattern-monitor 和 2s alternate 动画；新版区间由 brokePoise/hasPoiseBrokenTag 回执投影，不能从曲线是否到顶反推。
- 补齐重复命中不重开区间、独立多次失衡、投影末帧截断、同帧结束不人为加宽的测试；并回归真实 CombatVitalsRuntime 的恢复与标签结束顺序。
- 正式组件 Chrome 夹具发现固定 56px viewBox 会纵向拉伸文字。工作区已改为失衡图使用实际像素高度：70px/140px 面板中文字边界高度均为14px，条纹仍45度；已查看截图。技力绘图坐标不随本次修改。
- 再次检查未暂存恢复内容，修复 spWarnings 落到 script 外、网格落入 label、测试嵌套等错位。资源组件及其接线仍在工作区，待剩余边界验收后整体提交，避免继续局部暂存破坏恢复位置。

### 失衡显示层边界验收与提交

- 正式组件浏览器夹具：准备区展开/收起、0.5/2/5 px 每帧、0/350px 滚动共12组。跨零秒区间 -30 至120帧起止坐标与统一换算一致；收起准备区按18px压缩，而非移除负帧。
- 70px/140px面板高度下文字边界均14px；标签与条纹在同一真实像素坐标系绘制，左侧读数遮罩层级高于绘图层。
- 提交失衡回执区间接线、显示层与共享网格。技力不足提示及外壳总折叠改动仍留在工作区，不随本次验收一并提交。

### 失衡读数精度与恢复交互

- 上游 currentStaggerValue 对末值 Math.round，读数进度条也使用取整值。新版只对失衡读数与进度条取整，不改事实点、曲线或其他资源。
- Chrome 正式组件：积累10.6显示11/300，进度条11/300，命中点标题仍10.6；恢复后显示0/300。隐藏再展开后，区间文字仍保持14px边界高度。
- 旧版 nodeSegments 的节点停顿条纹尚未对应新版原生事实；不能凭失衡百分比画出不存在的停顿。该项作为明确差异保留，失衡区不称完全一致。

### 用户复核：失衡圆点、满值线和最小高度

- 上游圆点只有 fill，没有 stroke；新版仅对失衡圆点去掉描边，不改技力圆点。
- 按上游 stagger-section-body::before 恢复绘图区顶部1px满值线，颜色 rgba(255,156,110,0.32)，固定于绘图区边界，不随时间滚动。
- 撤销此前自行增加的60px最低限制，恢复主体26px + 顶栏14px。Chrome实际拖动测得内容40px，加新版1px分隔边框占高41px；折叠仍14px。
- 40/70/140px夹具检查描边为none、满值线位置及颜色；真实面板拖动、折叠展开通过，已查看截图。

### 技力曲线像素尺寸与不足提示

- 对照上游 ResourceMonitor 的 SP_BODY_HEIGHT、spPolyline、spWarningZones/getSpWarningTagTop：使用实际主体高度，圆点无描边；提示相对扣除前的技力位置，间距及上下边距随高度变化并夹在旧版范围内。
- 技力 SVG 改为实际像素高度，去除圆点描边。低于-40显示下界的事实不再钳到下界线上，保留坐标并由SVG裁切，不改变模拟技力。
- 正式组件 Chrome 夹具在66/140/250px三种高度下验证4px圆点、18px提示、两次负值扣除提示；自动恢复不新增提示。截图已查看。失衡区和准备区按钮回归同时覆盖。
- 本次提交技力不足提示及其接线；外壳总折叠改动仍待独立验收。

### 技力读数控件与主题色

- 对照上游 COLOR_SP_MAIN=var(--ea-gold)、readout-title 与 module-control-row：技力曲线改用主题金色，标题恢复11px/1 Inter，控件标签继承Inter。
- 新旧Chrome页面同尺寸测量：标题位置(13,8)、行高11px；两行输入框位置(104,25)/(104,53)、尺寸65×22px均一致，已查看两张读数截图。数值默认200/8与新版300/10是数据差异，不为视觉对齐改模拟初值。
- 正式编辑入口验证初始技力100可输入、500受上限约束变300、上限增量按钮禁用、恢复速率10递增为10.5、负值离焦归0；修改后曲线投影仍正常生成。

### 分区折叠与外壳联动

- 上游 TimelineEditor.toggleBottomTool 在从关闭状态打开时增加 expandAllToken；ResourceMonitor 监听后展开三段。新版补齐同类通知，不重置分区比例或模拟数据。
- 资源监控区隐藏外壳重复关闭按钮；分区按钮与侧栏入口保留，其他底部工具的关闭入口不变。
- Chrome 实页六种分区折叠顺序均通过：前两段各14px，最后一段关闭总面板；侧栏重开三段展开且比例保持。局部折叠后从侧栏关闭、重开同样通过。30项结构回归通过。

### 底部最小高度与分隔线命中范围

- 对照上游 bottomPanelMinHeight：0/1/2个折叠分区对应240/180/120px。新增折叠数量通知，同时接入实际布局与拖动限制；小视口仍优先保证时间轴。非监控工具保持其正常最低高度。
- 实拖发现底部分隔线缺失上游的9px伪元素命中区，已恢复1px占位、9px命中范围、z-index30与悬停/拖动反馈。
- Chrome真实鼠标拖动依次达到240/180/120px，再展开分区恢复180/240px；无页面异常。几何单测覆盖三档最低高度、小视口以及总折叠。

底部已有改动的分项验收已记录；仍不应把节点停顿事实缺口等明确差异称作“全部完成”。

### 技力整段曲线与准备期资源规则修正

- 用户截图暴露此前局部样式验证遗漏了整段曲线：扣费斜连、满值后截断、准备区填充三角形。此前验证不能证明技力区整体一致。
- 对照上游 `projectSpSeries.ts`：瞬时变化需要同帧前后值，自然恢复单独插值，暂停段保持。新版新增纯展示补点，不修改原始回执或增加交互标记；最后已知值延续至显示终点，填充从准备区初值垂直闭合，不再从基线斜连。
- 对照上游 `TeamState.advanceTime`、`ActionStartHandler`、`SpRegenPauseHandler`：准备期不推进自然恢复，准备期开始的技能免技力费用及扣费暂停。新版按施法开始是否小于 0 帧记录此规则，延迟扣费跨 0 帧也保持免除；到达 0 帧不提前获得一帧恢复。终结技能量和技能效果主动回技力不在本次免除范围内。
- 更正前文默认值判断：按用户确认，新建方案初始技力改为 200，上限仍为 300；不覆盖已保存方案。恢复速率仍使用现有定义 10，未擅自改为旧版 8。
- Chrome 独立上下文实际拖入佩丽卡战技，覆盖空轴、正帧战技、负帧战技：初值均为 200；负帧无自动恢复或战技扣费；正帧扣费同横坐标垂直下降；满值保持到 900 帧；无页面异常。完整底部截图已查看，临时截图及脚本保留在忽略的 `tmp/ui-browser/sp-real-*`。
- 40 项针对性测试通过；项目、投影、资源账本、文档命令、模拟入口另 210 项回归通过。此项仅收口上述技力缺陷，不宣称整个底部监控区完全复刻。

### 技力不足提示横向滚动遮挡

- 上游 `ResourceMonitor.vue` 的 warning-tag 在右侧 section-body 内，固定读数栏不被提示覆盖。新版提示 z-index 5 高于读数栏 2，横向滚动可越过边界覆盖输入区；改为与曲线同层 1，由固定读数栏遮挡。
- Chrome 正式组件夹具验证准备区展开、折叠两种坐标映射，提示滚入读数栏后由其覆盖；已查看截图。另回归 66/140/250px 高度下提示位置、18px 高度、4px 圆点和低于 -40 的真实坐标。测试辅助文件仍仅保存在忽略的 tmp 中。

### 轴上技能拖动的指针所有权

- 原实现只有 window 冒泡监听，没有指针捕获；跨控件时会受子节点事件拦截及悬停浮层影响。改为捕获阶段监听，并在超过拖动阈值后捕获指针到稳定的 timelineScroll 容器，避免模拟刷新替换技能节点导致捕获丢失。落点继续使用 elementFromPoint，不将捕获目标误作落点。
- 松手、Esc、pointercancel、lostpointercapture 均清理监听和捕获；普通点击不启用捕获。技能库仍使用浏览器原生 HTML 拖放，没有混用这套轴内移动协议。
- Chrome 真实按住技能跨越拦截 pointermove/up 的测试控件、左侧面板后返回原轨道，移动成功；松手后控件重新收到事件；Esc 回滚通过。脚本为忽略的 tmp/ui-browser/cast-drag-capture.mjs。结构回归 6 项通过。
