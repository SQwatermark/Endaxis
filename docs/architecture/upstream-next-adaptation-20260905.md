# 2026-09-05 上游功能核对与 Next 接入

## 比较基准

- 已抓取 `origin`（Lieyuan621/Endaxis），默认分支 `main` 为 `4dadc55f`。
- 共同祖先 `302b80d2` 至上游 main 共 51 个提交；不是相对昨日的新增数量。
- `origin/1.5` 为 `565d359f`，在 main 上增加一个临时提交：台风、两把术式单元与空的甜点时刻文件。
- 当前 Next 工作分支为 `refactor/common-game-data`，HEAD `8cbbb4e8`；工作区已有大量数据更新和 UI 修改。
- 此次只 fetch，尚未 merge/cherry-pick。旧版实现用于行为对照；模拟规则以同版本游戏证据为准。

## 接入顺序与证据边界

| 次序 | 上游变化                                 | Next 核对／实现要求                                                                                            | 当前状态                                                                          |
| ---- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| 1    | 底部状态栏折叠                           | 曲线区水平中央按钮；14px 折叠条；按用户要求统一堆叠在展开区下方                                                | 已调整，结构回归通过                                                              |
| 2    | 三种重置（`f046457c`）                   | 保留配装清轴、重置当前轴、重置全部轴；通过项目命令支持撤销；不能误删项目模板库                                 | 已接入上游面板和 Next 项目命令；不再重置为样例场景                                |
| 3    | 工具与显示菜单（`a25f3eef`、`66bb80be`） | 对照当前已有图层开关、工具状态与布局，补缺入口                                                                 | 独立显示入口、辅助线、Buff 布局及持续条调色／来源／区域筛选已接入；待完整视觉验收 |
| 4    | 全队连携冷却／回转控制（`3d4aaaa2`）     | 核对原生连携窗口和冷却语义，接入统一时间轴外部控制；避免额外维护一套触发器                                     | 已接线与行为回归，标记选择／微移／抓取偏移已补；待视觉验收                        |
| 5    | 非技能动作（`d39d8d4c`）                 | 重点核对梨诺终止动作：不误触发技能类型事件，不误消耗一次性技能效果                                             | 事件信号保留、战技/终结姿态终止均已生产回归；具体一次性配装组合仍待覆盖           |
| 6    | 倍率来源详情（`80d23a33`）               | 检查 Next Hit 详情的直接倍率／技能倍率来源，使用现有回执和定义身份                                             | 待核对                                                                            |
| 7    | 干员模拟修复                             | 阿列什结晶与连携充能、庄方宜强化／免费战技和天赋、汤汤技力返还及水龙卷增伤、伊冯、骏卫、安塔尔、诀、别礼、卡缪 | 将上游修复转成 Next 行为测试，再按原始数据判定差异                                |
| 8    | 新干员／武器／装备                       | 台风、梨诺、1.5 武器、点剑纤维护甲等已在当前生成链覆盖；核对具体机制                                           | 不能把上游新增文件数视为 Next 缺失数                                              |
| 9    | 移动端、平板与触控编辑                   | 技能库抽屉、触摸选择／拖动、菜单和面板布局；保留 Next 项目与编辑命令                                           | 待实现                                                                            |
| 10   | 危机合约及其他活动                       | 当前包体检索 → 表和 Buff/触发动作取证 → 公共协议与模拟接入 → UI；仅接入影响现有模型的机制                      | 当前 effective 表实际解码已确认合约与词条存在；词条 Buff 语义尚待取证             |

## 不直接移植的内容

- 上游删除俄语、旧数据 slug 和旧持久化补丁不能直接套到 Next。
- 技能库分组仅决定展示和放置；技能身份、等级来源和事件类型仍由单技能定义决定。
- 上游危机合约含纯展示的己方冰冻逻辑，不能仅因已有代码便增加模拟范围。
- 旧版手配活动数值只用于定位线索；当前包体找不到时记录缺失，不凭它补造当前游戏事实。

## 当前验证

- 底部状态栏组件结构回归：4 项通过。
- 尚未完成浏览器视觉验收、上游功能全部接入或活动机制实现；本文件是执行清单，不是完成声明。
- 显式运行 `vue-tsc --noEmit -p tsconfig.next.json` 发现已有装备 `display` 必填改造尚有调用方和测试夹具缺项；默认不指定项目的类型检查不足以证明 Next 类型检查通过。错误涉及 `GearDefinitionWorkspaceDialog`、装备编辑／编译测试等，需在后续批次补齐。

## 活动资源初步定位

- 8765 服务启动前检测索引过期，已启动自动重建。旧 SQLite 仅用于候选路径定位，不能当作当前包体存在证明。
- 旧索引仍有 `Data/TableCfg/ActivityContingencyContractTable.bytes`、`ContingencyContractTable.bytes`、`ContingencyContractLevelTable.bytes`、`ContingencyContractKeyLockTable.bytes` 和活动任务组表。
- 还有 `ContingencyContractUtils.lua`、`ContingencyContractHudCtrl.lua`、详情、词条选择等 UI 脚本。
- 下一步：在重建完成的 effective 索引重新定位并实际解码这些表，由词条追到引用的 Buff／属性／触发动作；确认同版本后才加入正式转换。

### 本轮续进

- 资源服务已完成启动，effective 查询及实际 TableCfg 解码成功，确认当前安装资源仍保留危机合约。`ContingencyContractTable` 有 `indie_contract001` / `activity_contingency_contract_0`；词条定义来自 `CcTagTable`，并非等级奖励表。
- `CcTagTable` 的 `tagTerms` 直接提供 `buffId`、`termType` 和黑板。例如 100003 引用 `global_buff_cc_chr_combo_special_cc0`，黑板 `cd_scale=0.4`、`dmg_scale=-0.6`；这些键值尚未完成运行时语义取证，不能直接据键名计算。
- 重置按上游范围清除战斗设置和初始能量，保留配装模式只保留轨道配装；所有模式保留项目模板库。删除来源边界时解除直接依赖方案的继承，并在面板说明。一次项目命令可完整撤销／重做。
- 重置窗口加入现有 modal 快捷键隔离。重置／项目方案／快捷键隔离 3 文件 8 项通过。

### 显示菜单与连携控制核对

- Next 原已有八种图层开关、逐干员效果可见性；新增显示菜单内辅助线与紧凑／宽松布局选择，复用现有状态和 localStorage，不引入第二份设置。
- 上游显示菜单还提供彩色持续条开关、饱和度、明度、来源筛选；当前 Next 尚未覆盖，不能将显示菜单整项标为完成。
- 上游 `ComboCooldownControlHandler` 的 ready 只清除冷却，不能解释为凭空创建合法连携候选。cooldown 按各干员基础时长创建强制冷却。
- Next 可复用 `CombatRuntimeAssembly.#setSkillCooldowns` 的 baseDurationRatio，分别设置 1/0，并通过 `SkillCooldownAdjusted` 发布事实；仍需独立的用户标记来源和编译接线，不把它伪装成敌方事件。
- 实施前需要回归：释放确认帧前 `SkillCooldown.finishCast()` 的退款不能覆盖后插入的控制标记；重复阶段／替换共享账本不能被多次设置；候选窗口和冷却是两个状态，ready 不得自动制造窗口；非零设置目前在 `skillCooldownTimelineViz` 中被跳过，需要独立来源的可视化投影。
- 以上是代码审计结论，连携控制尚未实现。显示工具栏／图层／现有连携条件 3 文件 46 项通过。

### 连携控制首轮接线与 UI 验收要求

- 用户明确要求：功能、入口、菜单分组、图标和标记外观均与旧版一致；仅功能跑通不能标记完成。显示菜单的入口位置和完整样式仍需集中核对。
- 已新增显式 `comboCooldownControl` 标记类型，复用标记存储／拖动／删除／撤销路径；运行时单独调用控制处理器，不发布受击或技能事件。作用域为全队，mode 为 cooldown/ready。
- 复用角色已有冷却账本（包括未放在轴上的连携），按账本对象去重，显式覆盖取消旧施放的冷却退款权；ready 不创建连携窗口。保持既有标记时序：同帧技能先于控制标记。
- 入口按上游 ContextMenu：连携冷却分组内先回转后冷却、回转箭头／时钟图标，位于循环分界线之前。中文采用上游原文。
- 控制标记按上游：青色 ready／粉色 cooldown 竖线、22px 图标标记、选中白色；控制冷却区间单独投影在技能下方，采用旧版金色 2px 线、时长和末端刻度。实际结束使用运行时回执，不用基础秒数猜结束帧。
- 冷却账本、常驻连携条件、编译、外部输入、投影 5 文件 74 项通过；包含旧施放退款不能覆盖控制、未放技能账本可控、重复控制分段和实际就绪闭合。
- 页面已通过浏览器打开，无加载错误；当前浏览器窄视口未完成新菜单／标记的完整视觉验收，不能声称已完成 1:1 验收。仍需补充控制标记的键盘微移、取消选择等与旧版对比。

### 连携控制交互续补

- 对照上游 `onComboCooldownMouseDown`／释放逻辑：首次点击保持选中；已选中的连携控制标记再次点击且未开始拖动时取消选择；拖动返回原位置也不误判为点击。
- 方向键现在优先移动选中标记，复用现有文档命令和历史事务；按当前精度微移，限制在标记有效的 0 至轴终点范围，达到边界仍消费快捷键。技能块准备期规则不受影响。
- 强制冷却条时长改用共享的秒＋帧格式，避免直接输出浮点秒数。没有引用旧版模拟或状态存储。
- 文档命令、标记几何、冷却投影、冷却账本、重置和交互接线 6 文件 68 项通过。其中交互接线为结构回归，不替代浏览器操作验收。
- 待续：宽视口视觉验收、标记拖动抓取偏移核对、显示菜单剩余项目；活动词条尚未进入正式模拟。

### 抓取偏移与显示入口续补

- 标记移动现在保存鼠标相对竖线的像素抓取偏移，在投影、吸附、边界裁定之前扣除；复用实时表面坐标，自动滚动不会丢失偏移。覆盖图标两侧、不同缩放、自动滚动和轴边界。不是以已吸附帧反推偏移。
- 对照上游 `TimelineEditor.vue` 和 `TimelineDisplayMenu.vue`，将显示选项从“更多”拆到其前方独立眼睛图标入口；两菜单互斥。辅助线使用上游十字准星图标、48px 卡片、快捷键提示和勾选框（提示为 Ctrl + G），固定于滚动区域之外。布局／图层／干员可见性仍接原 Next 状态，不依赖旧 store。
- 菜单内容加入 overlay 快捷键作用域，图层和干员开关补 `aria-pressed`，三语言补显示按钮文本。
- 标记几何、交互接线、工具栏结构、文档命令 4 文件 66 项通过；两处 Vue 文件经 compiler-sfc 脚本与模板编译通过。尚未宣称浏览器视觉验收完成，也未完成彩色持续条的所有设置。

### 持续条染色接线

- 新增 UI-only `durationBarColor` 策略和组件上下文，设置只存 `endaxis:next:duration-bar-color`，不进入项目、公共协议、模拟或回执。默认遵循上游：enabled、饱和度 50、明度 90；武器／套装／干员默认关闭，异常开启，两区域开启。
- 显示菜单提供总开关、0–100 步长 5 的饱和度和明度、四类来源和轨道／敌人区域开关；复用已有三语言文本。关闭总开关时收起调色控件。
- 轨道 Buff 和敌人原生可见 Buff 的持续条共用解析器。来源来自已有 `equipment:`／`upgrade-initialization:` 回执身份，不从名称反推。现有异常 Buff 配色身份由敌人组件集中到共享 UI 策略，以便轨道也一致处理。
- 色系参照上游武器淡紫／套装青绿／干员蓝；同一原生 buffId 生成稳定色差，不随本地化、施放 ID、时间轴移动变化。色彩设置不修改状态起止、图标、层数或模拟结果。
- 边界：目前覆盖上述真实 Buff 持续条；Next 专属数值 HUD 激活高亮、技能冷却线、连携候选窗口不冒充 Buff 来源，未强行统一着色。
- 组件结构、配色策略、标记几何共 32 文件 225 项通过，新增设置和相关父组件 compiler-sfc 编译通过。完整浏览器样式验收仍未完成；默认项目类型检查已有装备 display 缺项，不能凭模板编译宣称全量类型通过。

### 桌面浏览器验收及修复

- 1440×1000 桌面视口实测显示菜单，发现图层文字使用较大默认字号导致中文截断。对照上游 `ui.css`，补 11px／600 紧凑列表、5px 7px 内边距和带分隔线的双列网格；修复后中文图层名称完整可见。来源／区域网格同步调整。
- 已实测染色总开关关闭时隐藏调色项、重新启用恢复设置；显示与更多切换。该样例轴没有可见 Buff，因此不能把菜单验收称为所有着色结果的视觉验收。
- 右键添加强制冷却标记，看到两干员分别出现 19s0f／3s0f 的冷却条；方向键从 0f 微移到 1f，撤销和重做有效。
- 发现标记位于 0f 时图标中央被准备分割线的 14px 命中区遮挡。仅提升图标层级，不把整根标记线抬到准备线之上；实测中央点击正常选中，边缘抓取拖动 120px 正确移动 60f。
- 所有测试标记和移动已通过历史撤回，染色设置恢复启用；浏览器临时视口已重置。验收页控制台 error 记录为空。尚待含 Buff 场景的染色视觉检查、短视口滚动验证及更多上游功能回归。

### 含异常 Buff 场景验收与非技能动作核对

- 临时启用样例弧光终结技，实际产生导电条。敌人区域染色开关将其背景从 `rgb(199,188,86)` 切到 `rgb(140,140,140)`，title、left=776px、top=2px、width=696px 不变；开关恢复后撤销技能启用，未保留测试轴修改。此次只验证敌人导电条，其他来源着色仍以策略测试覆盖。
- 1440×600 下显示菜单滚动可达最下方区域开关，辅助线卡片保持固定；临时视口已恢复，控制台 error 为空。技能库 HTML 拖放在本次自动化中未成功产生技能，不据此断言实际拖放功能正常或损坏，改用已有技能启用进行染色验收。
- 上游 `d39d8d4c` 用 nonSkill 分组解决梨诺终止动作误算技能释放的问题。Next 生成定义已包含 `chr_0035_liino_normal_skill_end`、nativeSkillType=extraActiveSkill、SwitchToBuff(asSkillCast=false)，属于单技能执行路由，而不是库分组语义。
- 新回归通过完整技能枚举（含 replacement）取得真实生成定义，验证战技进行中按该终止动作只执行结束信号 Buff，继承当前施法身份；不改变当前技能、不扣技力、不发新技能语义事件、不调用开始／费用／结束回调。操作执行器在该测试中为 spy，不能将其扩大为姿态 Buff 已完整清理或所有配装消费已端到端验证。
- SkillRuntime／AbilitySystemRuntime／染色策略 3 文件 61 项通过。此次未更改模拟机制，仅补真实定义路径回归；后续先补姿态与一次性效果组合验收，再推进倍率详情及活动词条。

### 梨诺终止动作完整路径缺口修复

- 生产回归暴露此前“旁路调用成功”不等于姿态结束：原始 `buff_chr_0035_liino_skill_end` 的战技分支创建 `buff_chr_0035_liino_normalskill_end_active`；生成产物却为空分支。SkillData 内已存在监听此 Buff 的 eventBuffIdMatch → 清理／设置冷却／jumpTimeline 路径。
- 根因：`collectCompiledBuffIdentityReadIds` 只收 buffIdStackCompare，没有收 eventBuffIdMatch。信号 Buff 无图标、无属性、无动作，被无可见效果闭包误剔除。公共分析现在同时保护事件读取的 Buff ID，不添加梨诺专用白名单，也不创建新 nonSkill 协议。
- 从 `tmp/game-data-sources-hybrid-20260905` 的同批 SkillData、BuffData、TableCfg-current 完整重生成梨诺，审计输出仍在 tmp。时间膨胀目录参数必须用 `timeDilationCatalog.generated.ts`，不是运行时包装文件 `timeDilationCatalog.ts`；旧 README 示例包装路径已不适用。
- 新生产用例覆盖 2f 与 120f 的终止操作。120f 位于监听窗口内，music_tag、music_damage、spellInfliction_extraattack 同帧结束；结束动作没有 SkillStarted／SkillEnded／SkillCostApplied。2f 在监听启用前，只验证旁路和信号创建，不强行把原生未监听的事件回放为立即结束；相关可操作性警告仍需后续核对。
- 原生监听开始位置为技能局部 90 帧；不得根据直觉扩大监听窗口。此前 60f 探针也早于窗口，不能用它单独证明修复无效。
- 公共引用分析／关键词／注册干员生产模拟／技能运行时 4 文件 103 项通过。剩余：其他干员中同类事件信号的重新生成审计、终结姿态终止、具体一次性武器／套装消费对照。未声称全体生成产物已同步公共修复。

### 全干员事件身份审计与终结技重入缺陷

- `generatedBuffIdentityClosure.test.ts` 遍历全体注册干员的 eventBuffIdMatch 与共有/私有 Buff 定义，
  锁定缺失项恰为 snowshine、catcher 对 `buff_eny_0018_lbtough_pre_catch` 的两处监听。
  同批 `eny_0018_lbtough_skill07`、`eny_0018_lbtough_skill07_endinggame`、
  `eny_0058_agdisk_skill08` 存在创建引用。这些不是漏删的干员结束信号；测试保留精确缺项，
  防止未来新增遗漏被广泛的 enemy-ID 白名单掩盖。本检查只覆盖静态事件身份，不等于所有动态 Buff 引用闭包已审计。
- 新生产重现：梨诺终结技 1f，battleSkillEnd 180f。监听局部 137f 后已启用，
  ultSkill_end 使 buffIdStackCompare 成立，随后创建 normalskill_music_cry_vfx 时
  同步再次触发 buffApplied，导致栈溢出，尚未执行 JumpTimeline(540)。
  原始 Ultimate SkillData 确实使用层数查询，不能擅自改成“当前事件 Buff ID 匹配”。
- 已核对 combat-spec 的 `ability-event-action-ordering.md`、`sequence-execute-policy.md`、
  `EventListenerAction.cs`、`AbilityEvents.cs` 与 `SequenceAction`：监听注册复用序列，
  子项存在 Pending/Executed 状态；Next 的 CombatEventListenerStep 每次创建序列，且条件转换
  将判断和后续体组合成一项。需要继续用原生执行状态证据辨别这两个差异的作用，不先写全局递归屏蔽。
- 重现用例明确 skip（防止 CI 递归产生巨量异常），普通终结技伤害/满血治疗用例继续运行。
  本轮五文件 104 passed / 1 skipped；未修改 combat-spec 的机制，也未宣称终结技结束已修复。

### 终结技结束重入修复（后续结果，覆盖上一节未解决状态）

- combat-spec `SequenceReentryTests` 验证既有原生序列状态：已经完成 Check 的同一序列在
  后续动作同步重入时返回 false，外层尾部与下次独立执行仍正常。未新增全局事件禁重入规则。
- Next CombatEventListenerStep 改为注册期创建并复用序列，事件载荷在执行时绑定、嵌套返回后恢复；
  使用监听动作的实际操作环境，而非回退到宿主根环境。
- 运行时把无 else 且非 alwaysNext 的短路条件树展开为“独立条件条目 + 后续条目”。
  因而 CreateBuff 发布事件时，前面的 Check 已有执行状态；含 else 或 alwaysNext 的分支仍用
  ConditionalStep。先前尝试直接改变生成树会造成无必要的生成物和导图变化，最终未采用；
  公共协议、编译器输出结构、现有生成条件树不变，也无需重生成全干员来接收本修复。
- 生产重现取消 skip：1f 开启终结技，180f 终止，回执验证 JumpTimeline 到 540、
  ultskill_music_damage 同帧结束、无新 SkillStarted/SkillCostApplied；正常终结技伤害和满血治疗仍通过。
- 公共监听测试另外锁定两次独立事件均能响应、嵌套事件不会污染外层尾部载荷、End 后注销。
  79 文件 / 1051 项通过；C# 新增状态机回归 1 项通过。未据此声称首动作自身重入或所有事件宿主均已完整验证。
- 最终全仓 Vitest **554 文件 / 6605 项全部通过，无 skip**；C# 序列/时间轴相关 13 项通过。
  首轮全仓仅有 enemyEffectRetention 仍要求旧 SPECIAL_BUFF_COLORS 字符串，已改为检查现有
  resolveDurationBarColor 接线；没有为了通过断言改回旧的颜色实现。
- 后续继续非技能动作的一次性配装消费对照，再按上游 `80d23a33` 核对 Hit 详情的倍率来源入口与展示。

### 实际配装消费与原生施法前事件门控

- 组合回归证明旧装配层仍在 180f 的梨诺结束动作上触发 beforeCastSkill：终结技能量套
  的首战技返还提前消耗。SkillStarted/SkillCostApplied 回执缺失不能替代底层事件断言。
- AbilitySystem 准备并按实际路由消费 BeforeCastStart 回调；原生 Buff 旁路的 asSkillCast=false
  丢弃回调，true 在应用费用/Buff 前调用。未命中旁路时保持正常启动事件；PostSkillCast 独立
  路径继续在既有准备之后执行一次。测试覆盖 true/false、旁路未命中的正常启动及延迟启动。
- 实际 `suit_usp01` 三件与 `wpn_lance_0011` 随梨诺上轴，50 技力首次返还保持到 300f 真正战技，
  武器 normal_magic_up 首次在 300f，540f 战技仍触发，180f 结束动作不触发。
  未宣称所有武器的一次性效果逐件穷举；这是两个真实消费者的端到端回归加公共门控修复。
- 复刻库现有 SwitchToAddBuffTests 7 项通过。全仓 554/6608 通过，追加武器断言后相关 4/117 通过。
- 倍率详情差距已定位：上游技能倍率可展开 base 与 stack/attribute/postMultiplier 来源，
  直接倍率用 title 列来源。Next TimelineHitDetailDialog 尚无这两处入口，HealthDamageDetail
  只有合计数据；PlayerDamageOperationExecutor 的 directDamageMultiplier 是 calculation、
  weakness/shelter、runtimeExtension、ignite、physicalInfliction 的乘积。
  后续从执行边沿记录实际分项和来源，不以合计值或当前面板推测历史 Buff 来源；暂不增加空壳折叠按钮。
