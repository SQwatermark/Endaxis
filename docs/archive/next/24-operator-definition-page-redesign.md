# 干员定义子页面重设计

> 历史记录：保留当时的设计、证据与验证范围。文中的“当前”“下一步”和机器信息不再代表现状；开发以文档总览和当前交接为准。

## 待讨论：干员外部展示资源与编辑权限（2026-09-08）

**状态：仅记录问题，等待用户与其他开发者讨论决定，不实施方案。**
用户尚未确定自定义定义应开放多大自由度，不能因为编辑器追求完整就自动开放全部资源字段。

现状问题：

- 基本信息的“资源来源”直接编辑 assetSlug，隐式联动头像、技能图标和本地化回退，
  不是按用户具体编辑意图划分的配置；仅改标签或换下拉框不能解决耦合。
- 外部资源主要包括干员头像/立绘、技能/天赋图标、专属状态图标、专属 HUD 纹理，
  以及干员/形态名称、技能名称与富文本说明、逐等级天赋文本、各档潜能文本。
- 专属 HUD 的部分纹理路径仍硬编码在 OperatorPassiveUiWidget；表现规则本身与纹理
  不是同一层，不能把计数、阈值等定义一起当作外部图片资源处理。
- 文件位于干员目录不代表归属于干员根：技能图标、Buff 图标和 UI 组件纹理可能应
  分别归属对应对象。公共元素/职业/属性图标和术语不应复制为干员私有资源。
- 已导出不等于正在使用，例如 portrait.webp 的有效消费入口仍需核实；
  当前盘点不是完整依赖审计，也不据此新增编辑字段。

讨论前应补齐每项资源的读取入口、实际所属对象、是否使用及公共共享关系，再讨论：

1. 允许改名称/说明到何种程度，是否支持多语言与富文本编辑？
2. 纹理仅允许选择内置资源，还是允许外部导入？技能、天赋、状态图标能否独立更换？
3. 专属 HUD 是否只允许已有样式，或开放布局/纹理配置？
4. 展示配置与战斗定义如何关联；项目导出、重载和资源缺失如何处理？
5. 既有 assetSlug 如何兼容，是否需要新的显式引用结构？

独立资源引用、逐项资源编辑、“从内置干员复制展示配置”均只是先前讨论的候选方向，
并非架构决议。讨论完成前不修改契约、资源生成链、assetSlug 解析或扩大编辑权限。
保留当前功能；其他已确定的页面、导航与历史工作可继续。

2026-09-08。设计约束与待实施方案，不代表现有页面已完成迁移。

实施进度：正式天赋/潜能入口已接完整效果图，旧摘要/二级行为入口已移除。
步骤、递归战斗条件复用公共节点和 Inspector；根历史跨页恢复已实测。
BuildCondition 节点化、顶层条目复制/排序与添加菜单交互屏障已接入，复杂样例仍待完整验收。
不提供天赋“预览等级”：同图编辑全部逐级值，不通过切换等级查找差异。
角色行为、条件初始化、操作规则已接图；状态表现、来源诊断、信赖已独立成页。
条件初始化已实测跨页撤销回到条件字段。连携入口也已接注册/响应同图与根历史，
但成员操作和语义显示仍待补；技能库组织等仍未完成，不代表全页验收通过。

## 页面身份

总览中的每个编辑入口对应独立页面，不进入混合分类页后再寻找目标。
每个天赋和每档潜能直接进入各自对象页；不得出现跨天赋/潜能分类标签。
同类对象可以提供轻量跳转，但不常驻宽列表挤占编辑空间。

页面身份与数据身份分离：页面共享干员根草稿、根保存与撤销历史，不产生子保存。
导航不产生编辑记录；撤销必须恢复类别、固定槽位、对象路径与节点位置。
现有 section/index/对话框开关不是新页面划分的依据。

## 统一视觉语言

- 页头：返回总览、对象名称、对象类别。身份清晰，不重复两层标题和返回按钮。
- 主区：只处理入口对应的任务；表格、表单、节点图按真实数据关系选择。
- 图页：完整可漫游画布与固定右侧 Inspector；默认选根节点，默认展开两级。
- Inspector：对象名称在先，再按语义排列本层字段；不递归展开子对象。
- 图的连线表示结构归属，不暗示不存在的执行先后。固定端口与可变条目明确区分。
- 强调色用于选中、焦点和主动作，不把每个区块都画成高亮大框。
- 标签、输入、分组间距统一；长说明次要呈现，不抢占主要编辑区域。
- 添加是动作：节点端口/上下文菜单添加，选类型即插入；不得常驻选择器等待再次添加。
- 空集合不画大片占位卡；保留清晰添加入口。不可用与缺失字段须解释，不静默隐藏。
- 根页底部是唯一保存与历史栏。各图可代理同一历史，但不能形成新的提交边界。

## 入口与内容

| 入口            | 页面主体             | 本层字段与结构边界                                         |
| --------------- | -------------------- | ---------------------------------------------------------- |
| 基本信息        | 紧凑属性表单         | 名称、资源、分类；游戏身份与来源只读且次要                 |
| 属性成长        | 固定六列数值表       | 1/20/40/60/80/90；信赖单独小分区，不逐级切换               |
| 单个技能        | 完整技能图           | 等级来源、费用等在根 Inspector；条件、监听、序列等在图中   |
| 单个天赋        | 完整天赋效果图       | 根有天赋等级数量；修正、初始化、监听、被动属于此根         |
| 单档潜能        | 完整潜能效果图       | 槽位即潜能等级；无等级数量；其他效果结构与天赋共用底层能力 |
| 技能库组织      | 分组与有序成员编辑   | 只影响显示/放置，与等级来源和执行规则分离                  |
| 角色黑板        | 键值表与派生初值结构 | 字面值用表；带条件的初始化用图，不与技能黑板混同           |
| 角色行为        | 角色行为图           | 常驻被动、监听与所属序列，按实际归属连接                   |
| 连携条件        | 条件结构图           | 优先级与根属性在 Inspector；监听/判断复用公共结构组件      |
| 操作选择规则    | 路由结构编辑         | 模式、槽位、路由以真实引用表达，不以技能库分组代替         |
| Buff / 能力实体 | 各自对象目录与对象图 | 两类独立入口；嵌套定义在所属结构内编辑，引用可定位         |
| 状态表现        | 表现配置页           | passiveUi 等表现字段，不与战斗行为编辑混在一起             |
| 来源与诊断      | 只读说明及问题定位   | 转换结论、兼容别名等，不伪装成普通效果字段                 |

以上是入口职责，不要求总览给所有项目同等视觉权重。技能、天赋、潜能直接可见；
角色级高级入口按语义紧凑排列，不能再汇入一个混杂的“运行数据”长表单。

## 天赋与潜能首批落地

对照真实 OperatorUpgradeDefinition：levels、simulationNoEffect、modifiers、
initializationSequence、eventHandlers、passiveSkills，逐项覆盖，不凭示例删字段。

1. 根节点表示当前天赋/潜能，根 Inspector 仅有自身属性；模型无效果原因是次要诊断区。
2. 构筑修正与运行行为是不同作用阶段，在图中用有名称的结构端口区分；不能连成执行流水线。
3. 修正条目为可选节点，其递归条件/效果继续画图；纯数值与逐等级数值留在 Inspector。
4. 初始化只有一条可选序列；已有序列时不再显示可覆盖原数据的“创建序列”。
5. 监听节点连到条件与响应序列；被动节点连到其真实子结构。不再“摘要→编辑行为”。
6. 先用真实简单潜能与复杂天赋验证同一视觉语言，再扩展其他页面；不平行拼多套布局。

## 验收

### 2026-09-08 当前实施边界

各入口已具备独立页面，但不能据此视为完成。后续按以下顺序收束：

1. 图页的本层字段与递归结构完整覆盖：特别检查技能变体、输入窗口、旁路，以及Buff/能力实体的旧嵌套表单残留。
2. 统一字段定位与历史：诊断和使用点已能定位四类真实技能容器；仍需字段聚焦、更多图节点的跨页恢复。
3. 逐页真实操作验收：新增/删除/复制/排序、取消和保存重开，窄窗口与长文本；不能由静态源码断言替代。
4. 最后统一间距、标题、辅助文案和本地化。核心顺序仍是先入口职责完整，再打磨细节。

根工作区始终保留唯一保存/撤销栏，不再隐藏它并提示“先保存行为、再保存干员”；所有对象页共享根草稿。

技能库四类成员的创建、复制、同容器排序及删除已统一；放置引用采用添加后选择，保留无效草稿。
Buff/能力实体使用点采用默认收起的公共辅助区；允许删除有引用定义，保留引用并报告缺失。
能力实体目录完整草稿模式不再根据 customDefinitions 存储字段推断“自定义”来源；独立覆盖模式保持原语义。
presentationVariants 当前只有类型声明，无生产或消费，不以编辑空壳伪装支持；后续需明确去留。

深色主题先验收。每页检查入口、标题、默认选中、字段完整性、图与 Inspector 归属、
窄窗口长文本、空状态、添加/删除、返回、跨页撤销/重做以及根保存重开。
必须以真实正式组件和真实定义验证；截图或静态原型通过不代表编辑流程通过。

### 2026-09-08 根保存闭环实测

正式组件隔离预览（宿主只在内存保留 save 输出，不写真实项目）完成以下连续操作：

1. 总览修改名称，力量 Lv.40 从45改52；进入信赖规则，将第二节点从15改18。
2. 点击唯一根保存，确认对话框关闭；重新打开，三项值均保留，撤销禁用。
3. 修改名称为另一值后取消；重新打开仍为上次保存名称，成长和信赖值保持不变。

该证据证明跨页草稿合并、保存输出重载与取消隔离，不能证明真实项目存储、所有深层定义保存和模型模拟正确。
下一轮优先验收技能/Buff/能力实体的跨对象引用编辑与根保存，不继续只累计单字段聚焦案例。

### 2026-09-08 Buff 跨对象闭环补验收

- 实测发现使用点只跳到天赋根页；修复为复用字段定位，选中实际初始化 applyBuff 节点，返回栈仍保留原 Buff 详情。
- 正式深色隔离预览修改佩丽卡天赋 Buff 优先级0→2，使用点往返后根保存、关闭、重开，值2与一处引用均保留，历史重置。仅验证内存宿主 save 输出，不代表真实项目持久化。
- 角色事件、连携条件、状态表现引用复用相同页面路由并增加回归；尚未逐一浏览器验收。技能和附属定义内部的深层引用定位仍待处理。
- 本次截图也显示 applyBuff 身份字段聚焦到了说明按钮；节点定位已正确，但字段内优先聚焦可编辑控件仍需统一完善。

### 2026-09-08 结构复审：Buff 页仍未满足目标

本轮重新按协议与实际组件核查，而非按已完成的小修数量判断进度。
当前最高优先级是 Buff 页结构迁移，暂不继续逐个打磨实体输入框。

证据：

- `BuffDefinitionGraphEditor.vue` 的根节点仍使用 `BuffStepEditor` 的 definition-only / inspector-only 模式。
- `BuffStepEditor.vue` 该模式仍渲染属性修正、伤害修正、槽位替换、治疗修正、韧性修正、护盾、关键词强化等集合编辑器。inspector-only 只隐藏了生命周期编辑，不代表本层模式完整成立。
- `BuffDamageModifierEditor.vue` 仍在同一表单递归编辑修正器、条件及 processors；`BuffDamageModifierConditionEditor.vue` 又递归处理 not/all/any。
- `buildBuffStructureMindMap` 把 damageModifiers[i].conditionProgram 直接挂在 Buff 根下，路径虽正确，结构归属仍缺少修正器父节点。

迁移必须以真实包含关系为依据：

| 对象             | 图中的位置               | 本层 Inspector                                         |
| ---------------- | ------------------------ | ------------------------------------------------------ |
| Buff 根          | 对象根                   | 叠加策略、优先级、时钟、持续/触发参数、标签等          |
| 伤害修正集合     | 根的固定端口             | 集合说明；添加由图端口触发                             |
| 单条伤害修正     | 集合成员                 | enabledSide；条件类型切换须明确会替换什么              |
| condition        | 修正器的条件子树         | 当前条件自身参数；not/all/any 子条件在图中             |
| conditionProgram | 同一修正器的条件程序子树 | 序列本层；不得独立挂在 Buff 根下                       |
| processors       | 修正器的有序子成员       | 单处理器的 kind、side/targetSide、zone/attribute、数值 |

condition 与 conditionProgram 在协议中互斥，但错误草稿不得被加载时静默删改。
DamageModifierCondition 不是 CombatCondition 的同义别名，不可强转复用而丢字段；可以复用图布局、字段组件、历史与交互基础。

下一实施批次必须形成完整纵切：伤害修正集合新增 → 单条本层编辑 → 条件/处理器节点 → 复制/排序/删除 → 根历史 → 保存重开。
独立 Buff 与技能内联 Buff 都用同一投影和本层组件。不能只画节点却继续把整组表单塞在右侧，也不能为了迁移先隐藏尚未接好的字段。
完成这一批后，再以相同方法迁移其他结构集合；标量表、简单标签列表、逐等级值不必机械拆成节点。

这次复审不表示已完成上述迁移。前几轮保存/返回与实体表单改进仍有效，但不足以证明“所有入口面板的大方向”已完成。

### 2026-09-08 剩余 Buff 结构的协议核查与迁移顺序

不能把其余集合统一套成伤害修正器。按当前公共协议安排：

| 根字段                                  | 图中结构                                       | 本层边界                                                                                            |
| --------------------------------------- | ---------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| attributeModifiers                      | 集合端口 → 属性修正成员                        | 属性选择、槽位、常量/黑板值、目标、converted 来源；主/副/全部属性是联合类型，不是字符串             |
| skillSlotReplacements                   | 集合端口 → 替换成员                            | 槽位、目标技能、恢复技能、冷却继承位；不是执行子步骤                                                |
| keywordEnhancements                     | 集合端口 → 强化成员                            | 触发 Buff ID 列表、操作、目标键、初始值、值；ID 列表不拆成逗号文本                                  |
| healModifiers                           | 集合 → 修正器 → 可选条件 / 处理器集合 → 处理器 | healer/receiver；三种条件目前均非递归；两种处理器各自固定 timing                                    |
| poiseModifiers                          | 集合 → 修正器 → 条件树 / 处理器集合 → 处理器   | attacker/defender；条件只有 casterControlled、eventDamageTagsMatch、all；不得借用伤害条件的 not/any |
| shields                                 | 集合 → 护盾 → 吸收规则集合 → 规则              | 护盾值可为常量/黑板或属性公式；公式是一个值编辑器，吸收规则是独立有序成员                           |
| presentation / childPresentations       | 自身表现端口；子表现集合 → 子表现 → 表现对象   | 图标、可见位置、排序等保持纯表现语义，不能因不参与伤害而省略                                        |
| sustainedProtection / role / spellBurst | 各自可选对象端口                               | 不再混在“高级原生语义”大折叠中；只有确有对象时才显示其字段                                          |

下一批先迁移三个非递归成员集合（属性、槽位替换、关键词），建立通用数组操作基础；再迁移治疗/失衡的条件与处理器；最后护盾与表现对象。通用的是结构操作和历史，不是把不同条件协议强制合并。

每批必须包含新增、选中本层编辑、复制、排序、删除、根历史和入口渲染。不能仅在图上挂一个节点却继续在 Inspector 内展开整组表单。原字段入口应在对应新路径完整后移除。

本次核查发现属性修正旧文本框无法表达 attribute 的 main/secondary/all 对象，已先补属性选择控件，保留其延迟选择语义；这只是数据完整性修复，不代表上述集合迁移完成。

### 2026-09-08 非递归集合实施结果

属性修正、槽位替换、关键词强化三个集合已接入公共节点投影及四种图宿主。本层编辑复用字段组件的 singleEntry 模式，去除成员内集合按钮、折叠和卡片边框；结构编辑只在图中执行。图模式根 Inspector 已移除这些重复集合，普通表单路径保留。

操作层泛化为 buffGraphOperations，类型仍分开，不能跨集合粘贴。共用的默认成员创建值受公共协议类型约束。关键词 ID 使用原子列表，不做 split/trim/filter。

正式深色独立 Buff 页已逐个新增和查看三个成员检查器；组件测试覆盖嵌套路径与根历史。全量 UI 回归同步修正技能图“不应展开内联 Buff”的旧断言，并检查真正包含路径。其他内联入口的浏览器端到端与拖放体验仍需继续补验。

后续优先治疗/失衡的条件与处理器子树，不再反复打磨这三个集合的细小样式；护盾与表现对象随后处理。

### 2026-09-08 治疗/失衡结构实施结果

两类修正器的条件与处理器均已入图，复用同一包含路径及历史操作，但保留不同协议。失衡递归仅支持自身协议的 all，治疗条件保持非递归；两类处理器的 timing 从其联合类型确定并展示，不另设一个可产生无意义组合的时机选择器。

失衡的 eventDamageTagsMatch 使用 DamageTag 枚举，治疗 healTagsMatch 使用 GameplayTag 路径，二者不再共用错误的编辑控件。根 Inspector 已移除已迁移的两类集合。下一步转向护盾和表现对象；此前跨入口浏览器验收欠账仍保留，不因节点化完成而自动视为验收完成。

### 2026-09-08 护盾结构实施结果

护盾与其有序吸收规则分层入图。本层护盾检查器保留无限值、数值/属性公式、吸收次数、优先级及消耗行为等自身属性，不嵌套吸收规则集合；单条规则检查器只编辑伤害类型、吸收比例和系数。公式属于联合类型值编辑器，不额外拆成结构节点。

新增默认值由原表单与图命令共用。切换值来源会替换数值/公式并给出提示，同类型选择不重置已有黑板；结构操作和撤销复用统一根历史。图模式移除旧根护盾集合，未迁移的表现和高级可选对象保持可编辑。

类型检查与 UI 全量 252 文件 / 1267 测试通过；深色正式隔离预览验证新增护盾、公式切换和独立吸收规则检查器。浏览器跨入口、拖放、删除撤销及保存重开仍需补验。下一批处理自身表现与子表现的真实包含关系，不因图标不参与模拟而省略相关编辑能力。

### 2026-09-08 表现结构实施结果

Buff 自身表现与子表现集合已入图，子表现成员只编辑目标 Buff ID，再通过其必填表现节点进入图标、显示位置、进度和动画等本层字段。排序优先级作为可选结构对象另设节点，不嵌入表现检查器。布尔字段紧凑同行排列，保留“未设置/是/否”三态。

可选对象的空端口支持新增和粘贴，但不覆盖已有对象；必填子表现对象不允许单独删除。清空表现最后一个字段不删除对象。图模式移除根表单重复入口，普通表单仍兼容。子表现允许填写无效 ID，定义检查报告问题，不妨碍草稿编辑。

正式深色隔离预览已验证自身/子表现进入与排序删除撤销恢复具体数值。UI 全量 254 文件 / 1270 测试及类型检查通过；跨宿主拖放、保存重开和完整字段验收仍待完成。剩余可选原生对象按相同包含关系原则处理，随后统一审查各入口信息层级。

### 2026-09-08 可选原生对象实施结果

持续保护、元素语义和法术爆发参数独立成为可选对象节点，复用原字段控件的本层模式；结构启用/移除归图命令负责。来源技能施法身份仍是根的标量，直接显示，不为了统一外观创造伪对象节点。

三个对象不支持数组式排序；同类型可复制到空端口，不能覆盖已有定义。元素语义同类型选择不重置参数，更换类型明确告知替换且可撤销。原始爆发倍率注明仅证据用途，不暗示其直接控制结果。

深色正式隔离预览已检查三个入口和法术异常类型切换；类型检查与 UI 256 文件 / 1276 测试通过。后续先整理根的叠加、时间触发、标签与初始黑板的信息层级，然后集中回查各入口导航/编辑/历史/保存闭环；全目标仍未完成。

根检查器补充（2026-09-08）：本层字段以叠加、时间触发、标签三个静态分区排列，不新增折叠。初始黑板在图模式直接显示，窄栏使用两行原子条目；名称与帮助文本各司其职。正式深色预览已验证独立 Buff 优先级保存重开，不将这一证据外推为所有内联宿主通过。下一批应回到各入口的完整操作矩阵，不再围绕 Buff 局部无限打磨。

入口复验记录（2026-09-08）：正式技能页的调度节点新增曾错误使用调度对象路径，现修正到其 sequence；内联 Buff 编辑与字段撤销焦点也修复。浏览器验证新增→进入定义→持续时间修改→撤销恢复及控件定位。仍发现总览技能入口的返回文案/目的地沿用技能库组织，下一批必须记录实际进入来源，避免让使用者被带到未经过的页面。

技能入口来源修正（2026-09-08）：总览与技能库组织两种入口分别记录视图来源，返回、面包屑、引用往返和撤销恢复均沿用它。来源不进入游戏定义；组织中新增技能仍返回组织。正式深色页面分别验证两种路径，后续继续查天赋/潜能与角色逻辑入口，不能将导航修复视作所有页面验收完成。

## 2026-09-08 节点树层级收束（覆盖上述属性对象一律入图的做法）

节点图表示行为结构，不按契约对象的每层嵌套机械生成节点：单一子动作序列直接挂在父行为下；只有多组子关系才保留命名端口。纯属性分组优先交由 Inspector 的可折叠分区组织，不能仅因为字段是对象便单独开节点。

已实施：共享图展示层压平黑板作用域、循环、一次执行、延迟回调的单序列端口。定义及逻辑树不变；父节点仍负责选中、复制、删除，新增/粘贴/向内拖放转交原序列路径；子动作 ID、路径不变，保留历史定位。条件分支、选择分支不受单个已配置分支数量影响。

表现属性已实施：自身表现、排序优先级和子 Buff 表现统一由根 Inspector 编辑，正式图不再生成这些属性节点。原生 details 保持字段挂载，统一定位可展开；显式启用/移除对象，清空字段不隐式删除，子表现的必填对象保留。沿用统一属性句柄与历史，未建立局部副本。

可选属性已实施：持续保护、元素语义、法术爆发参数移回 Inspector，各自及来源技能施法身份独立折叠，摘要显示是否启用。统一根属性句柄承担新增/移除与历史定位，沿用原有类型切换及数值校验，不改变模拟语义。

护盾参数已实施：damageAbsorptions 是伤害类型、比例、倍率参数表，不再生成图端口及成员节点；归护盾 Inspector 的可折叠区，保留新增/编辑/复制/排序/删除。修改通过当前护盾的属性句柄提交，历史定位护盾及吸收参数区。护盾本身仍作为独立图成员，复制包含全部吸收参数。

旧入口清理已完成：删除表现图生成器及表现节点 Inspector，可选属性模块仅保留默认值；移除 7 类已迁走属性的图类型、菜单、剪贴板和添加命令，表现/高级属性组件删除旧 layer 模式。归属边界测试禁止图重新接管这些属性；字段编辑器及统一历史保留。

子表现条目体验已补（2026-09-08）：复制深拷贝嵌套参数并分配未占用 ID，新增/复制/排序/删除以及字段修改定位具体条目。条目、ID 与嵌套表现区使用真实属性路径，根属性句柄接收集合内位置，撤销沿统一历史返回 Inspector 并展开对应折叠区；不得为数组成员另建历史或恢复图节点。隔离浏览器验证复制往返、折叠后改名撤销定位，窄栏输入保持可用。

跨宿主接入（2026-09-08）：装备附属 Buff 已删除局部草稿、历史和子保存/取消入口，直接读所属贡献并写入同一根历史；返回仅导航。外层效果索引用 objectId、Buff 页用 page，节点和字段仍用 path/propertyPath，不能用 Buff ID 覆盖效果身份。撤销/重做可重新打开对象页；对象已不存在则选仍存在的对象或空状态。子页重开不清根历史，仅最外层会话重置。正式共享组件的隔离浏览器验证返回后根撤销自动重开及根保存；武器/装备/套装完整外层弹窗视觉验收仍未全部覆盖。

待实施：继续补细粒度字段定位和复制体验，审查其余修正器属性层级。真实条件、处理器和动作序列关系仍由图编辑。不能只隐藏节点而丢失编辑入口。跨宿主实际交互与保存重开仍需浏览器验收。

## 参考与取舍

正式保存验收补充（2026-09-08）：佩丽卡通过时间轴技能库进入自定义干员，新增 Buff 的表现排序与元素语义可保存、重开；后续未保存修改取消后不污染已保存定义。项目文件往返由宿主持久化回归覆盖所有迁移属性。定义保存只提交当前项目草稿，不能声称自动持久化到文件或刷新后保留。
弧光保存阻塞已修复：引用索引与必需定义依赖不是同一概念。finishBuffsById 的直接 buffIds 参数标注为实例筛选，保留使用处导航，但无本地定义不阻止保存；applyBuff 等创建引用仍严格校验，同一 ID 的其他引用不随之豁免。此边界对应生成器 buffReferenceClosure 的创建边闭包及 Endaxis BuffContainer 的实例筛选，不补造定义，也不将所有查询泛化为可缺失引用。正式浏览器已验证弧光新 Buff 保存重开及取消隔离；根保存回归验证同 ID 的结束/施加分别处理。

浏览器验收补充（2026-09-08）：隔离浏览器使用正式 Buff 图组件验证表现排序及护盾吸收参数的修改→折叠→撤销自动展开→重做。
窄栏子 Buff ID 改为独占一行，排序/删除按钮另排，图标字段标签置于输入上方；1100px 窗口复验输入宽度 277px/211px。
正式技能弹窗加载已检查，跨干员/武器/装备全部入口及保存重开尚未完成，不能外推为完整验收。

- [Blender 编辑器设计规则](https://developer.blender.org/docs/features/interface/human_interface_guidelines/editors/)：统一编辑区域职责，同时保持不同编辑任务可辨认。
- [Unreal Blueprint Details](https://dev.epicgames.com/documentation/en-us/unreal-engine/details-panel-in-the-blueprints-visual-scriting-editor-for-unreal-engine)：所选对象的属性由 Details 承担。

借鉴职责与信息层级，不照搬其庞大工具栏、窗口体系或皮肤；Endaxis 保留已认可的紧凑实例面板视觉基础。
