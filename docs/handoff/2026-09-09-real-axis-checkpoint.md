# 2026-09-09 真实分享轴审计与推送检查点

## 接续目标

继续使用真实旧轴验证新版模拟与显示完整性；不是把新版总伤害强行调成旧版。
当前工作树 `refactor/common-game-data`，旧版只读参考 `D:/Projects/Endaxis-upstream-main`
（参考版本 `4dadc55f`）。详细证据见
[分享轴审计](../../tools/legacy-timeline/public-share-evidence.md)与
[当前快照](current-context.md)。本检查点优先于下方历史条目的“尚未修复/未推送”。

## 本批成果

- 私有三个轴与公开“别赛羊诀”逐步建立构筑、伤害、资源、打断、Buff 生命周期对照。
  普攻伤害乘区、主控条件、法术爆发与复合反应、腐蚀周期、终结技持续命中等已积累
  证据及回归；不是所有新版告警都属于新版错误。
- 接通失衡 Buff 原生生命周期；combat-spec 的两个待推提交分别实现该生命周期并记录
  Endaxis 集成。Endaxis 同批还修复静态/运行时攻击加成槽、攻击快照回执、提前消费的
  Buff 生命周期发布、投射物独立回调、被动上下文隔离与部分被动生命周期事件。
- 转换和运行时改用导出的 SkillSetting 资源参数，并修正把终结技能量恢复误套入主控
  技力恢复限制的问题。原始资源账本与旧版差异分别记录，不调整输入掩盖差异。
- 修复艾尔黛拉溅射目标排除丢失：主目标不再额外承受只属于其他目标的溅射。
- 修复公开轴诀终结技变体映射；来源显示统一枚举基础、形态、替换与路由技能，武器
  Buff 来源使用发布时捕获的资源显示身份，避免内部 ID 直接成为名称。
- 敌人 Buff 栏使用原生头顶显示配置，隐藏内部计算 Buff 而非删除模拟效果。
- 持续技能使用实际回执投影逐次命中，点击携带执行帧，不再只显示/查看首次执行。
- 命中连接统一传递 stepKey，动态回执可提供锚点；纵向位置使用页面实际标尺与轨道
  布局，消除固定高度造成的偏移。连接仍是定义级身份，重复执行锚定首次命中。

本批变更较多，具体提交以 Git 为准。最新代码提交 `5377ff2d`；此前持续命中修复
`3ee18b0b`，来源枚举修复 `99605b53`，溅射转换修复 `584cfc74`。

## 原始基线与验证边界

- 公开轴 ID：`6a8db78895147370855b45ed`，标题“别赛羊诀”。
- 原始分享：`tmp/public-share-candidates-20260908/6a8db78895147370855b45ed.json`；
  转换原轴：`tmp/public-last-rite-arcana-corrected/project.json`。
- 当前原轴 77 笔伤害：实际约 `792780.458805`，逐笔期望之和约 `812599.970275`。
  后者不是包含状态性随机分支的整轴数学期望。原始主控切换为空，不能把改变主控后的
  诊断轴当作原始结果。所有中间产物留在忽略的 tmp，不随 Git 推送。
- 艾尔黛拉终结技 11 帧命中投影已逐帧验证；正式深色页面点验首笔期望 4715、第二笔
  8199，第二笔截图可读；未逐个点击其余九笔，不称整轴像素验收。
- 正式深色页面实测从赛希终结技拖线到艾尔黛拉动态命中，连线数 0→1；修复后截图
  确认来源端口与目标首个命中对齐。仅临时 UI 编辑，不修改磁盘原轴。
- 最新连接阶段 4 文件 21 项测试通过，无跳过/预期失败；完整应用 vue-tsc 会话 36390
  已取得 exit 0。更早丢失的检查会话不作为通过证据。各前置阶段验证见对应证据记录；
  不是在推送时重新运行过全仓所有测试。

## 下一步（按优先级）

1. 收尾伤害显示语义。旧版 `timelineStore.getHitDisplayDamage`、ActionItem、
   ResourceMonitor、useDamageAnalysis 是参考入口；新版命中 title 当前用实际 value
   并四舍五入，命中详情/敌人效果点使用期望值。刚完成入口检索，**尚未修改**。
   明确标注与统一展示选择，不修改真实扣血，不把固定样本包装成整轴期望。
2. 对现有原轴尚未解释且影响输出的重要缺口继续归因；不要重复已证明的 77 笔乘区。
   概率状态、部分被动 Enable/Cast 生命周期仍有证据边界，不能通过删保护条件猜修。
3. 扩大分享轴样本，优先旧版零告警且覆盖其他干员的轴。既有候选有告警时明确记录，
   不能称作无错误旧轴。每条轴记录输入转换、差异分类与证据，无须每条都修改代码。

## 推送与恢复

本次授权推送 Endaxis 到 `sq/refactor/common-game-data`
（SQwatermark/Endaxis），combat-spec 到 `origin/refactor/operator-completion`。
Endaxis 的 origin 是 Lieyuan621 上游，**不要误推上游**。推送前两个工作树干净、
远程无分叉，分别领先 79 与 2 个提交（不含本次交接提交）。最终成功状态以 push 输出
及远程分支 SHA 核对为准。下次先 fetch/status，不切分支或改动只读旧版。
