# 当前交接

更新：2026-09-18。当前分支 `refactor/legacy-timeline-import`，本轮完成方案继承与历史区交互。
接续以实际 HEAD 和远端状态为准，先核对工作树，不操作其他有未提交内容的旧工作树。

## 当前实现

2026-09-18 原生一致性核验：先完善 combat-spec 的 `docs/timeline-lifecycle.md`、
TimelineActionProcessor 与回归测试，再修改 Endaxis。台式机当前 runtime image 和配套符号确认：
Load 保留配置顺序，普通 Tick 按配置顺序逐节点推进并结束，不按起始帧重排执行。
Endaxis 保留按起始帧索引的程序/切面结构，执行时恢复声明顺序，不再先推进所有活动项、
再启动新项、最后倒序结束；Buff 时间线去掉提前排序，复用公共创建与恢复路径。
这会纠正同帧 Buff 结束与伤害节点的先后关系。证据地址、文件哈希及 IFix 限制见上述规格文档。
后续补证并修正首次启动：普通时间线启动当次只 Execute，后续访问才 Tick 并检查 End，
包括起止同帧与跨过整个区间；主动 CastEnd/Jump 仍按已有协议清理。瞬时区间沿用活动集合，
不新增切面字段；覆盖 Execute 后恢复、不重放、正确收尾及同帧效果寿命回归。
另在同一当前镜像确认 TickIntervalAction.ExecuteInternal 自身调用 OnTick(0)，
保留启动当次周期触发，删除 Endaxis 对该动作“跳过首次普通 Tick”的旧补偿。
combat-spec 已先补证并修正此动作内部调用；不能把外层首次无 Tick 泛化为具体动作不自调 Tick。
IFix 和跳转重入边界仍不宣称全部闭合。

资源审计边界：本机 `tmp/game-data-hybrid-full-20260903` 的 Skill/Buff/Character/
AbilityEntity/Projectile 数据共 6211 份，未检出 IgniteDamageScalar 与
PhysicalInflictionDamageScalar 属性引用，元数据默认值均为 1；暂不扩展无消费者的动态通道。
本机重建报告标记 vfsVersionVerified=false，不能据此声称覆盖最新同版本全量数据。
装备直接施加异常/倒地的限制也未发现生成代码直接消费者；79 武器、24 套装的真实构筑检查通过。
验证：combat-spec 时间线/跳转/周期动作 48 项通过；Endaxis 首次启动修正后
调度/技能/Buff/切面/投射物/装备反应 6 文件 204 项通过，新增切面和同帧效果回归后的调度文件 24 项通过。
前轮装备定向 103 项属于首次启动修正前的验证。均为串行定向验证，本轮未跑全量测试。
周期动作最终修正后，动作序列 51 项、干员技能模拟与投射物回调 364 项通过；
切面分支测试更新旧跳过预期后 3 项通过，应用非增量类型检查通过。
用户要求改完立即提交推送，至此停止追加测试。

随后正式合并 Lieyuan621/main（13b6905c）：保留新版已删除的旧页面、旧模拟器、旧生成器目录的删除，
不恢复旧架构新增的孤立模块；生成资源与游戏语言表保留新版统一导出结果。
接收选择弹窗分组标题背景修正、公共图标测试、数字框主题色测试和失衡目标装备词条显示修正。
公共样式已在 c02f3113 适配；合并冲突中的新版组件接口、布局测试及图标目录予以保留。

2026-09-18 上游样式适配：已 fetch Lieyuan621/main 至 13b6905c，以 39d7398e 为合并前检查点。
同步公共控件尺寸、悬停反馈、显示菜单选中样式和数字框主题色处理，养成技能加减改用公共图标。
App.vue 删除重复全局覆盖，主题、基础布局和 Element Plus 适配统一由 design-system/styles 负责。
保留新版数字框 focus/select、侧栏图标插槽与骰子图标接口；旧版导出业务和战斗改动不属于此轮样式适配。
验证：设计系统、数字框、工具栏与 hit 面板定向 6 文件 84 项通过；应用非增量类型检查通过。
浏览器刷新后时间轴正常加载，已查看显示菜单；本轮未跑全量测试。

敌人状态区已按旧版恢复密度适配：可用高度除以行数、扣除 4px 行距后，图标与行高限制在
14–20px；不再用完整 20px 行布局抬高区域最小高度。分隔条使用旧版固定下限 46/26/52px，
仅在相邻展开区域之间转移高度，跨越折叠区域；保留比例记忆、拖动光标和禁止文本选中。
实际拖动发现 clientHeight 扣边框/取整会使第三段漂移，现用 border-box 实际高度作为起点。
浏览器实测状态区增高 24px、失衡区降低 24px，技力区高度不变，图标从 14px 恢复 20px。

最新 UI 边界（2026-09-18）：用户明确要求 hit 详情的静态构筑只保留一个攻击力数值，
逐项养成、装备及四维静态来源留在属性详情面板；hit 中只展开动态影响因素及保留计算公式。
下文先前扩展静态来源树的进度已被此取舍替代，相应 UI 和无消费者的投影辅助函数已删除。
武器静态来源命名使用已有显示身份（发布结果用发布快照、属性面板用当前定义），
不直接拿 `wpn_funnel_0016` 查询以 `wpn_artsunit_0016` 为键的语言表，也不另写前缀替换。

方案继承设计与验收见[方案继承与通用编辑约束](../architecture/scenario-inheritance.md)。
本轮基于 `3e909888`；用户已完成界面验收，代码审查后随本次提交保存。

- 存档保留独立构筑与完整作者输入，仅新增继承帧和来源 ID；不存切面、不复制游戏定义。
  来源仅导航，删除来源或单独保存继承方案不影响打开；接受游戏数据更新改变历史结果。
- 创建使用正式模拟回执裁切连续组，只保留 F 前输入；整条未来组直接排除。
  已开始组的未知实际帧拒绝猜测。再次继承不能倒退，不能早于原轴实际初始化帧。
- 场景/项目编辑会话共用配置锁、历史前缀和对象输入锁；保留原有位置锁语义。
  技能、切人、消耗品和人工事件均受约束，F 帧可编辑；历史外观、复制和查看保留。
- 模拟服务重放到 F 输入前并缓存切面，后续替换后缀；单服务最多一个前缀，释放时清理。
  自动接续和紧凑排列共用原运行时判定；历史技能/Buff/投射物及回执随切面继续。
  后缀及历史外观编辑不重建前缀，不把未来输入保存为战斗状态。
- 继承起点复用准备区的竖栏、底色、折叠箭头及顶部按钮；顶部按钮和边界右键打开继承菜单。
  共用准备区坐标换算，折叠时裁掉 F 前历史，仅保留 18px 展开入口，不压缩历史内容。
  跨 F 的持续内容仅显示 F 后片段；展开可回看负帧，不限制视窗滚动。
  不再显示重复的模拟起始线、右上角入口或常驻说明栏。横向拖动条使用原布局。
- 已修复未来技能拖入历史后锁死：拖动按最早组首整体限位到 F；提交拒绝时撤回预览。
  养成配装、资源监控、随机配置、敌人和危机合约等入口已只读。

验证证据（定向检查，不是全量测试）：
- 模拟服务 28 项；输入运行时/会话 43 项；拖动/命令/吸附/几何 74 项；
  创建继承 5 项。应用类型检查通过，UI 改动另做组件编译和定向检查。
- 真实旧轴经正式创建函数在 F=600 继承，独立保存重开、来源缺失、全新仓库与服务：
  2794 条回执、113 次命中及最终资源与同输入普通轴一致。
  审计脚本 `tmp/check-inherited.mjs` 和样本均在忽略目录，不提交。
- 浏览器佩丽卡：F=74，未来战技左拖停在 F，再次右拖成功；历史技能不移动，
  删除/禁用菜单不可用而复制/颜色可用。来源导航、配置只读、起点对齐、弹层和滚动条已实测。
- 用户已验收继承区交互：继承图标菜单仅保留来源跳转，轴上箭头展开/折叠；
  展开定位 F−10 秒，更早历史可通过滑块查看，分界线拖动只调整视窗，不改冻结边界。
  浏览器确认历史技能不再压进窄栏，来源按钮可正常跳转。
- 提交前定向检查：核心继承、编辑约束、存档及坐标等 10 文件 87 项中，
  86 项首次通过；1 项仍断言旧压缩语义，已修正并与编辑会话测试一起复跑通过。
  不执行全量测试。临时样本、审计脚本和转储不进入提交。

优先项 1 已提交为 `56cc0707`，收束实际使用的属性修正回执。优先项 2 仅扩展只读贡献投影：
直接 multiplyValue 复用独立因子分配，当前元素的纯加法抗性按百分点转换为因子增量；
混合乘法槽、缺少冻结读数、非正基线或限幅情况不冒充分解。未新增运行时字段、来源链或黑板计算图。
贡献及现有总分析/图布局定向 3 文件 27 项通过。真实旧轴 113 次命中逐项总量守恒，
总伤害 4702772.4794，自身 3782268.1264、外部 920504.3530；样本外部分配仅覆盖 damageScale，
新增两条路径由定向测试覆盖。89 次命中仍有不支持的修正，相应效果留在自身项，诊断仅供开发使用。
应用非增量类型检查通过；未跑全量测试。

本机接续已修复七乘区属性来源的误收录：伤害回执只保留实际注入乘区的属性明细，
不再追加完整属性快照中的同类记录，同时过滤加成 0、倍率 1 和属性槽单位元。
图、hit 详情与贡献查询共用修正后的回执，旧发布结果需要重新模拟。
非乘区属性也已改为在实际计算分支/公式输入读取处收集：固定伤害不带攻击来源，指定属性计算只取该属性，
攻击取基础计算阶段，抗性只取当前伤害类型；四维系数为零时不收录该四维加成。
暴击率与暴伤仍用于回执同时发布的实际/期望伤害，不因本次未暴击而剔除。
攻击快照缺少历史来源时不拿当前 Buff 填补；未做逐项移除重算或钳制后的边际贡献判定。
定向 4 文件 44 项、依赖提升后的应用非增量类型检查通过；未跑全量测试。
本机真实旧轴同版本前后对照为 113 次命中、2794 条回执，
除修正明细外的全部回执字段一致。七乘区修复后记录从 3488 降至 1638，非乘区收束后进一步降至 1155；11 次自然爆发命中中
无关的 electricEnhancedDamageIncrease 来源从 11 条降至 0。审计脚本在忽略目录 tmp。
后续非乘区定向 4 文件 48 项通过，覆盖固定/指定属性/真伤、零系数四维及前后阶段攻击来源。
依赖已按锁文件安装到本工作树 .local/node_modules，根 node_modules junction 指向该目录，
不再共享旧 Endaxis 工作树的依赖；Dagre 可正常解析。未修改依赖清单或锁文件。
该独立目录需使用 --shamefully-hoist：现有源码直接引用了 Vue 编译器、服务端渲染器及
Element Plus 图标等间接依赖，默认隔离安装无法解析。此为既有依赖声明缺口，不属于伤害修复。

提弗洛斯连携块外的大量命中已核对来源：连携创建独立实体
`abilityentity_chr_0034_typhoea_combo_presistdamage`，子技能持续 180 帧，
每约 0.333 秒命中一次、最多 18 次，总持续伤害倍率除以 18。用户轴保留原时间时，
18 次命中落在第 257～606 帧；实体时间受膨胀影响，不能据此延长角色动作块。
目前仍按来源 castId 显示在连携后方；独立持续效果条只是后续展示建议，本轮尚未实现。

梨诺自定义保存误报缺失 Buff 已修复：五处 `buffIdStackCompare` 查询会引用未收录的
`buff_chr_0035_liino_chrdung_armorbreak`。这类条件只统计已有实例，没有匹配时为 0，
并不需要创建该 Buff。编辑器引用索引现在将其标为实例查询，保留使用处导航；
`applyBuff` 等创建引用仍检查定义是否存在。真实梨诺定义与缺失创建引用的回归通过，未改生成数据或模拟行为。

补查 Buff 时钟：用户指出旧版会把诀连携脆弱随终结技冻结延长。现用“保留原时间”排除顺延后，
最后爆发在两版均为 20 秒，新版仍为 136935.39、旧版 644637；之前只解释技能顺延并不完整。
旧版诀连携脆弱从 9.2 到 20.783 秒，新版从 8.967 到 15.6 秒。原始 BuffData 中数值脆弱
useTimeDilationDt=false；只有 seal_total 内部流程为 true，新版已正确生成 global 时钟。
combat-spec/time-dilation.md 的 9 月 8 日原生调用链更正说明 TimeDilation 原因下默认 Buff
使用未缩放时间。旧版默认给效果寿命加冻结时长不等于原生规则，本轮不改新版计时。
116 项时钟/Buff 测试通过；详细记录已补入 tmp 中的 burst-comparison.md。

旧轴导入弹窗现在可选“智能修复时间”（默认）和“保留原时间”。保留模式跳过同轴顺延、
全局间隔推迟、终结技避让、同帧主控输入拆分和接续窗口搜索；仍复用逐步模拟判断技能形态、
补主控切换。没有另建一套转换器。用户提弗洛斯轴实测零时间调整，最后技能保持第 589 帧，
强化连携仍自动识别；浏览器中完成实际选择、导入和模拟。相关 40 项测试及应用类型检查通过。

提弗洛斯轴最后法术爆发已逐项复算：旧版 644637，新版 95293.2789（14.78%）。
同一技能因转换顺延从旧版战斗时间 20 秒推迟至 23.367 秒，多项增幅/脆弱/易伤结束；
基础爆发倍率和附着增强一致，旧版另乘 1.45408163 等级系数，当前原生动作链没有这一步。
完整因子及结束回执见 `tmp/typhoeus-combo-fixed-20260917/burst-comparison.md`。
分析中修复了事件条件用错 Buff 技能编号的问题：普通事件中的 CheckSkillCastId 也要读
affixSkillCastId，0 不得回退普通来源。这样“使命必达”连携击飞后正常给全队增伤；
中段爆发从 50.11 万提升至 59.92 万，最后一击因增益已结束不变。
条件回归、Buff 生命周期和标准环境共 187 项通过，实际武器触发/15 秒结束回归通过。

提弗洛斯强化连携导致模拟失败的问题已修复。其 `smartTarget: trigger` 可以选择触发连携的
干员，原来的施法准备却一律拒绝非敌人目标。对照 combat-spec 的
`combo-smart-target-and-template-init.md`、`combo-cast-preparation.md` 和
`skill-smart-target-outer.md`，现在保留干员目标的真实身份，沿用当前固定有效实体、零距离
模拟范围；没有把友方偷偷替换为敌人，也没有宣称支持完整空间选敌。
能力实体和空间点仍需要各自的选中规则，不能直接放行。

用户的 `Downloads/Endaxis_Timeline_2026-09-16.json` 已重新转换到
`tmp/typhoeus-combo-fixed-20260917/project.json`。原有转换流程从施放前切面试运行，
依据实际技能输入路由，将普通技能替换为同技能组中声明的替代技能；本次不增加提弗洛斯特例。
报告记录了 `track:0:cast:5` 从 `comboSkill` 改选为 `floatingComboSkill`，转换无错误，
整轴模拟成功。审计命令也修正为先按项目加载数据仓库，再叠加自定义定义。
错误提示保持右下角固定位置，支持选中、滚动全文及复制；浏览器已核对完整多行复制。
相关施法准备、转换及提示测试共 44 项通过，应用类型检查通过。

敌人身上的 Buff 伤害不再依赖 Buff 是否有可见持续条。击飞等被头顶栏隐藏的效果
保留独立瞬时图标和可点击伤害点，图标取实际来源 Buff；没有图标时使用通用占位。
有持续条的伤害仍挂在原状态行，独立伤害不重复挂回技能块。战斗伤害归属和计算不变。

物理异常仅指击飞、倒地、猛击、碎甲；它们与破防共用敌人状态区第一行。
破防本身不归类为物理异常，其他状态也不能凭颜色或头顶栏标记混入该行。
`physicalStatusDisplay.ts` 以实际 `producedBy` 关联
击飞/倒地与它们施加的破防，使用异常图标和破防层数，详情可切换两个真实 Buff；
同帧不相关实例不合并。同帧多个图标横向错开，持续条到下一个物理状态标记处截断显示，
详情仍保留实际时长。消费破防的猛击/碎甲也在同一行，角标显示剩余破防层数。
没有破防时，物理异常输入可能只创建破防；施加回执新增 `physicalInflictionType`，
用于选图标，不虚构第二个 Buff，不新增切面状态。旧发布结果需重新模拟取得该字段。
组件浏览器验收覆盖同一行、层数角标、双 Buff 翻页和伤害点击入口。

同步旧轴导入保留。hit 详情已展示七个伤害加成区间、攻击公式及实际执行的 Buff 属性槽、
damageScale、multiplyValue 直接修正，区分增伤、易伤和脆弱。武器 Buff 名称沿用时间轴规则。
总伤害分析保留攻击者统计，另接通显式 damageScale、直接 multiplyValue 及纯加法抗性的外部贡献分配。
攻击、暴击等尚未分解的效果留在自身项，不能把来源图当作完整归因。

本轮基于 `fa9b39ed` 贯通直接修正：运行时记录精确 Buff 实例和七乘区双方结算值；
`CombatObjectOrigins.directModifiers` 共供命中明细、来源图及贡献投影使用。
贡献只沿提供者归属找干员，不沿产生链追溯分账，不依赖图布局和节点上限。
来源图将有精确 Buff 实例的修正折叠进该 Buff 节点，命中直接连到 Buff；同实例多项修正
在详情列出并汇总贡献，无实例修正仍独立显示。仅展示层折叠，底层记录及贡献算法不变。
总分析按提供者聚合，出现负贡献时改用有符号表格。
护盾吸收、非正基线及不稳定分解暂退回自身，诊断仅供开发。详见[贡献设计](../architecture/damage-contribution.md)。

[对象来源查询](../architecture/object-origins.md)第一阶段已实现：

- `CombatObjectOrigins` 从发布回执建立可重建的只读索引，复用现有实体、Buff、施法和回执身份。
  区分产生者、原生 Source、归属、来源施法、直接修正和提供者，不建持久来源镜像或黑板计算图。
- 实体、投射物、Buff、全局 Buff 出生及伤害回执保存直接执行者。Buff 出生早于 Start/Enable，
  投射物出生早于同步发射回调；施加记录使用实际返回实例，刷新不改写出生，恢复不重复出生。
- 删除 `collectTriggeredHitStepKeys` 静态扫描和发布元数据中的步骤集合。
  hit 仅在实际 producedBy 路径包含 Buff 时标黄，增益、Source、归属不混入产生路径。
- Buff 过程回执保留实际层数前后值、叠层尝试、消费/吸收和结束/回收。满层增强尝试不冒充实际增长；
  不按同帧或定义 ID 猜实例，不从消费推导贡献比例，不新增可恢复层数历史状态。

hit 详情的“对象来源图”打开独立节点面板。当前最终交互要求：

- **所有模式都只向前追溯**。默认“产生与增益来源”，包含实际加成及其提供者；
  不遍历 `castEvent` / `buffEvent` 反向过程索引，不展开提供者施法的其他效果或 Buff 后续记录。
  公共查询仍保留过程索引供显式审计，不能重新接成图的递归展开。
- 布局已替换为 `@dagrejs/dagre` 的分层布局（network-simplex），删除手写分层、排序和顶部绕线。
  多重边及跨层边交给库处理，SVG 沿输出路由点平滑连接，关系方向与底层查询不变。
  已修复历史来源误用命中时最新状态形成环的问题：每条边按自身回执序号定位此前状态。
  同帧 A 产生 B、B 增强 A 的用例此前可复现 Dagre 矩形交点异常，现在保持 A₂ → B → A₁。
  另确认 Dagre 3.1.1 在合法分支 DAG 的三条平行关系上也会报相同异常。
  每条关系现经一个不可见布局点，仍由 Dagre 路由，再接回单条显示线；业务节点与关系不变。
  4 节点、5 边的报错回归、1000 个分支 DAG 及复杂图浏览器验收通过。
  时间轴 Buff 详情也可打开来源图，从当前选中的精确实例开始追溯；合并状态翻页不混用同名实例。
  已接入附着转化回执：产物 → 转化事件 → 被消耗实例，并展示消耗层数。
  Buff 层数状态按回执串成前后关系（3 层 → 2 层 → 1 层），每次变化保留各自来源。
  状态段保存 startSequence，详情按所选窗口的序号截断，同帧后续变化不会混入。
  叠层写入透传本次 producedBy 与 skillCastInfo，附着执行器复用 operationProducer；
  来源不再只停在干员。自动定时增长记录为 Buff 自身，图中定位到增长前的状态。
  旧发布结果需重新模拟才能取得新增的执行者事实。
  转化的消耗边定位到转化前的具体状态；不新增运行时层数历史镜像。
  转化关系仅在其回执已发布到当前历史截止时可见；这些关系不参与贡献分配。
  7 项布局测试、应用类型检查和生产构建通过；浏览器验收了 12 Buff、3 个共享提供者的复杂图样本。
- 共用历史状态节点只画一次，因果图保持 DAG。Dagre 统一计算节点位置与连线路由；
  打开时以可读比例定位当前命中，可拖动、缩放、适应全图。仅选中节点的邻边显示标签。
- 行为沿归属关系连到具体干员。节点和详情接入干员、技能/天赋、武器/装备、Buff、元素及法术爆发图标。
  展示元数据随发布结果冻结；Buff 图标按所属对象、实例和截止序号查找，武器来源优先，缺图保留文字。
- 查询按当前 hit 的回执序号截止，最多显示 128 个对象；没有新增图编辑功能。

## 下一步与缺口

1. 来源图程序节点已复用发布快照的命名规则，区分技能、天赋、潜能、武器技能、装备词条和套装效果。
   武器/装备的处理器键通过发布时冻结的词条索引定位，重名处理器不强选；技术 ID 留在详情。
   Buff 等对象的完整名称解析仍需补齐。
   修正已记录精确 Buff 实例；无实体宿主的内部子技能仍无独立执行身份，不能猜补。
2. hit 明细已复用发布面板回执补齐四维与暴击提示的静态配装/信赖/养成来源，不增加运行时字段。
   攻击快照现在随捕获值保存已有直接修正记录，命中时复用；不读取命中时攻击 Buff 冒充快照来源。
   实时与快照共用攻击公式的属性读取清单。捕获后替换 Buff、保存恢复再命中的回归及相关 28 项通过。
   技能步骤直接属性/乘区修正已复用 appliedDamageModifiers，采用实际执行宿主与行为身份，
   不伪造 Buff。详情以已有行为命名显示来源；相关执行、来源查询与贡献投影 42 项通过。
   Buff 内伤害步骤的直接修正同时复用 executingBuff 的定义与实例身份，避免误显示为普通技能；
   普通技能仍只记录行为身份。执行器与对象来源查询定向 28 项通过。
   技能 statModifiers 的暴击率/失衡增伤补入快照已有 modifierDetails，环境合并而非覆盖静态来源；
   下游仍在实际读取属性时筛选，因此失衡增伤不会无条件进入所有命中。快照/执行器 40 项、
   标准伤害环境 95 项通过，未改数值计算。
   尚需真实轴与页面核对各乘区来源闭合、无效修正过滤及阶段间时点，不能据局部测试宣称完成。
   本轮真实轴完整时长检查：125 hit（21 次攻击快照），125 项冻结公式还原期望伤害通过；
   1301 条直接修正无加零/乘一记录。此样本无技能直接修正，该路径单独回归覆盖。
   忽略目录脚本 tmp/audit-hit-sources.mjs 仅用于审计，不提交。
   浏览器实测佩丽卡战技：展开攻击树后，智识与意志的静态来源分别嵌套在对应属性下，
   不列攻击系数为零的属性；固定攻击也补入逐来源行。来源投影及组件定向 10 项通过。
   四维换算系数修正明确标注对应属性与“属性换算系数”，不再与属性数值加成混淆。
   该页面样本无装备和动态 Buff，不能代替这些来源及技能直接修正的页面验收。
   当前防御来自敌人配置，点燃/物理异常倍率在正式属性快照固定为 1；不补造动态来源。
   已确认仍未闭合的常驻来源：resolveOperatorPanel 的 applyUpgradeModifier /
   applyEquipmentContribution 将 addStaticDamageIncrease、damageBonus、damageScale 放入
   combatModifiers 时丢弃已有 OperatorPanelContributionSource；属性集只保留 equipment 类别，
   Buff 来源采集不能恢复具体装备。真实生成定义包括 suit_atk01 / suit_burst01 套装及
   艾维文娜等干员的静态增伤潜能，不能当成未使用的旧接口略过。
   已在构筑聚合时保留已有来源身份，并在对应静态属性读入处复用现有修正回执；
   详情复用面板来源命名，不在 UI 重算装备筛选、不按数值配对、不新增递归来源链。
   同属性多装备、技能/元素筛选及构筑来源身份的定向检查通过，共 3 文件 37 项；
   应用类型检查通过。补充标准伤害执行器实测：未失衡不记录套装来源，失衡后出现
   20% 来源且伤害乘 1.2，快照文件 22 项通过。
   浏览器佩丽卡装备四二式·肃阵并实际放置战技，伤害加成 x1.320 的提示显示
   “四二式·肃阵 +32.0%”，与轴上 Buff 来源名称一致；已查看提示框截图。
   该样本验证武器 Buff 名称，新增套装静态来源及多来源提示仍须页面验收。
   继续页面验收时发现弹窗切换异常：关闭 hit 详情后立即打开护甲选择，
   useInputRegion 激活 gear-selection 报 Cannot activate a background region through a modal boundary，
   随后 Vue locateNonHydratedAsyncRoot 报空 component，选单无法打开。
   已确认 Element Plus 在 afterLeave 才发送 update:modelValue，hit 详情此前仅监听该事件；
   改为监听关闭开始的 close，与其他选单一致，不放宽模态隔离。浏览器重建测试页后
   复测“关闭详情→立即打开护甲”成功，控制台无 error，组件结构 7 项通过。
   随后实际装备壤流轻甲并重新模拟，详情增伤 x1.149、提示“壤流轻甲 +14.9%”，
   证明新接入的静态装备来源已走到页面。随后同配队加入四二式·肃阵，提示正确分两行。
   发现旧固定精度把 14.95% 显示为 14.9%、1.4695 显示为 1.470，已改为最多六位小数，
   浏览器确认“壤流轻甲 +14.95% / 四二式·肃阵 +32.0%”与 x1.4695 对应。
   未改伤害数值，组件 7 项通过；套装整体展示仍需核对。
   本轮来源查询、贡献、图布局及来源命名定向 4 文件 34 项通过，不能代替弹窗验收。
   最新真实旧轴复跑为 125 hit、1470 条修正（新增 169 条非 Buff 来源），
   总伤害仍为 4777634.695572648，全部冻结公式闭合。
3. 总贡献按[有限直接归因](../architecture/damage-contribution.md)继续，未分解伤害算自身，诊断仅供开发。
   已撤回的递归归因、专用来源链、黑板计算图和 Buff 层历史方案不得恢复；可追溯不代表应逐层分配贡献。
4. 复核运行时装配根、标准环境和时间轴页面的职责。扩展功能牵涉面超出预期时先检查架构，不能只机械接字段。
5. 切面 T8 尚需实际浏览器生产环境的主线程/Worker 堆组成测量，Vite SSR 数字不能代替。
   继续补充不同配队、自定义连续组，以及同时包含 afterCastId 与 customDefinition 的真实用户存档。
   三种尚无专用编辑器的动作仍属 UI 待办。

切面接续见[切面计划](runtime-checkpoint-plan.md)，性能背景见
[测量记录](../research/combat/checkpoint-retiming-cost-analysis-2026-09-14.md)。
按需定义加载和 Worker 场景数据包已接通，不要重新当作待实现。

## 验证边界

以下是本次开发各阶段已执行记录，提交整理不重复跑全量测试：

- 直接贡献贯通：来源查询、贡献数学、回执、标准伤害环境及总分析等 8 文件 138 项通过；
  另检查生命伤害、伤害执行器和 Buff 运行时 3 文件 130 项通过。
  应用非增量类型检查与生产构建通过，构建仍有混合导入及大 chunk 提示。
  覆盖正负抵消、双方乘区、独立乘区、精确实例、回收后查询、分支句柄隔离和护盾回退。
  本轮未进行浏览器交互验收或真实轴贡献数字对照，不能将构建通过视为视觉验收。
- 对象查询及运行时定向 15 文件 479 项通过；Buff 过程接入阶段 9 文件 440 项通过。
- 图标相关 4 文件 26 项通过；最终上游遍历与布局 2 文件 15 项通过。
  应用非增量类型检查在最终布局修改后通过；生产构建在图标阶段通过，仍有既有大 chunk 提示。
- 下载目录 `Endaxis_Timeline_2026-09-16.json`（“提诀（意）梨洁”）：602 帧、113 次命中，
  总伤害 3,989,438.2037710985。小箭雨 12/12 黄、主箭雨 0/1 黄、连携持续伤害 0/17 黄、梨诺声波 11/11 黄。
  第 122 帧实体出生后的切面恢复，状态、完整回执和结果一致。
  该轴不含投射物发射，投射物回收后来源由专门集成测试验证。
- 真实轴有 33 次实际增减层、25 次叠层尝试、1 次消费；过程回执接入后总伤害和切面恢复仍一致。
  图标审计涉及 44 个不同资源路径，均存在。独立浏览器验证页检查图标、节点、缩放、拖动和关闭；
  最终消费后命中图已确认只向上追溯、保留增益，不再因展开过程记录触及 128 节点上限。
- 历史全量测试曾有 Windows symlink 夹具问题、需原生快照的跳过项；转换器也曾有语言数量断言修复后补跑。
  不能宣称当前全仓单轮全绿。旧详细日志查 Git 历史，不将历史记录当作本次复跑。

临时验证页、真实轴和审计脚本仅在忽略目录 tmp 中，不提交，也不保证跨机器存在。
本机 pnpm exec 曾尝试重新安装并被跨工作树 node_modules 安全检查阻止；使用已有 node_modules 中的
Node CLI 执行验证，不删除或重装共享依赖。重型检查串行、限制 worker，见[验证指南](../development/validation.md)。

## 必须保持的边界

- 原生规则和同版本证据归 combat-spec；Endaxis 为单敌人、无敌人主动行为、攻击确保命中的场景。
- SP 属于全队，终结技能量属于干员；不建立投射物私有能量账户。
- 回收顺序：结束当前技能 → reset 通知（来源仍可读）→ ClearSource。
- 切面先于新输入；技能、切人和人工标记都从输入边界提交，未来不能影响过去。
- 回执历史与瞬时状态分离，分支共享不可变前缀；固定程序与每次施放登记分离。
- 切面数据归 combat/state，模块依赖单向；必要的真实对象循环与共享引用保留。
- 不手改生成技能文本，不重复实现公共结构，不提交中间产物、转储或本机环境文件。
