# 当前交接

更新日期：2026-09-15。当前分支为 `refactor/common-game-data`。本文件只记录当前状态和下一步；设计
见架构文档，调查过程和原生证据见 research，已经结束的阶段记录见 archive。

## 当前任务

上游同步（2026-09-15，本机 Windows）：在 `a14072c6` 上合并 `Lieyuan621/main` 的
`0262ca07`，包含合流点 `a56d77e8` 之后的 5 个前端设计系统提交。个人远端没有新的分支提交。
公共表单、关闭按钮、提示浮层、抽屉、活动栏及主题变量已合入；当前时间轴的按钮选中态、
干员/武器/装备选择卡片和浮层消费者已适配。保留重置弹窗的焦点陷阱、键盘区域和现有工作台布局。
旧 `src/components` / `src/views` 中已删除的编辑器及其孤立测试保持删除，未恢复旧 store 或旧模拟路径。
技能块选择使用 `data-selected` 保留专用边框，不套用普通切换按钮的填充；天赋连线仍是非交互状态。
本轮没有修改战斗核心、转换器或生成数据。目录清理主线的剩余项不因本次合并而视为完成。

合并验证：UI 与设计系统串行跑过 274 文件、1448 项，初轮有 12 项旧结构断言失败；
更新组件归属、选中属性和格式无关的选择器断言后，相关 37 文件、279 项补跑仅剩 1 项
CSS 原文加载问题，改用文件读取后该项通过。应用非增量类型检查通过，生产构建 20.30 秒通过；
仍有大 chunk 和依赖 PURE 注释提示。此处不是重新执行整个仓库的全量测试。

本轮提交验证（2026-09-15，本机 Windows）：默认全量 Vitest 覆盖 781 文件、8337 项，
初轮 8333 项通过、1 项 Windows 文件 symlink 夹具权限失败、3 项按原设定跳过，耗时 566.18 秒。
发布器测试改用 Windows 真实 junction，并明确断言链接被拒绝；该文件重跑 5 项全部通过。
开启 GC 后额外运行切面释放测试文件，4 项全部通过；两份 Python 测试共 5 项通过。
仅剩两项需要显式指定原生快照的 HideUI 测试未运行；没有用现有生成产物代替原生快照。
应用非增量、转换器完整及公共契约类型检查通过，生产构建 22.60 秒通过（仍有大 chunk 提示）。
敌人六档数据精简独立提交为 `89032bd0`，代码归位、状态补漏及干员生成命名另作一次提交。

生成命名补充：31 个干员产物已统一为 `src/data/operators/<slug>.generated.ts`。
转换器渲染、发布清单、审计入口、静态引用及按需加载均使用新后缀；生成内容保持不变。
单名/整批候选与数据加载相关测试 30 文件、290 项通过。
应用非增量类型检查、转换器完整类型检查和生产构建（22.27 秒）通过，仍有大 chunk 提示。

最新补充：用户指出先前切面状态归位遗漏，本轮补齐 Buff 容器/实例/叠层/护盾等数据，以及动作序列、
事件订阅、随机流和输入数据。`combat/state` 增加共享基础数据层，消除对执行模块的反向类型依赖；
共同施法数据由运行时事件扩展回调端口。保留实际对象图中的共享引用和递归关系，不要求对象图无环。
本机串行战斗测试 177 文件、1804 项通过；应用模拟与编译消费者 56 文件、2343 项通过。
新增边界检查发现的实体蓝图内联反向引用已改归游戏数据层，边界与实体测试重跑 22 项通过。
最终应用非增量类型检查、转换器完整类型检查和生产构建（25.92 秒）通过，仍有大 chunk 提示。

当前主线为[代码组织与冗余清理](code-organization-plan.md)，基线为 `ae2dc5af`。
敌人六档生命值精简与本轮整理分开提交。其他工作树不参与本轮整理。

- P1 旧组件/主题/包装清理及 P2 公共边界整理已完成。
- P3 已按动作、技能、能力实体、Buff、资源、状态、事件、时间和恢复归位；装配根与标准环境
  已减少部分重复实现，完整职责复核仍待收束。
- P4 模拟应用归入 application/simulation；时间轴按 definitions、library、interaction、results
  归位。页面已提取缩放、项目文件、发布结果与定义工作区协调，合并重复下载实现；
  放置交互与模拟装配仍待复核。
- 最近本机串行时间轴全目录测试：240 文件、1268 项通过。随后发布结果提取覆盖
  45 文件、244 项，定义工作区提取覆盖 35 文件、112 项，修正结构断言后通过。
  最新非增量类型检查及生产构建（19.98 秒）通过，仍有大 chunk 提示。
- P5 已归位优化阶段的 17 文件，并修复旧模拟对照夹具的回执读取与回调身份；
  相关 100 项测试和转换器生产类型检查通过。动作与条件编译另归位 20 文件，
  相关 197 项及动作消费者 215 项测试、转换器生产类型检查通过。
  Buff、能力实体与公共引用分析也已归组，相关 316 项及引用调整后 220 项测试、
  转换器生产类型检查通过。技能编译另归位 18 文件，相关 20 文件、205 项测试与
  转换器生产类型检查通过。候选校验与发布另归位 12 文件，18 项中 17 项通过；
  1 项在创建文件符号链接时遇到 Windows EPERM，未进入发布器断言。生产类型检查通过。
  技能结构校验已提取条件与基础值模块，相关 5 文件、109 项测试及非增量应用类型检查通过。
  Buff 安装参数与内联定义校验也已提取，保留嵌套序列递归与当前目标上下文，
  相关 9 文件、254 项测试及非增量应用类型检查通过。
  动作/能力实体/子技能的递归校验已集中归入 actionPrograms，相关 26 文件、431 项测试、
  非增量应用类型检查及转换器生产类型检查通过。
  剩余公共编译模块另归位 19 文件，删除无生产消费者的武器类型旧别名；相关 12 文件、166 项
  经修正生成路径断言后通过，转换器生产类型检查通过。P5 归组与校验拆分已落地。
  运行时普通施放与常驻事件的技能控制绑定已去重，176 项测试通过；GlobalBuff 来源解析归回
  Buff 模块并删除注入端口，相关 144 项及新增上下文来源验证后 GlobalBuff 10 项通过，
  最终非增量应用类型检查通过。
  技能拖动完整会话已从页面提取，交互与结果保留 271 项经修正源码断言后通过，
  新增两个指针事件用例验证取消回退与项目替换时丢弃旧预览。修正页面残留接线后，
  相关 2 文件、9 项及非增量类型检查通过；生产构建 19.25 秒通过，仍有大 chunk 提示。
  运行时与页面其余职责复核及 P6 全量收尾尚未完成。

本轮转换器完整验证：186 文件、2027 项，首轮 2021 通过、4 失败、2 跳过（174.67 秒）。
三个失败为契约边界测试旧路径/扫描范围，修正后该文件 13 项全部通过；剩余发布器文件 symlink
创建的 Windows EPERM 保留记录，未进入发布器断言。没有宣称全量全绿。
完整转换器类型检查（含测试）补齐旧优化夹具实例与 reset 登记身份后通过，相关 13 项测试通过。

本体与旧轴工具完整串行验证：593 文件、6307 项，首轮 6303 通过、3 失败、1 项按原设定跳过
（442.41 秒）。三个失败均为拖动提取后残留的源码位置断言，两文件修正后重跑 13 项通过。
17 份当前文档的本地文件链接复查通过，已移除 5 个空目录。最终职责审阅、格式整理、
同基线真实轴对照及提交拆分仍未完成。

格式收尾已检查 1168 个改动文件，83 个按仓库配置修正后复查通过（排除生成产物）；
格式影响测试及 UI 结构测试 81 文件、1059 项通过。最终职责审阅、同基线真实轴对照及提交拆分待完成。

下一步收束 P4 页面协调，推进转换器编译职责归组，并集中进行全量验证与提交整理。

下列截面性能事项保留为后续工作，不因切换整理主线而视为完成：

战斗运行时切面的 T6、T7 已完成。T8 已完成第一轮组成分析和后台模拟内存优化：

1. 切面数据已按环境、动作、能力、动态实例和战斗根整理为五个基础文件；
2. 后台 Worker 已改成只接收当前场景所需定义，不再加载完整游戏数据仓库；下一步测实际浏览器
   主线程与 Worker 的生产环境内存。

任务顺序和验收标准统一维护在[切面任务清单](runtime-checkpoint-plan.md)。

### 笔记本接续：干净类型检查

2026-09-15 在 Windows 笔记本从 `eb2d3606` 接续，禁用增量缓存首次复现 91 条诊断，修复后同一完整
检查通过。`npm run type-check` 已固定使用 `--incremental false`，不再把缓存命中作为验收依据。

- 修正状态模块搬迁后的旧导入、可变初始化标志和恢复清理闭包的空值收窄；
- 区分底层 Buff 容器实例恢复与定义层编译恢复入口，消除同名不同参数的接口冲突；
- 原生恢复订阅按公共事件名绑定，排除不支持响应的 `outputKnockDown`；手工标记保持独立类型；
- 测试改用当前技能结构和真实原生资源事件，保留恢复身份与分支隔离断言；
- 移除默认路由和仓库测试对未提交 `tmp` 项目的静态导入。普通时间轴恢复为空项目；临时真实轴仍从
  显式预览路由打开。测试使用自包含引用样本，不把它算作真实轴验收。

本轮本机验证：标准 `npm run type-check`（含 schema 准备、禁用增量缓存）通过；串行单 worker 运行
战斗核心、正式应用入口、旧轴转换和项目数据加载测试，共 188 个文件、1892 项通过，1 项按原设置
跳过。`npm run build` 通过，仍有依赖注释与大 chunk 警告。没有重跑全仓 `src` 测试，也没有重新运行
私有真实轴内存审计。

### 全仓失败收束

笔记本完整复现 `src` 基线为 582 个文件中 19 个失败、26 个失败用例。已按根因修复：

- 普攻连续放置的预判只读取固定技能定义，不再查找尚未提交的 `castId` 实例；
- 标准环境预检覆盖固定定义与逐次施放程序，并按实际输入和观察范围分别检查，避免漏检或混用同技能
  不同施放的时刻；起身状态省略检查同时覆盖新程序入口；
- 莱万汀试验技能保留原技能的来源身份与原生类型，不放宽生产身份校验；
- Vue 的公共图片绝对 URL 不再转成 Windows 无盘符文件导入，恢复五个 UI 文件的加载；
- 对话框与回执共享入口的旧源码断言已对齐；Buff 详情翻页使用公共按钮；
- 装备覆盖包含启用序列。装备摘要已核对 `86eea80a` 的 15 个初始化根迁移后更新；SkillSetting 版本断言
  对齐已提交生成资源；本轮没有修改生成产物；
- 缺失的下一技能窗口文案已补齐；三个尚无专用编辑器的动作明确记入待办，没有为通过测试伪装为可编辑。

原失败范围定向复跑 26 文件、1460 项全部通过。

随后串行、单 worker 完整运行 `vitest run src tools/legacy-timeline --maxWorkers=1 --silent`：589 个
文件全部通过，6296 项通过、1 项按原设置跳过，耗时 440.23 秒。此前 19 个失败文件、26 个失败用例
已收束；这里的全仓范围是 `src` 加旧轴转换工具，不包含游戏数据转换器全量测试或私有真实轴内存审计。
最终 `npm run type-check`（禁用增量缓存）和 `npm run build` 均通过；构建仍保留依赖注释与大 chunk
警告。`git diff --check` 通过。

## 已完成的切面能力

- `CombatStateGraph` 保存完整帧边界的战斗数据，固定动作程序和回调不进入数据图。
- `CombatRuntimeSession` 支持保存、恢复、分支、回退、逐帧输入和显式丢弃保存点。
- 恢复先预检并建立完整候选装配，成功后才替换当前分支；失败不污染原分支。
- 新战斗与恢复战斗共用同一帧阶段管线，主控、技能、Buff、实体、投射物和回调保持原顺序。
- 固定技能程序与每次 `castId` 的施放登记分开；局部自定义技能只在对应输入提交时登记。
- 单块随机种子只随技能输入提交。随机源不再接收整轴 `castSeeds` 表。
- 回执历史移出状态图复制路径，保存点共享不可变前缀；增量 reader 只读取新增事实。
- `CombatInputSchedule` 同时管理外部输入边界和战斗保存点，可以替换边界后的技能、连续组、切人和标记。
- 旧轴转换按候选最早影响帧保存，只提交已确认但尚未执行的输入和当前候选，不预先生成后续旧轴技能。

详细结构见[战斗运行时切面](../architecture/runtime-checkpoints.md)和
[回执历史](../architecture/combat-receipt-history.md)。

## 已修复的问题

切面验收过程中已经修复以下会改变结果或破坏恢复的问题：

- 恢复漏发或重复切人通知，导致主控条件读取错误；
- 候选技能登记污染父分支，或恢复后重新编号固定程序；
- 能力实体子技能逐实例克隆固定程序，动作账本却发生交叉；
- 连携窗口活动 Buff 事件、SkillAffix、投射物和回调宿主没有完整恢复；
- Buff 动作宿主回收后仍被旧引用使用；
- Buff 乱序时间线按错误下标恢复；
- 群体 GlobalBuff 恢复后的目标顺序与新战斗相反；
- 候选试算完成后仍由外壳、排程器或回执游标持有重型分支；
- 旧测试把带 `castId` 的实例程序放进固定技能目录，或省略投射物回调的定义干员归属；
- 随机源保留整轴 `castSeeds` 过渡入口，使未来输入可能提前进入状态。
- `finishByAction` 或待继承 Buff 在动作结束前自然回收时，恢复预检把合法的空句柄误判为损坏引用。

生产代码仍严格拒绝重复技能身份、同技能不一致的冷却、缺失程序和缺失回调归属。没有为旧测试放宽
这些校验。

## 正确性验收

### 真实轴保存与恢复

第一批覆盖 4 个项目中的 6 条真实轴，期望和固定种子随机模式分别在第 1200、1800、2400 帧保存，
共 36 次恢复续跑。父分支、恢复分支和正式整轴的完整状态与应用结果一致。

随后对两条带主控切换的真实转换轴按实际回执选择事件前后帧：

- “简单自用电队钱本（副本）”：终结技膨胀开始/结束、能力实体子技能、Buff 周期伤害；
- “弭洛卡骏”：能力实体子技能、Buff 生命周期结束、终结技膨胀开始/结束。

每条轴、每种随机模式各检查 8 个保存点，共 32 次续跑。父分支和恢复分支的完整状态、应用结果与
全部有序回执相同；逐帧输入和正式整轴的结果与有序回执也相同。

两条轴当前期望模式结果：

| 轴                       | 回执数 | 命中数 | 期望伤害           |
| ------------------------ | ------ | ------ | ------------------ |
| 简单自用电队钱本（副本） | 10129  | 147    | 2370658.0089585194 |
| 弭洛卡骏                 | 8853   | 449    | 4658307.6932699755 |

### 广域回归

2026-09-15 串行、单 worker 运行 `src/core/combat`、正式场景服务和公开轴应用入口：

- 180 个测试文件全部通过；
- 1835 项测试全部通过。

最近删除整轴 `castSeeds` 后已再次运行同一组测试，结果仍为 1835/1835。

T7 状态模块整理后又运行一次同一广域回归，仍为 180 个文件、1835 项全部通过；旧轴转换 55 项
通过、1 项按原设置跳过，真实转换轴的完整状态、结果和有序回执保持一致。

T8 第一批优化后再次运行同一门禁，仍为 180 个文件、1835 项全部通过；旧轴转换仍为 55 项通过、
1 项跳过。另行执行整个 `src` 的 581 文件测试时，当前仓库基线仍有 19 个失败套件、26 个失败用例，
主要是 UI 源码结构断言、资源版本摘要和几项切面主线以外的模拟回归，不能把这次局部门禁描述成全仓
全部通过。生成武器测试中测试对照工具遗漏删除 `enableSequence` 的问题已修正，184 项全部通过。

## 性能与内存

三条真实旧轴共有 102 个技能块、127 次候选试算。串行预热后：

- 旧整场重跑中位数：21.47 秒；
- 切面路径中位数：6.00 秒；
- 按事实停止候选观察后：约 4.16 秒；
- 回执前缀移出状态图复制路径后：约 3.06 秒。

这些数字来自同机同输入的阶段测量，不能跨机器直接比较。

内存审计分两组：

1. “弭洛卡骏”66 个输入、3600 帧，在第 1800 帧保存；预热 96 次后再分叉、推进并释放 96 次。
   可观察堆峰值约 216.3 MiB，进程 RSS 峰值约 574.3 MiB。丢弃保存点后堆比基线低约 2.17 MiB。
2. 下载目录中的 3 条旧轴、102 个技能块；预热 3 次后完整转换 12 次。结果哈希相同，释放后堆在
   约 133.6–134.5 MiB 间波动，最终比基线低约 0.61 MiB。可观察堆峰值约 289.3 MiB，进程 RSS
   峰值约 584.5 MiB。

当前没有观察到随分支或转换次数持续增长的堆泄漏。总峰值仍偏高，下一阶段必须拆分测量模块加载、
固定程序、状态图、回执、结果投影和转换中间数据，不能把 RSS 全部归因于切面。

分阶段测量已经确认，加载编辑模拟入口会连带保留 419 个 `src/data` 模块，使 Vite SSR 稳定堆从
约 23.4 MiB 增至 124.6 MiB。切面状态图约 0.53 MiB、回执前缀约 1.86 MiB、最终结果约
3.12 MiB，不是这段稳定增量的主要来源。

后台模拟现使用当前场景数据包。真实轴数据包约 0.55 MiB，完整仓库与小仓库的 5866 条回执和结果
逐字节一致。干净 Worker 入口稳定堆约 53.1 MiB、RSS 约 261.5 MiB、278 个模块；原整库入口分别
约 124.6 MiB、446.8 MiB、696 个模块。此处是包含 Vite 转换缓存的同环境对比，实际浏览器仍需
单独测量。

本地 Vite 主界面已实际打开验证：默认真实轴完成后台模拟并显示技能伤害、Buff 与资源投影，浏览器
控制台没有错误，说明场景数据包能够通过 Worker 消息边界并恢复为可用仓库。

页面内存已进一步拆分。当前生产构建打开同一默认长轴后，渲染器私有内存约 64 MiB，和旧发布版
约 80 MiB 同量级。开发环境的主要额外成本来自关闭状态的对话框仍被静态导入：定义编辑器、选择、
构筑、导出、分析和详情面板现已全部按打开时加载。重启 Vite 后，同轴开发进程私有内存由此前约
644 MiB 先降到约 445 MiB。仓库又改成按项目引用加载：默认轴首次只装入 4 名干员、4 把武器、
13 件装备和 4 个套装，打开选择器或导入项目时才扩充整库。最终同轴开发进程约 400 MiB，打开完整
干员选择器后约 411 MiB。生产时间轴入口由约 3.97 MB 降到 1.84 MB，入口 CSS 由约
200.7 kB 降到 134.8 kB。默认轴、另一条临时旧轴预览、伤害分析、干员构筑、定义编辑器和完整
干员选择器已经逐项打开验证。

详细方法和原始字节数见
[切面耗时与内存分析](../research/combat/checkpoint-retiming-cost-analysis-2026-09-14.md)。

## 尚未完成

1. **真实组合样本**：当前 `tmp` 和 Downloads 中没有同时包含 `afterCastId` 与 `customDefinition` 的
   用户实际存档。派生组合场景已经通过，但不能冒充真实编辑数据。
2. **更细的定义目录**：时间轴已按当前项目加载完整定义，选择时才扩充整库。若数据规模继续增长，
   再生成轻量选择目录，并只加载最终选中的完整定义；现阶段不要把 Promise 查询侵入同步模拟核心。
3. **其余业务工作**：切面主线以外的游戏机制、生成器和 UI 待办继续以各自架构与研究文档为准。
   游戏数据转换器全量测试不包含在本轮通过范围内；不能把应用与运行时测试通过写成生成器全量通过。

## 本机复现入口

真实转换轴和审计脚本位于忽略目录，只用于本机验收：

- `tmp/public-6aa244e7-control-inferred-final-20260913/project.json`
- `tmp/public-6aa007f2-control-inferred-final-20260913/project.json`
- `tmp/verify-checkpoint-real-axes.mjs`
- `tmp/audit-checkpoint-long-axis-memory.mjs`
- `tmp/audit-legacy-conversion-memory.mjs`
- `tmp/audit-checkpoint-memory-phases.mjs`
- `tmp/audit-scoped-simulation-game-data.mjs`
- `tmp/audit-scoped-worker-memory.mjs`
- 下载目录中的 `Endaxis_Timeline_2026-08-31.json`
- `tmp/mappings.2026-08-31.formatted.json`

常用检查命令：

```powershell
npx vitest run src/core/combat src/application/simulation/integration/publicShareRegression.test.ts src/application/simulation/scenarioSimulationService.test.ts src/application/simulation/runScenarioSimulation.test.ts --maxWorkers=1 --silent
npx vitest run tools/legacy-timeline --maxWorkers=1 --silent
npm run type-check
```

重型检查顺序运行。当前机器内存使用应保持克制，不并发执行完整类型检查、全量测试和内存审计。

## 文档入口

- [架构总览](../architecture/README.md)
- [战斗运行时](../architecture/runtime.md)
- [战斗运行时切面](../architecture/runtime-checkpoints.md)
- [回执历史](../architecture/combat-receipt-history.md)
- [时间与显示](../architecture/time-and-display.md)
- [代码规范](../development/code-style.md)
- [本地开发环境](../development/local-environment.md)
- [验证指南](../development/validation.md)
- [旧轴转换工具](../../tools/legacy-timeline/README.md)
