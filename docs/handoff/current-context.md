# 当前交接

## 2026-09-20 全局配置编辑收束（未提交）

- 左下只显示已配置的数值修正及编辑入口，名称沿用 V2；初始技力与回复速度只在资源监控编辑，技力上限不提供编辑入口。
- 底部改为紧凑的全局 Buff 多选卡片：内置连携加速与自定义项独立启停，自定义项可添加、编辑、删除；复杂内容复用 Buff 结构编辑器，按需加载。
- `globalConfig.enabledPresetIds/customBuffs` 随方案保存，停用保留定义；公共方案命令负责撤销，继承配置冻结沿用现有整体约束。自定义名称仅在展示侧查询，不进入战斗回执。
- 编译入口将启用项安装为普通全队 GlobalBuff，不修改战斗内核。已定向验证多项并用、全队属性、停用、切面恢复与存档往返；未跑全量测试。

更新：2026-09-20。当前分支 `refactor/legacy-timeline-import`。本次交接先看下节；后文保留此前的
旧轴、闪避、来源图和切面研究记录，历史测试数字不代表当前整仓测试结果。
接续以实际 HEAD 和远端状态为准，先核对工作树，不操作其他有未提交内容的旧工作树。

## 2026-09-20 交接重点

全局属性修正已改走战斗初始化 GlobalBuff → 全队子 Buff → 公共属性槽；界面和
`globalConfig.modifiers` 存档保留，内部 Buff 不显示状态图标，详情名称仍为“全局属性修正”。
删除静态面板加值、专用 `skillCooldownReduction` 战斗分支及静态 `globalConfig` 来源类型。
连携冷却沿危机合约的 `ComboSkillCooldownScalar/finalMultiplier` 路径，每条写入 `1 - value`，
保持多条相乘；不扩展到其他技能类型。配置在首个输入前（含负帧）安装，分支/切面恢复不再施加。
构筑面板只显示养成配装，修正后的战斗属性和贡献由运行时公共路径提供；旧轴全局配置导入仍未接通。
本轮串行定向验证累计 10 文件 258 项通过（恢复测试修正会话归属后单独复跑通过），应用类型检查通过；未跑全量测试。

本轮 Endaxis 的修改集中在时间轴编辑、V2 界面复刻、导出和少量原生显示修正：

- 技能详情移除了镜头到目标的夹角设置及自定义时间条，项目数据也不再保存这些字段；
  旧轴转换中遇到自定义展示条会删去，镜头夹角按当前单敌人零距离模型处理。
  技能块之间的连线按 V2 的操作和显示方式调整；Hit/效果端点连线不再进入 V3 项目，
  转换报告会列出被删的连线。要验收连线时，同时检查端口、重新指向、右键菜单、删除和撤销。
- 导出主面板按 V2 分成项目数据和当前方案图片。JSON 与数据码可选当前或全部方案；
  当前方案单独导出时保留继承时间和历史冻结边界；来源方案可以缺席，只影响导航。
  单方案文件只携带其引用的项目级自定义干员、武器、装备、套装模板，导入时作为独立项目打开。
  小图预览、明暗按钮、文件名、时长、
  尺寸和显示开关按 V2 调整；长图沿用当前时间轴捕获。2026-09-20 审查中按用户要求改为
  PNG 导出，沿用 V2 的 `tEXt/EndaxisData` 元数据，项目打开入口可从 PNG 恢复方案。
  元数据写入失败会报错，不再悄悄导出缺少存档的图片。普通 PNG 没有项目数据时明确报错。
- 技能提示可显示键盘或手柄键位；物理异常在时间轴和对象来源图中使用原生 termicon。
  提弗洛斯复合图标补齐猎矢左侧三角形，猎矢自下而上积攒，场外恢复状态不显示。
  专属 HUD 的外观仍只读取投影数据，不改战斗数值。
- combat-spec 同步保留闪避、连段、Buff 生命周期和技能结束顺序的原生证据与 C# 回归。
  原生事实见 `D:/Projects/combat-spec/docs/dodge-runtime.md` 等专题；本仓负责产品场景中的
  生成、模拟、转换和展示。两个仓库需分别提交与推送。

本轮提交前串行运行相关 22 个测试文件、281 项通过，应用非增量类型检查通过；
已在本机浏览器查看导出主面板和小图预览的实际布局。combat-spec 的闪避、连段、
技能结束与 Buff 定向回归 239 项通过。上述都是定向验证，不能称为两仓库全量测试通过。
图片导出统一使用 PNG；游戏图标的 WebP 资源格式与用户导出的图片格式分开处理。

接续优先检查真实项目的 JSON、数据码、小图和长图能否分别导出并重开；尤其要核对
大时间轴的长图尺寸、PNG 元数据以及图片下载后的实际可读性。随后继续用旧轴样本验收
技能块连线和被报告的 Hit/效果连线缺口。不要为兼容尚未发布的 V3 旧存档恢复已删除字段。

## 当前实现

2026-09-19 公开旧轴全量复算：沿用 2026-09-18 下载并校验哈希的 157 份原始分享，串行转换和模拟
全部 256 个方案，原始输入未改。本轮结果在 `tmp/public-share-recheck-20260919`；第一次运行的
34 份轴、42 个方案因标准模拟兼容性预检漏认已实现的 `markCurrentSkillCanDash` 而报错。
预检补齐该步骤后，仅将这 34 份重算到 `tmp/public-share-recheck-fixed-20260919`，修复前结果保留。
合并两批结果：157 份均有旧版与新版结果，模拟失败 0；119 份完整转换、26 份只缺展示连线、
12 份仍有模拟内容转换缺口；施放告警 1696 个。相较上次全量记录，7 份轴从模拟内容不完整
转为完整或仅缺展示连线，没有反向变化。含旧版闪避的 9 份轴均完成模拟，部分仍因其他旧格式
字段而未完整转换。剩余 12 份分别涉及不支持的用户配置 7 份、伊冯早期分段 2 份、莱万汀
未知变体 1 份、递归展开与调时 2 份；这些规则仍按下文待定议题处理，不能靠删输入或猜映射消除。
兼容性预检定向 20 项通过；本次全量复算只证明转换与模拟可完成，不代表逐 hit 与旧版相同。

2026-09-19 时间轴提示调整：没有玩家移动状态输入时，不再向用户展示这三项未建模条件的常驻提示；
真实提供状态后仍检查已确认的禁止条件。闪避中断结果用回执里的释放身份对应轴上技能块名称，
内部隐藏技能没有轴上块时不显示原始技能 ID。输入路由不一致时，实际候选也用轴上技能块的
命名规则展示；无法唯一识别才使用概括提示。技能中断告警保存当前技能块的释放身份，
可直接显示“A3尚不能被A4中断”，技能名不加引号。技能块本体已移除浏览器 `title`，
命中点和独立告警图标的提示继续保留。

2026-09-19 中心状态时间域修复：原生 `CharacterMainCtrlSystem.OnFixedTick` 把固定帧增量乘以
角色最终时间倍率，再推进中心状态；`CenterBlackboard.Tick` 的三只输入计时器共用该增量。
Endaxis 已改为用 `timeDilation.getEntityScale(operatorId)` 推进攻击阻挡、攻击接续和极限闪避后
再次 Dash 的剩余时间，保留半帧精度。零倍率暂停、半倍率减速及实际装配下的全局膨胀均有定向回归。
另查明共享连续 Dash 窗口由 `PlayerController.PreTick` 按全局时间倍率推进，
不会读取当前干员的局部倍率；Endaxis 已分别接入两种时间域并覆盖全局/局部膨胀差异。
人工成功延迟仍使用现实帧，不能凭这些证据改成缩放时间。
具体原生地址和边界见 [闪避设计](../architecture/dodge-markers.md) 与
`D:/Projects/combat-spec/docs/dodge-runtime.md`；后续继续核对其他时间域和真实轴样本。

2026-09-19 闪避审查后的修复：恢复排程与窗口配置、中心状态迁移、成功声明的标签身份、连续组窗口、
交换轨道后的所属、显式攻击窗口诊断和过期声明结果均已修复。Dash 与成功之间可以建立继承分支；
输入运行时与共享连续闪避运行时不再持有可变切面。新增定向回归及真实生成数据测试覆盖上述边界。
`SkillRuntime` 已向连段系统提供原生 `offsetRecordFrame`，验证了佩丽卡 A1 记录点前后闪避分别接
A1/A2，并以洛茜 A1 的记录点后接 A2 做了另一名干员的对照。
旧轴标准闪避块已转成普通闪避标签，智能顺延保留其相对前项的时间顺序。闪避标签拖动会实时模拟，
右键菜单可在普通和极限闪避之间切换；选中极限闪避时可见延迟成功的定位点。右键或快捷键
复制标签后，在鼠标所在的轴上时间粘贴；鼠标离开轴时使用时间游标。技能块使用同一落点规则。
粘贴保留方向、所属轨道与成功延迟，并生成新 ID。
闪避标签详情已按明确的产生链列出 Buff、技力、时间膨胀、隐藏技能等实际效果，每项可打开来源图；
资源与时间膨胀创建回执保留执行宿主，延迟施放请求保留发起者，隐藏 Dodge 技能的普通启动回执
可追到精确标签。同帧同名但没有该产生链的效果不归入标签。已在浏览器检查佩丽卡极限闪避：
详情列出两段时间膨胀，来源图能沿监听 Buff 回到 `dash:` 标签。体力仍不在 UI 显示。
闪避、延迟施放、能力系统、来源投影及图布局的定向测试共 140 项通过，应用类型检查通过。
仍需核对技能取消、其他后续效果、更多干员接续样本及原生时间域。审查过程与原始复现见
`tmp/dodge-review/README.md`。体力只记录内部相对变化，不限制执行或显示在 UI。

2026-09-19 按原生证据实现闪避。combat-spec 已复刻 `Free / Attack / Skill / Dash`
中心状态、Dash 中断、三只独立输入计时器、共享闪避能量和移动层驱动的 Dash→Free 判定；原生证据见
`combat-spec/docs/dodge-runtime.md`。Endaxis 已接通闪避/极限闪避标签、存档校验、固定输入排程、
继承切分、可恢复中心状态、Dash 中断、隐藏 Dodge 技能入口和编辑面板。

状态机只负责输入合法性和实际技能开始后的 `Dash → Skill` 迁移。极限闪避标签只发布一次成功机会；
`buff_common_dash_behit_listener` 按生成数据检查主控与防重复条件，并执行技力、闪避能量恢复、时间膨胀、
Buff、事件和隐藏技能。生成器会在 CharacterData 没有显式 dashBuff 时注入公共 Dash Buff，并把各干员的
`dodgeSkillId` 作为实例黑板值传入；动态 Buff/技能 ID 走通用黑板依赖和运行时解析，没有干员特例。
完整生成与发布报告已更新为 `tmp/game-data-rebuild/run-ZlGuOs/report.json`：来源、31 名干员、技能、
技能库/组合场景、装备、类型、资源和发布门禁全部通过。31 份生成干员定义都已包含原生
`offsetRecordFrame` 和有序 `normalAttackSkillKeys`。仍需继续验收标签详情和来源追溯；
隐藏 Dodge 技能继续使用通用技能运行时；技能是否伤害由动作序列决定。当前原生公共 Dodge 技能没有
伤害动作，不需要伤害结果入口，也不能按 `dodge` 类型硬编码伤害行为。
`GlobalConst.maxDashEnergyLimit = 480` 与 `dashCostEnergyValue = 60` 给出最大 8 次，不代表当前账号容量。
`PlayerController._OnMaxDashCountSynced` 实际读取账号同步值；Endaxis 尚无该输入，因此只记录相对消耗
与返还。产品约定体力不限制闪避执行，也不在 UI 显示余额、透支或缺少容量。
`BattleCommandMappingConfig.asset` 已由 VFS 确认并接入自动生成：普通 Dash 的攻击禁用/允许接续为
0.1/0.35 秒，极限闪避技能阶段为 0.1/0.25 秒，再次 Dash 禁用为 0.5 秒；半帧值保持原精度。
实际页面已验证诀的极限闪避标签可创建、可选中、模拟不失败且告警 tooltip 正确。

本轮继续按机器码和 Unity 资源闭环连续 Dash。`PlayerController.OnSprintPressed`
（RVA `0x03CA1B30`）证明：超过外侧 `dashInputCd` 后下一次计数重置为 1；同一串内只有达到
`dashSecondDashInterval` 且尚未达到上限才允许计数加一，原生比较会减去当前帧时长。
`MovementSetting_Default`（manifestId `456952`、assetIndex `14246`）给出 0.8/0.3 秒。
`OverrideMultiDashLimit`（Execute `0x06006E10`、OnEnd `0x060072A4`）把负数转成
`int.MaxValue`，并在动作结束时清回 null；当前唯一来源是庄方宜终结技 Buff。
combat-spec 已补动作解析、生命周期和连续计数模型。Endaxis 生成器不再把该动作当表现丢弃；
AbilitySystem 切面保存本角色的临时上限；全队唯一的 PlayerController 共享切面保存计数和距上次
Dash 帧数，并由单独运行时每帧推进一次。切人不会重置连续闪避串，恢复后继续使用同一判断。
无效时间轴输入仍执行，并把极限闪避技能锁定、输入过早、达到次数上限分别写入可复制详情。

真实生成数据的端到端场景已经验证 Dash 输入、极限闪避声明、公共监听器成功、7 点技力、
0.5 份共享闪避能量返还和隐藏 Dodge 技能启动。该验证同时修复三处装配断链：补入
`TurnRateScalar` 原生属性元数据；用固定 `center:dash` 程序身份执行 Dash Buff 生命周期，单次输入仍由
`producedBy` 保留；将原生非直接黑板赋值改为按实际类型复制，并只在完整数据流证明所有动态候选均为
纯表现 Buff 时裁掉动态创建动作。运行时仍严格拒绝其他未知动态 Buff。
连续组在前段被 Dash 中断后会保留后续成员，等待 Center 的实际攻击接续窗口开放后再提交；
窗口数据未知时降级为下一帧继续，不会永久卡死。尚需继续闭环来源追溯、标签详情，并用强化普攻及
Dodge 专用后续攻击补充 offset 的真实轴验收。
设计与验收矩阵见 `docs/architecture/dodge-markers.md`。

连段 offset 已按本轮重新核对的机器码接通。`Skill.Interrupt` 的 Dash 分支条件是当前技能为
`Attack`，或已经存在 offset modifier；早期 combat-spec 文档误写为 `PassiveSkill`，现已修正。
普攻到 `offsetRecordFrame` 时提交下一段，Dash 创建唯一的临时 Attack 映射；优先级位于
Skill/Buff 之后、Mode/Default 之前，按全局缩放时间保留 30 帧，到期只移除映射而不清空目标。
状态保存在 AbilitySystem 纯数据切面中，中心状态机不复制连段段数。战技、终结技和隐藏 Dodge
技能开始不会覆盖连段目标。核心运行时 4 文件 151 项、combat-spec 定向 47 项、生成器整名夹具
17 项、标准伤害场景 38 项通过；正式生成的公共 Dash 链端到端测试通过。标准场景中一处仍断言
旧式 `abilityEntity:` 身份的夹具已改为当前稳定的 `ability-entity:` 身份，没有改运行行为。

2026-09-18 全量公开分享审计（取代下文小样本通过率作为当前覆盖结论）：
分页读取网站公开列表，共 157 份轴、256 个方案，全部下载；内容哈希无重复，下载失败 0。
全部逐份执行旧版模拟、新版智能转换和新版结束线/完整时长审计。子进程串行、Node 堆上限 2 GB，
单份超时上限 180 秒；本轮没有超时、进程崩溃或内存溢出，旧版模拟错误 0。

| 结果（按整份存档判定）                 | 存档数 | 所含方案数 |
| -------------------------------------- | -----: | ---------: |
| 完整转换且模拟完成                     |    118 |        194 |
| 模拟内容完整，仅展示连线未迁移         |     20 |         24 |
| 模拟内容转换仍有缺口，但后续模拟能完成 |     19 |         38 |
| 存在模拟失败                           |      0 |          0 |

累计施放告警 1743 个。整份存档只要有模拟内容遗漏，就不给其中方案发布伤害比值，不能把模拟能跑完
等同于转换正确。此前 33 份/50 方案已通过，只是较小样本，不代表全量老格式已覆盖。
首轮固定模拟代码跑完全量；随后定向复算两份能力实体恢复失败轴、五份点燃上下文失败轴。
本轮再复算受早期连携身份影响的 25 份轴、54 个方案。
随后修复切人身份混用，复算 6a363aed92fd487199e16e05 的两个方案。
上表汇总这些结果，其余轴未在最新代码上重新跑一遍，不能视为最新代码全量验收。
本次修复能力实体创建时丢失实际定义身份：原生允许不继承施法来源，恢复不能据此猜定义。
实体状态现在单独保存 definitionProgramId，固定定义复用已有 CombatOperationPrograms 目录；
不继承来源的实体仍保留 skillCastInfo=null，不改变伤害归属。登记过的定义恢复失败时仍严格报错。
两份真实轴 6aaa89a9ecff634734db329e、6aa9ad93f39230b37683a8ce 的三个方案均完成模拟，
不再发生定义查找失败；前者完整转换，后者仅剩 inheritedInitialEffects/inheritedInitialEnemyState 未支持。
实体动作、实体目录、关系恢复、整场装配和模拟入口共 5 文件 157 项测试通过，应用类型检查通过。
旧结果保留在全量目录的 before-entity-definition-fix，原始输入未改，对比表和临时预览已更新。

已修复点燃回调把触发者当成 Buff 来源，导致管理员天赋给敌人添加增攻并读取不存在的 Atk。
原生 Buff.OnIgnite 保留 Buff 的 Source/Owner，仅把 igniteSource 作为 InputTarget；有调用环境时
临时使用调用方施放信息。此前交接与 combat-spec 对来源的解释有误，现已按反汇编纠正。
运行时保留原来源，生成器将原生 InputTarget 编译为 actionInputTarget，查询和施加 Buff 都走该目标。
只发布官方生成器产出的公共 Buff 定义差异，不给敌人补属性、不手改干员生成文件。
证据地址、镜像哈希和调用细节见 combat-spec/docs/ignite-finish-dispatch.md。
五份轴共十个方案均能模拟，Atk 错误和对应智能调时异常消失；其中四份仍有映射或连线缺口。
完整样本为 6a5325ac28e129aa002c88c8（冷启动无弭弗混伤）。定向结果在
tmp/ignite-context-regressions-20260919，已更新全量表与预览；旧结果在 before-ignite-context-fix。
Endaxis 相关五文件 345 项、combat-spec 195 项通过，应用与生成器类型检查通过；
官方候选生成和独立重建一致性检查通过。未发布候选中与本次修复无关的其他生成文件差异。

已补早期连携身份：普通多段连携读取 comboSegmentIndex，普攻仍读 attackSegmentIndex，
并校验完整 ID 与段号。旧版 419be04a 的保存逻辑证明这两个字段不同。
历史表补入洛茜 ROSSI_link 和 ROSSI_variant_v_1772384921148，分别对应第一、第二段，
证据为 9a2bbf28:public/gamedata.json 与 419be04a 多段连携改动；没有泛化猜测未知时间戳。
25 份轴、54 个方案均模拟完成，新增 14 份轴、33 个方案完整转换；未改原生模拟或生成技能。
结果在 tmp/legacy-combo-fix-20260919，已更新全量结果、对比表和临时预览；
此前结果备份在 before-legacy-combo-fix。转换器测试 7 文件 65 项通过、1 项因未启用显式 GC 跳过；
转换器独立类型检查通过，同时修正了测试中 JSON 判别字段扩宽和可选放置时间的类型标注。

已修复切人标记保存 gameId、轨道保存 slug 时无法匹配：两侧共用 resolveLegacyOperator，
仍拒绝重复目标、未知身份及显式轨道下标冲突。真实样本同时保存 ROSSI 和 rossi，
修复后两个方案完整转换并模拟完成。只改旧格式入口，没有更改主控模拟规则。
结果在 tmp/legacy-switch-fix-20260919，已更新对比表及预览；旧结果在 before-switch-identity-fix。
转换器回归 7 文件 66 项通过，1 项因未启用显式 GC 跳过；独立类型检查与差异检查通过。

按用户要求分开展示与模拟问题：连接端点未迁移、源连线端点不存在标记 impact=presentation。
report.issues 保留所有诊断，另输出 simulationIssues 与 presentationIssues；不隐藏连线遗漏。
全量表的 presentation-only 单独统计，恢复其伤害和百分比；共 20 份轴、24 个方案。
本次只重分历史诊断，没有重算或更改伤害。分类前结果保存在 before-presentation-classification。
其中效果端点涉及 21 份轴、25 个方案、269 条连线，全部涉及效果，没有独立 Hit 端点案例；
只缺这些效果连线的有 15 份。另有仅缺悬空技能连线的存档，因此展示问题独占合计为 20 份。

### 待定议题

按用户要求，以下项目在规则明确前保留诊断，不为了提高通过率猜测或删除。分类可能重叠。

| 议题                         | 已确认的事实                                                                                                                                                                         | 仍需确定                                                                                              |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- |
| 递归展开与调时（2 份）       | 第一段过早、同轴后续块又可能截断展开；下文有实验记录                                                                                                                                 | 如何把递归输入纳入逐技能排程并保持跨轨道顺序                                                          |
| 旧轴闪避转换（9 份）         | 19 个旧 dodge 块均为 0.5 秒、无伤害点、无附加效果；旧版 419be04a 的 createDodgeSkill 正是这样生成。现按所属干员的完整 `<GAME_ID>_dodge` 身份转换到同轨、同开始时间的普通向前闪避标签 | 旧格式没有方向和成功事实；向前仅是导入约定，绝不自动补极限闪避收益。仍需重跑 9 份真实轴并审计主控告警 |
| 伊冯早期强化普攻分段（2 份） | 存在 v_1770188884725_seg1 至 seg5；历史已知的整体变体不能直接对应这些分段                                                                                                            | 分段与新版递归链的对应关系，尤其第五段；不能把每段各展开成整链                                        |
| 莱万汀未知变体（1 份）       | 1769443359596、1769444207351、1769445413383 在本机 Git 全历史字符串检索中无匹配                                                                                                      | 找到旧定义或明确转换规则；不能仅按名称或类别猜测                                                      |
| Hit/效果连线（21 份）        | 旧派生端点不能直接作为新版节点身份                                                                                                                                                   | 如何根据来源行为与具体命中/状态重新定位                                                               |
| 缺失连线端点（9 份）         | 66 次诊断中的 instanceId 在各自源方案技能块中均不存在，不是重复身份                                                                                                                  | 悬空连线如何保留或报告；当前不擅自删除以标为完整转换                                                  |
| 继承初始状态（5 份）         | 含实际 Buff、剩余持续时间、失衡、附着及周期计时，不是空对象误判                                                                                                                      | 如何用新版定义恢复来源、时钟和状态，不能直接搬旧数值                                                  |
| 合约及装备覆盖（各 1 份）    | 6a379575995e559c358c49ba 有合约标签；6988685ecd66ab50c976a9d0 修改了套装持续时间                                                                                                     | 对应规则如何转换；用户此前允许忽略的是技能覆盖，不能自动扩大到这些配置                                |

递归展开的详细调查：

- 伊冯另两份递归展开未到收尾：6a7c8768be5733cbd2f1627b、6a6efc22fadd074c1dd67068。
  已确认两份均在展开第一段 ultimateAttack1 时实际路由到 basicAttack1，并非递归中途卡住。
  展开发生在智能调时之前；第二份强化普攻比旧终结技块结束早 3 帧，不能假定原时间足够精确。
  2026-09-19 临时实验将展开移到调时之后：第一份能完整展开，第二份第一段路由也正确，
  但其后同轴连携已按单段普攻宽度放在 1345 帧，规划器观察到 1344 帧即停止，第三段尚未结束。
  因此仅交换两步顺序仍不正确；实验未合入正式转换器，也未更新正式对比结果。
  实验结果在 tmp/yvonne-after-retiming-probe-20260919，原始诊断在 tmp/yvonne-investigation-20260919。
  后续应把递归输入纳入逐技能排程：先确定第一段合法时间，再根据已确认输入产生的真实路由
  逐段安排，收尾后才决定同轴下一旧技能的位置。其他轨道输入仍需按旧全局顺序参与模拟，
  不能先屏蔽它们生成整链，也不能把整条轴反复重排当作正式算法。保留时间模式仍不自动后移。
  验收至少覆盖这两份轴、已有零帧收尾轴、跨轨道 Buff 改变递归路由、同轴下一技能过近，
  并确认新段已纳入切面输入、主控推断和最终时长。不得强塞强化状态或改游戏技能规则。

结果根目录 `tmp/public-share-batch-20260918`：sources 为原始解码存档，results 为逐轴项目、
转换报告和模拟审计；manifest.json 记录列表、版本与输入哈希，outcomes.json 记录每份结果，
comparison.md 为全量伤害/预览表，problem-groups.json 保留详细错误分组。
157 份输入哈希已复核，256 个方案均有模拟执行结果记录。所有原文件留在忽略目录。
入口脚本为 tmp/batch-public-shares-20260918.mjs，汇总为 tmp/summarize-public-share-batch-20260918.mjs。
临时预览优先读本轮全量结果，找不到才读原小样本目录；两批文件没有相互覆盖。
下一步继续修复证据和处理方式明确的问题；上表议题先明确规则，再实现，不把待定项视为已修复。

2026-09-18 台式机全量下载轴复算：31 份公开分享原文件与 Downloads 中两份用户存档，
共 33 份、50 个方案，均已重新转换并调用正式模拟审计；旧版使用对照工作树 d248cf30
重新构建后重算。tmp 根目录另有 5 份公开轴副本，核对 scenarioList 后均与分享目录重复。
结果目录 `tmp/axis-refresh-20260918`，含清单、输入哈希、逐轴转换报告、逐方案审计及汇总。
`tmp/timeline-damage-comparison-20260918.md` 和之前 20260913 的表已更新，历史表另存目录内。
临时预览路由统一读取本次输出，包括本地用户轴；同一文件的方案通过页面顶部切换。

本批次 50 个方案现已全部完成转换，无转换遗漏或智能重排回退，对比表已更新。
完整转换不代表所有排轴衔接正确；累计仍有 233 个技能块施放告警，需要继续审计。
本次修复敌人和干员定时标记恢复遗漏 global/globalScaled 时钟绑定，覆盖正式装配与独立恢复入口；
恢复测试 2 文件 3 项、应用类型检查通过。简单自用电队钱本不再因此回退，当前期望 2334881.75，旧版 3318525。
随后修复能力实体定时标记的宿主身份不一致：创建/恢复均使用 logicalAbilityEntityRuntimeId，
不再写入恢复查询无法识别的 abilityEntity: 前缀。3 份存档的 6 个方案恢复完整智能转换：
洛汤洁佩三方案、别汤冷/热启动、诀汤梨洁。实体恢复回归 21 项通过。
本轮继续修复技能附属 Buff 的引用失效：回收回调结束后才置失效；技能清理逐项检查有效性，
恢复保留失效引用的身份，到正常清理阶段再移除。使用容器已有分配进度校验，不新增死对象副本。
错误编号、错误宿主和活实例缺少绑定仍报错。6 份原先因此回退的轴全部重算，转换报告均无问题。
随后修复动作结束时解析已回收能力实体上的旧 Buff 引用：使用既有引用解析入口，
不再通过要求目标存活的新动作入口查找；连续执行和恢复执行使用同一规则。
普通计数 Buff 没有来源属性消费者，创建时不再保存多余的来源属性读取绑定；来源身份仍保留，
真正读取来源属性的护盾保留绑定与恢复校验，未解析定义的请求亦保留原行为。
原先因此失败的另外 6 个项目、10 个方案已重算，转换报告无问题。
新增验证：Buff 动作、运行时装配、Buff 恢复共 3 文件 191 项通过，应用非增量类型检查通过。

历史格式后续结果如下，不能统一归类为模拟 bug：

最新决定：用户要求忽略旧技能自定义覆盖，直接按标准技能转换。入口已删除转换副本中的
characterOverrides，并通过 ignoredCustomizations 非阻塞记录；下表涉及覆盖的阻塞已解除。
受影响的存档已重算，对比表已更新。伊冯递归展开问题也已修复，当前无未完成转换。

| 存档 ID                  | 当前阻塞                           | 后续边界                                                                                             |
| ------------------------ | ---------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 6960d9afaf72c0e43d122dfb | 已完整转换                         | 按用户决定忽略技能覆盖，报告无转换问题                                                               |
| 697c86319b5c55801918f23c | 两个方案已完整转换                 | 历史 gamedata 确认莱万汀与狼卫变体，关联连线正常保留，报告无转换问题                                 |
| 6a1be443fcf0bc1eebab6d48 | 已完整转换                         | 用户明确指定 v_1776605099210 对应强化重击，三个分段分别映射 enhancedBasicAttack1/2/3；报告无转换问题 |
| 6a7401f9fadd074c1dd678d2 | 已完整转换，展开为 10 段并到达收尾 | 修复运行时已记录零帧接续边界却不发布回执的问题；不是转换规则缺失                                     |

伊冯根因：旧动作 default_sc/0/11 展开到帧 699 的 ultimateAttack3B 时，当前状态使收尾技能
在局部第 0 帧已可接续。运行时记下 reachedOperableBoundaryFrame=0，但发布入口拒绝 <=0，
之后也不再重判，导致展开器永远收不到边界。现允许零帧边界发布一次，显示层沿用实际帧差+1，
因此显示宽度最少 1 帧；递归输入仍在回执帧+1，不会在同帧连续施放。
不改生成技能数据、不放宽输入条件、不添加伊冯特例。真实轴已重算，转换 issues/fatalIssues 为空；
新版期望 337521.64，旧版 529183，仍有 8 个施放告警，伤害差异尚不能据此认定为 bug。
验证覆盖边界发布、显示时长、递归规划及真实普攻接续：首轮 3 文件 54 项，新增规划回归后
递归/普攻接续 2 文件 119 项通过。定向验证没有替代全量新旧伤害核对。

早期格式适配依据旧工作树 d248cf30 的 data/index、hydrateTrackInstances 与项目加载流程，
实现集中在 tools/legacy-timeline 的入口，不修改模拟内核。旧表默认养成提取为静态快照，
普通技能先归一化再复用现有全量映射；已有构筑实例保留，轨道派生属性不重复叠加。
随后依据两个历史 gamedata 快照补入 32 个预设变体与 4 个莱万汀强化普攻分段。
映射记录历史来源，未把旧命中数值写入新版定义，也不强制满足旧变体名称中的层数条件。
再按用户指定补入庄方宜三个强化重击分段，重算后完整方案为 49/50。映射证据注明用户指定，
没有将其误写为历史数据证明；映射与入口定向 18 项通过。
入口和转换回归 2 文件 36 项通过。细则见 tools/legacy-timeline/README.md。
引用生存期的研究和实施顺序已写入[切面任务清单](runtime-checkpoint-plan.md#回收后引用研究结论与修复顺序)：
本机镜像直接补证了 ObjectPtr 的缓存 UID 校验、Buff 回收清零 UID、技能清理跳过失效引用。
方案已修正为统一还原引用身份和有效性，不在恢复层按用途猜测能否丢弃引用，也不默认引用阻止回收。
回收回调先于 UID 清零，已保留此顺序。能力实体 Reset 和来源属性读取路径仍需补证，未放宽相关检查。
定向检查覆盖 21 个文件、487 项：首次 486 项通过，剩余一条旧投射物测试按已确认的首次仅 Execute
规则纠正后，其所在文件 7 项通过。引用回归覆盖存活、已结束未回收、已回收、同名新实例和清理中失效。
核心修改集中在 Buff、引用解析、技能清理和恢复预检；其他源码变动主要是测试句柄补齐回收状态。
最终应用非增量类型检查、git diff --check 通过。本轮未运行全量测试。
上述原始数据与脚本均留在忽略目录，未提交用户存档。当前新增代码尚未提交。

2026-09-18 原生一致性核验：先完善 combat-spec 的 `docs/timeline-lifecycle.md`、
TimelineActionProcessor 与回归测试，再修改 Endaxis。台式机当前 runtime image 和配套符号确认：
Load 保留配置顺序，普通 Tick 按配置顺序逐节点推进并结束，不按起始帧重排执行。
Endaxis 保留按起始帧索引的程序/切面结构，执行时恢复声明顺序，不再先推进所有活动项、
再启动新项、最后倒序结束；Buff 时间线去掉提前排序，复用公共创建与恢复路径。
这会纠正同帧 Buff 结束与伤害节点的先后关系。证据地址、文件哈希及 IFix 限制见上述规格文档。
后续补证并修正首次启动：普通时间线启动当次只 Execute，后续访问才 Tick 并检查 End，
包括起止同帧与跨过整个区间；主动 CastEnd/Jump 仍按已有协议清理。瞬时区间沿用活动集合，
不新增切面字段；覆盖 Execute 后恢复、不重放、正确收尾及同帧效果寿命回归。
另在同一当前镜像确认 TickIntervalAction.ExecuteInternal 自身调用 OnTick(0)，
保留启动当次周期触发，删除 Endaxis 对该动作“跳过首次普通 Tick”的旧补偿。
combat-spec 已先补证并修正此动作内部调用；不能把外层首次无 Tick 泛化为具体动作不自调 Tick。
IFix 和跳转重入边界仍不宣称全部闭合。

资源审计边界：本机 `tmp/game-data-hybrid-full-20260903` 的 Skill/Buff/Character/
AbilityEntity/Projectile 数据共 6211 份，未检出 IgniteDamageScalar 与
PhysicalInflictionDamageScalar 属性引用，元数据默认值均为 1；暂不扩展无消费者的动态通道。
本机重建报告标记 vfsVersionVerified=false，不能据此声称覆盖最新同版本全量数据。
装备直接施加异常/倒地的限制也未发现生成代码直接消费者；79 武器、24 套装的真实构筑检查通过。
验证：combat-spec 时间线/跳转/周期动作 48 项通过；Endaxis 首次启动修正后
调度/技能/Buff/切面/投射物/装备反应 6 文件 204 项通过，新增切面和同帧效果回归后的调度文件 24 项通过。
前轮装备定向 103 项属于首次启动修正前的验证。均为串行定向验证，本轮未跑全量测试。
周期动作最终修正后，动作序列 51 项、干员技能模拟与投射物回调 364 项通过；
切面分支测试更新旧跳过预期后 3 项通过，应用非增量类型检查通过。
用户要求改完立即提交推送，至此停止追加测试。

随后正式合并 Lieyuan621/main（13b6905c）：保留新版已删除的旧页面、旧模拟器、旧生成器目录的删除，
不恢复旧架构新增的孤立模块；生成资源与游戏语言表保留新版统一导出结果。
接收选择弹窗分组标题背景修正、公共图标测试、数字框主题色测试和失衡目标装备词条显示修正。
公共样式已在 c02f3113 适配；合并冲突中的新版组件接口、布局测试及图标目录予以保留。

2026-09-18 上游样式适配：已 fetch Lieyuan621/main 至 13b6905c，以 39d7398e 为合并前检查点。
同步公共控件尺寸、悬停反馈、显示菜单选中样式和数字框主题色处理，养成技能加减改用公共图标。
App.vue 删除重复全局覆盖，主题、基础布局和 Element Plus 适配统一由 design-system/styles 负责。
保留新版数字框 focus/select、侧栏图标插槽与骰子图标接口；旧版导出业务和战斗改动不属于此轮样式适配。
验证：设计系统、数字框、工具栏与 hit 面板定向 6 文件 84 项通过；应用非增量类型检查通过。
浏览器刷新后时间轴正常加载，已查看显示菜单；本轮未跑全量测试。

敌人状态区已按旧版恢复密度适配：可用高度除以行数、扣除 4px 行距后，图标与行高限制在
14–20px；不再用完整 20px 行布局抬高区域最小高度。分隔条使用旧版固定下限 46/26/52px，
仅在相邻展开区域之间转移高度，跨越折叠区域；保留比例记忆、拖动光标和禁止文本选中。
实际拖动发现 clientHeight 扣边框/取整会使第三段漂移，现用 border-box 实际高度作为起点。
浏览器实测状态区增高 24px、失衡区降低 24px，技力区高度不变，图标从 14px 恢复 20px。

最新 UI 边界（2026-09-18）：用户明确要求 hit 详情的静态构筑只保留一个攻击力数值，
逐项养成、装备及四维静态来源留在属性详情面板；hit 中只展开动态影响因素及保留计算公式。
下文先前扩展静态来源树的进度已被此取舍替代，相应 UI 和无消费者的投影辅助函数已删除。
武器静态来源命名使用已有显示身份（发布结果用发布快照、属性面板用当前定义），
不直接拿 `wpn_funnel_0016` 查询以 `wpn_artsunit_0016` 为键的语言表，也不另写前缀替换。

方案继承设计与验收见[方案继承与通用编辑约束](../architecture/scenario-inheritance.md)。
本轮基于 `3e909888`；用户已完成界面验收，代码审查后随本次提交保存。

- 存档保留独立构筑与完整作者输入，仅新增继承帧和来源 ID；不存切面、不复制游戏定义。
  来源仅导航，删除来源或单独保存继承方案不影响打开；接受游戏数据更新改变历史结果。
- 创建使用正式模拟回执裁切连续组，只保留 F 前输入；整条未来组直接排除。
  已开始组的未知实际帧拒绝猜测。再次继承不能倒退，不能早于原轴实际初始化帧。
- 场景/项目编辑会话共用配置锁、历史前缀和对象输入锁；保留原有位置锁语义。
  技能、切人、消耗品和人工事件均受约束，F 帧可编辑；历史外观、复制和查看保留。
- 模拟服务重放到 F 输入前并缓存切面，后续替换后缀；单服务最多一个前缀，释放时清理。
  自动接续和紧凑排列共用原运行时判定；历史技能/Buff/投射物及回执随切面继续。
  后缀及历史外观编辑不重建前缀，不把未来输入保存为战斗状态。
- 继承起点复用准备区的竖栏、底色、折叠箭头及顶部按钮；顶部按钮和边界右键打开继承菜单。
  共用准备区坐标换算，折叠时裁掉 F 前历史，仅保留 18px 展开入口，不压缩历史内容。
  跨 F 的持续内容仅显示 F 后片段；展开可回看负帧，不限制视窗滚动。
  不再显示重复的模拟起始线、右上角入口或常驻说明栏。横向拖动条使用原布局。
- 已修复未来技能拖入历史后锁死：拖动按最早组首整体限位到 F；提交拒绝时撤回预览。
  养成配装、资源监控、随机配置、敌人和危机合约等入口已只读。

验证证据（定向检查，不是全量测试）：

- 模拟服务 28 项；输入运行时/会话 43 项；拖动/命令/吸附/几何 74 项；
  创建继承 5 项。应用类型检查通过，UI 改动另做组件编译和定向检查。
- 真实旧轴经正式创建函数在 F=600 继承，独立保存重开、来源缺失、全新仓库与服务：
  2794 条回执、113 次命中及最终资源与同输入普通轴一致。
  审计脚本 `tmp/check-inherited.mjs` 和样本均在忽略目录，不提交。
- 浏览器佩丽卡：F=74，未来战技左拖停在 F，再次右拖成功；历史技能不移动，
  删除/禁用菜单不可用而复制/颜色可用。来源导航、配置只读、起点对齐、弹层和滚动条已实测。
- 用户已验收继承区交互：继承图标菜单仅保留来源跳转，轴上箭头展开/折叠；
  展开定位 F−10 秒，更早历史可通过滑块查看，分界线拖动只调整视窗，不改冻结边界。
  浏览器确认历史技能不再压进窄栏，来源按钮可正常跳转。
- 提交前定向检查：核心继承、编辑约束、存档及坐标等 10 文件 87 项中，
  86 项首次通过；1 项仍断言旧压缩语义，已修正并与编辑会话测试一起复跑通过。
  不执行全量测试。临时样本、审计脚本和转储不进入提交。

优先项 1 已提交为 `56cc0707`，收束实际使用的属性修正回执。优先项 2 仅扩展只读贡献投影：
直接 multiplyValue 复用独立因子分配，当前元素的纯加法抗性按百分点转换为因子增量；
混合乘法槽、缺少冻结读数、非正基线或限幅情况不冒充分解。未新增运行时字段、来源链或黑板计算图。
贡献及现有总分析/图布局定向 3 文件 27 项通过。真实旧轴 113 次命中逐项总量守恒，
总伤害 4702772.4794，自身 3782268.1264、外部 920504.3530；样本外部分配仅覆盖 damageScale，
新增两条路径由定向测试覆盖。89 次命中仍有不支持的修正，相应效果留在自身项，诊断仅供开发使用。
应用非增量类型检查通过；未跑全量测试。

本机接续已修复七乘区属性来源的误收录：伤害回执只保留实际注入乘区的属性明细，
不再追加完整属性快照中的同类记录，同时过滤加成 0、倍率 1 和属性槽单位元。
图、hit 详情与贡献查询共用修正后的回执，旧发布结果需要重新模拟。
非乘区属性也已改为在实际计算分支/公式输入读取处收集：固定伤害不带攻击来源，指定属性计算只取该属性，
攻击取基础计算阶段，抗性只取当前伤害类型；四维系数为零时不收录该四维加成。
暴击率与暴伤仍用于回执同时发布的实际/期望伤害，不因本次未暴击而剔除。
攻击快照缺少历史来源时不拿当前 Buff 填补；未做逐项移除重算或钳制后的边际贡献判定。
定向 4 文件 44 项、依赖提升后的应用非增量类型检查通过；未跑全量测试。
本机真实旧轴同版本前后对照为 113 次命中、2794 条回执，
除修正明细外的全部回执字段一致。七乘区修复后记录从 3488 降至 1638，非乘区收束后进一步降至 1155；11 次自然爆发命中中
无关的 electricEnhancedDamageIncrease 来源从 11 条降至 0。审计脚本在忽略目录 tmp。
后续非乘区定向 4 文件 48 项通过，覆盖固定/指定属性/真伤、零系数四维及前后阶段攻击来源。
依赖已按锁文件安装到本工作树 .local/node_modules，根 node_modules junction 指向该目录，
不再共享旧 Endaxis 工作树的依赖；Dagre 可正常解析。未修改依赖清单或锁文件。
该独立目录需使用 --shamefully-hoist：现有源码直接引用了 Vue 编译器、服务端渲染器及
Element Plus 图标等间接依赖，默认隔离安装无法解析。此为既有依赖声明缺口，不属于伤害修复。

提弗洛斯连携块外的大量命中已核对来源：连携创建独立实体
`abilityentity_chr_0034_typhoea_combo_presistdamage`，子技能持续 180 帧，
每约 0.333 秒命中一次、最多 18 次，总持续伤害倍率除以 18。用户轴保留原时间时，
18 次命中落在第 257～606 帧；实体时间受膨胀影响，不能据此延长角色动作块。
目前仍按来源 castId 显示在连携后方；独立持续效果条只是后续展示建议，本轮尚未实现。

梨诺自定义保存误报缺失 Buff 已修复：五处 `buffIdStackCompare` 查询会引用未收录的
`buff_chr_0035_liino_chrdung_armorbreak`。这类条件只统计已有实例，没有匹配时为 0，
并不需要创建该 Buff。编辑器引用索引现在将其标为实例查询，保留使用处导航；
`applyBuff` 等创建引用仍检查定义是否存在。真实梨诺定义与缺失创建引用的回归通过，未改生成数据或模拟行为。

补查 Buff 时钟：用户指出旧版会把诀连携脆弱随终结技冻结延长。现用“保留原时间”排除顺延后，
最后爆发在两版均为 20 秒，新版仍为 136935.39、旧版 644637；之前只解释技能顺延并不完整。
旧版诀连携脆弱从 9.2 到 20.783 秒，新版从 8.967 到 15.6 秒。原始 BuffData 中数值脆弱
useTimeDilationDt=false；只有 seal_total 内部流程为 true，新版已正确生成 global 时钟。
combat-spec/time-dilation.md 的 9 月 8 日原生调用链更正说明 TimeDilation 原因下默认 Buff
使用未缩放时间。旧版默认给效果寿命加冻结时长不等于原生规则，本轮不改新版计时。
116 项时钟/Buff 测试通过；详细记录已补入 tmp 中的 burst-comparison.md。

旧轴导入弹窗现在可选“智能修复时间”（默认）和“保留原时间”。保留模式跳过同轴顺延、
全局间隔推迟、终结技避让、同帧主控输入拆分和接续窗口搜索；仍复用逐步模拟判断技能形态、
补主控切换。没有另建一套转换器。用户提弗洛斯轴实测零时间调整，最后技能保持第 589 帧，
强化连携仍自动识别；浏览器中完成实际选择、导入和模拟。相关 40 项测试及应用类型检查通过。

提弗洛斯轴最后法术爆发已逐项复算：旧版 644637，新版 95293.2789（14.78%）。
同一技能因转换顺延从旧版战斗时间 20 秒推迟至 23.367 秒，多项增幅/脆弱/易伤结束；
基础爆发倍率和附着增强一致，旧版另乘 1.45408163 等级系数，当前原生动作链没有这一步。
完整因子及结束回执见 `tmp/typhoeus-combo-fixed-20260917/burst-comparison.md`。
分析中修复了事件条件用错 Buff 技能编号的问题：普通事件中的 CheckSkillCastId 也要读
affixSkillCastId，0 不得回退普通来源。这样“使命必达”连携击飞后正常给全队增伤；
中段爆发从 50.11 万提升至 59.92 万，最后一击因增益已结束不变。
条件回归、Buff 生命周期和标准环境共 187 项通过，实际武器触发/15 秒结束回归通过。

提弗洛斯强化连携导致模拟失败的问题已修复。其 `smartTarget: trigger` 可以选择触发连携的
干员，原来的施法准备却一律拒绝非敌人目标。对照 combat-spec 的
`combo-smart-target-and-template-init.md`、`combo-cast-preparation.md` 和
`skill-smart-target-outer.md`，现在保留干员目标的真实身份，沿用当前固定有效实体、零距离
模拟范围；没有把友方偷偷替换为敌人，也没有宣称支持完整空间选敌。
能力实体和空间点仍需要各自的选中规则，不能直接放行。

用户的 `Downloads/Endaxis_Timeline_2026-09-16.json` 已重新转换到
`tmp/typhoeus-combo-fixed-20260917/project.json`。原有转换流程从施放前切面试运行，
依据实际技能输入路由，将普通技能替换为同技能组中声明的替代技能；本次不增加提弗洛斯特例。
报告记录了 `track:0:cast:5` 从 `comboSkill` 改选为 `floatingComboSkill`，转换无错误，
整轴模拟成功。审计命令也修正为先按项目加载数据仓库，再叠加自定义定义。
错误提示保持右下角固定位置，支持选中、滚动全文及复制；浏览器已核对完整多行复制。
相关施法准备、转换及提示测试共 44 项通过，应用类型检查通过。

敌人身上的 Buff 伤害不再依赖 Buff 是否有可见持续条。击飞等被头顶栏隐藏的效果
保留独立瞬时图标和可点击伤害点，图标取实际来源 Buff；没有图标时使用通用占位。
有持续条的伤害仍挂在原状态行，独立伤害不重复挂回技能块。战斗伤害归属和计算不变。

物理异常仅指击飞、倒地、猛击、碎甲；它们与破防共用敌人状态区第一行。
破防本身不归类为物理异常，其他状态也不能凭颜色或头顶栏标记混入该行。
`physicalStatusDisplay.ts` 以实际 `producedBy` 关联
击飞/倒地与它们施加的破防，使用异常图标和破防层数，详情可切换两个真实 Buff；
同帧不相关实例不合并。同帧多个图标横向错开，持续条到下一个物理状态标记处截断显示，
详情仍保留实际时长。消费破防的猛击/碎甲也在同一行，角标显示剩余破防层数。
没有破防时，物理异常输入可能只创建破防；施加回执新增 `physicalInflictionType`，
用于选图标，不虚构第二个 Buff，不新增切面状态。旧发布结果需重新模拟取得该字段。
组件浏览器验收覆盖同一行、层数角标、双 Buff 翻页和伤害点击入口。

同步旧轴导入保留。hit 详情已展示七个伤害加成区间、攻击公式及实际执行的 Buff 属性槽、
damageScale、multiplyValue 直接修正，区分增伤、易伤和脆弱。武器 Buff 名称沿用时间轴规则。
总伤害分析保留攻击者统计，另接通显式 damageScale、直接 multiplyValue 及纯加法抗性的外部贡献分配。
攻击、暴击等尚未分解的效果留在自身项，不能把来源图当作完整归因。

本轮基于 `fa9b39ed` 贯通直接修正：运行时记录精确 Buff 实例和七乘区双方结算值；
`CombatObjectOrigins.directModifiers` 共供命中明细、来源图及贡献投影使用。
贡献只沿提供者归属找干员，不沿产生链追溯分账，不依赖图布局和节点上限。
来源图将有精确 Buff 实例的修正折叠进该 Buff 节点，命中直接连到 Buff；同实例多项修正
在详情列出并汇总贡献，无实例修正仍独立显示。仅展示层折叠，底层记录及贡献算法不变。
总分析按提供者聚合，出现负贡献时改用有符号表格。
护盾吸收、非正基线及不稳定分解暂退回自身，诊断仅供开发。详见[贡献设计](../architecture/damage-contribution.md)。

[对象来源查询](../architecture/object-origins.md)第一阶段已实现：

- `CombatObjectOrigins` 从发布回执建立可重建的只读索引，复用现有实体、Buff、施法和回执身份。
  区分产生者、原生 Source、归属、来源施法、直接修正和提供者，不建持久来源镜像或黑板计算图。
- 实体、投射物、Buff、全局 Buff 出生及伤害回执保存直接执行者。Buff 出生早于 Start/Enable，
  投射物出生早于同步发射回调；施加记录使用实际返回实例，刷新不改写出生，恢复不重复出生。
- 删除 `collectTriggeredHitStepKeys` 静态扫描和发布元数据中的步骤集合。
  hit 仅在实际 producedBy 路径包含 Buff 时标黄，增益、Source、归属不混入产生路径。
- Buff 过程回执保留实际层数前后值、叠层尝试、消费/吸收和结束/回收。满层增强尝试不冒充实际增长；
  不按同帧或定义 ID 猜实例，不从消费推导贡献比例，不新增可恢复层数历史状态。

hit 详情的“对象来源图”打开独立节点面板。当前最终交互要求：

- **所有模式都只向前追溯**。默认“产生与增益来源”，包含实际加成及其提供者；
  不遍历 `castEvent` / `buffEvent` 反向过程索引，不展开提供者施法的其他效果或 Buff 后续记录。
  公共查询仍保留过程索引供显式审计，不能重新接成图的递归展开。
- 布局已替换为 `@dagrejs/dagre` 的分层布局（network-simplex），删除手写分层、排序和顶部绕线。
  多重边及跨层边交给库处理，SVG 沿输出路由点平滑连接，关系方向与底层查询不变。
  已修复历史来源误用命中时最新状态形成环的问题：每条边按自身回执序号定位此前状态。
  同帧 A 产生 B、B 增强 A 的用例此前可复现 Dagre 矩形交点异常，现在保持 A₂ → B → A₁。
  另确认 Dagre 3.1.1 在合法分支 DAG 的三条平行关系上也会报相同异常。
  每条关系现经一个不可见布局点，仍由 Dagre 路由，再接回单条显示线；业务节点与关系不变。
  4 节点、5 边的报错回归、1000 个分支 DAG 及复杂图浏览器验收通过。
  时间轴 Buff 详情也可打开来源图，从当前选中的精确实例开始追溯；合并状态翻页不混用同名实例。
  已接入附着转化回执：产物 → 转化事件 → 被消耗实例，并展示消耗层数。
  Buff 层数状态按回执串成前后关系（3 层 → 2 层 → 1 层），每次变化保留各自来源。
  状态段保存 startSequence，详情按所选窗口的序号截断，同帧后续变化不会混入。
  叠层写入透传本次 producedBy 与 skillCastInfo，附着执行器复用 operationProducer；
  来源不再只停在干员。自动定时增长记录为 Buff 自身，图中定位到增长前的状态。
  旧发布结果需重新模拟才能取得新增的执行者事实。
  转化的消耗边定位到转化前的具体状态；不新增运行时层数历史镜像。
  转化关系仅在其回执已发布到当前历史截止时可见；这些关系不参与贡献分配。
  7 项布局测试、应用类型检查和生产构建通过；浏览器验收了 12 Buff、3 个共享提供者的复杂图样本。
- 共用历史状态节点只画一次，因果图保持 DAG。Dagre 统一计算节点位置与连线路由；
  打开时以可读比例定位当前命中，可拖动、缩放、适应全图。仅选中节点的邻边显示标签。
- 行为沿归属关系连到具体干员。节点和详情接入干员、技能/天赋、武器/装备、Buff、元素及法术爆发图标。
  展示元数据随发布结果冻结；Buff 图标按所属对象、实例和截止序号查找，武器来源优先，缺图保留文字。
- 查询按当前 hit 的回执序号截止，最多显示 128 个对象；没有新增图编辑功能。

## 下一步与缺口

1. 来源图程序节点已复用发布快照的命名规则，区分技能、天赋、潜能、武器技能、装备词条和套装效果。
   武器/装备的处理器键通过发布时冻结的词条索引定位，重名处理器不强选；技术 ID 留在详情。
   Buff 等对象的完整名称解析仍需补齐。
   修正已记录精确 Buff 实例；无实体宿主的内部子技能仍无独立执行身份，不能猜补。
2. hit 明细已复用发布面板回执补齐四维与暴击提示的静态配装/信赖/养成来源，不增加运行时字段。
   攻击快照现在随捕获值保存已有直接修正记录，命中时复用；不读取命中时攻击 Buff 冒充快照来源。
   实时与快照共用攻击公式的属性读取清单。捕获后替换 Buff、保存恢复再命中的回归及相关 28 项通过。
   技能步骤直接属性/乘区修正已复用 appliedDamageModifiers，采用实际执行宿主与行为身份，
   不伪造 Buff。详情以已有行为命名显示来源；相关执行、来源查询与贡献投影 42 项通过。
   Buff 内伤害步骤的直接修正同时复用 executingBuff 的定义与实例身份，避免误显示为普通技能；
   普通技能仍只记录行为身份。执行器与对象来源查询定向 28 项通过。
   技能 statModifiers 的暴击率/失衡增伤补入快照已有 modifierDetails，环境合并而非覆盖静态来源；
   下游仍在实际读取属性时筛选，因此失衡增伤不会无条件进入所有命中。快照/执行器 40 项、
   标准伤害环境 95 项通过，未改数值计算。
   尚需真实轴与页面核对各乘区来源闭合、无效修正过滤及阶段间时点，不能据局部测试宣称完成。
   本轮真实轴完整时长检查：125 hit（21 次攻击快照），125 项冻结公式还原期望伤害通过；
   1301 条直接修正无加零/乘一记录。此样本无技能直接修正，该路径单独回归覆盖。
   忽略目录脚本 tmp/audit-hit-sources.mjs 仅用于审计，不提交。
   浏览器实测佩丽卡战技：展开攻击树后，智识与意志的静态来源分别嵌套在对应属性下，
   不列攻击系数为零的属性；固定攻击也补入逐来源行。来源投影及组件定向 10 项通过。
   四维换算系数修正明确标注对应属性与“属性换算系数”，不再与属性数值加成混淆。
   该页面样本无装备和动态 Buff，不能代替这些来源及技能直接修正的页面验收。
   当前防御来自敌人配置，点燃/物理异常倍率在正式属性快照固定为 1；不补造动态来源。
   已确认仍未闭合的常驻来源：resolveOperatorPanel 的 applyUpgradeModifier /
   applyEquipmentContribution 将 addStaticDamageIncrease、damageBonus、damageScale 放入
   combatModifiers 时丢弃已有 OperatorPanelContributionSource；属性集只保留 equipment 类别，
   Buff 来源采集不能恢复具体装备。真实生成定义包括 suit_atk01 / suit_burst01 套装及
   艾维文娜等干员的静态增伤潜能，不能当成未使用的旧接口略过。
   已在构筑聚合时保留已有来源身份，并在对应静态属性读入处复用现有修正回执；
   详情复用面板来源命名，不在 UI 重算装备筛选、不按数值配对、不新增递归来源链。
   同属性多装备、技能/元素筛选及构筑来源身份的定向检查通过，共 3 文件 37 项；
   应用类型检查通过。补充标准伤害执行器实测：未失衡不记录套装来源，失衡后出现
   20% 来源且伤害乘 1.2，快照文件 22 项通过。
   浏览器佩丽卡装备四二式·肃阵并实际放置战技，伤害加成 x1.320 的提示显示
   “四二式·肃阵 +32.0%”，与轴上 Buff 来源名称一致；已查看提示框截图。
   该样本验证武器 Buff 名称，新增套装静态来源及多来源提示仍须页面验收。
   继续页面验收时发现弹窗切换异常：关闭 hit 详情后立即打开护甲选择，
   useInputRegion 激活 gear-selection 报 Cannot activate a background region through a modal boundary，
   随后 Vue locateNonHydratedAsyncRoot 报空 component，选单无法打开。
   已确认 Element Plus 在 afterLeave 才发送 update:modelValue，hit 详情此前仅监听该事件；
   改为监听关闭开始的 close，与其他选单一致，不放宽模态隔离。浏览器重建测试页后
   复测“关闭详情→立即打开护甲”成功，控制台无 error，组件结构 7 项通过。
   随后实际装备壤流轻甲并重新模拟，详情增伤 x1.149、提示“壤流轻甲 +14.9%”，
   证明新接入的静态装备来源已走到页面。随后同配队加入四二式·肃阵，提示正确分两行。
   发现旧固定精度把 14.95% 显示为 14.9%、1.4695 显示为 1.470，已改为最多六位小数，
   浏览器确认“壤流轻甲 +14.95% / 四二式·肃阵 +32.0%”与 x1.4695 对应。
   未改伤害数值，组件 7 项通过；套装整体展示仍需核对。
   本轮来源查询、贡献、图布局及来源命名定向 4 文件 34 项通过，不能代替弹窗验收。
   最新真实旧轴复跑为 125 hit、1470 条修正（新增 169 条非 Buff 来源），
   总伤害仍为 4777634.695572648，全部冻结公式闭合。
3. 总贡献按[有限直接归因](../architecture/damage-contribution.md)继续，未分解伤害算自身，诊断仅供开发。
   已撤回的递归归因、专用来源链、黑板计算图和 Buff 层历史方案不得恢复；可追溯不代表应逐层分配贡献。
4. 复核运行时装配根、标准环境和时间轴页面的职责。扩展功能牵涉面超出预期时先检查架构，不能只机械接字段。
5. 切面 T8 尚需实际浏览器生产环境的主线程/Worker 堆组成测量，Vite SSR 数字不能代替。
   继续补充不同配队、自定义连续组，以及同时包含 afterCastId 与 customDefinition 的真实用户存档。
   三种尚无专用编辑器的动作仍属 UI 待办。

切面接续见[切面计划](runtime-checkpoint-plan.md)，性能背景见
[测量记录](../research/combat/checkpoint-retiming-cost-analysis-2026-09-14.md)。
按需定义加载和 Worker 场景数据包已接通，不要重新当作待实现。

## 验证边界

以下是本次开发各阶段已执行记录，提交整理不重复跑全量测试：

- 直接贡献贯通：来源查询、贡献数学、回执、标准伤害环境及总分析等 8 文件 138 项通过；
  另检查生命伤害、伤害执行器和 Buff 运行时 3 文件 130 项通过。
  应用非增量类型检查与生产构建通过，构建仍有混合导入及大 chunk 提示。
  覆盖正负抵消、双方乘区、独立乘区、精确实例、回收后查询、分支句柄隔离和护盾回退。
  本轮未进行浏览器交互验收或真实轴贡献数字对照，不能将构建通过视为视觉验收。
- 对象查询及运行时定向 15 文件 479 项通过；Buff 过程接入阶段 9 文件 440 项通过。
- 图标相关 4 文件 26 项通过；最终上游遍历与布局 2 文件 15 项通过。
  应用非增量类型检查在最终布局修改后通过；生产构建在图标阶段通过，仍有既有大 chunk 提示。
- 下载目录 `Endaxis_Timeline_2026-09-16.json`（“提诀（意）梨洁”）：602 帧、113 次命中，
  总伤害 3,989,438.2037710985。小箭雨 12/12 黄、主箭雨 0/1 黄、连携持续伤害 0/17 黄、梨诺声波 11/11 黄。
  第 122 帧实体出生后的切面恢复，状态、完整回执和结果一致。
  该轴不含投射物发射，投射物回收后来源由专门集成测试验证。
- 真实轴有 33 次实际增减层、25 次叠层尝试、1 次消费；过程回执接入后总伤害和切面恢复仍一致。
  图标审计涉及 44 个不同资源路径，均存在。独立浏览器验证页检查图标、节点、缩放、拖动和关闭；
  最终消费后命中图已确认只向上追溯、保留增益，不再因展开过程记录触及 128 节点上限。
- 历史全量测试曾有 Windows symlink 夹具问题、需原生快照的跳过项；转换器也曾有语言数量断言修复后补跑。
  不能宣称当前全仓单轮全绿。旧详细日志查 Git 历史，不将历史记录当作本次复跑。

临时验证页、真实轴和审计脚本仅在忽略目录 tmp 中，不提交，也不保证跨机器存在。
本机 pnpm exec 曾尝试重新安装并被跨工作树 node_modules 安全检查阻止；使用已有 node_modules 中的
Node CLI 执行验证，不删除或重装共享依赖。重型检查串行、限制 worker，见[验证指南](../development/validation.md)。

## 必须保持的边界

- 原生规则和同版本证据归 combat-spec；Endaxis 为单敌人、无敌人主动行为、攻击确保命中的场景。
- SP 属于全队，终结技能量属于干员；不建立投射物私有能量账户。
- 回收顺序：结束当前技能 → reset 通知（来源仍可读）→ ClearSource。
- 切面先于新输入；技能、切人和人工标记都从输入边界提交，未来不能影响过去。
- 回执历史与瞬时状态分离，分支共享不可变前缀；固定程序与每次施放登记分离。
- 切面数据归 combat/state，模块依赖单向；必要的真实对象循环与共享引用保留。
- 不手改生成技能文本，不重复实现公共结构，不提交中间产物、转储或本机环境文件。

# 2026-09-19 闪避继续实现

- combat-spec 已按原生 `Skill.canDash`、`Skill.MarkCanDash` 和 `MarkCanDash.ExecuteInternal` 复刻本次施放标记；
  标记在新施放开始时清零，测试证明它不会误开 `canInterrupt`。
- Endaxis 生成器不再把 `MarkCanDash` 归为 `dashWindowOmitted`。契约新增
  `markCurrentSkillCanDash`，技能运行时把标记保存在 `SkillRuntimeState`，切面复制和恢复会自然保留。
- `chr_0032_lizhiyan_ultimate_skill` 已在第 48 帧生成该步骤；第 48 帧前 `canDash=false`，执行后
  `canDash=true` 而 `canInterrupt=false`。
- 时间轴声明的不合法 Dash 始终执行。`DodgeInputForced` 记录非主控、连续闪避禁用窗口和终结技未开放
  Dash 等会影响游戏输入许可的原因；体力按产品约定不参与许可和 UI 诊断。
- 闪避详情新增实际模拟结果，可直接选择复制；角标也会识别 `DodgeInputForced`。
- 原生普通 Dash 输入门已从反编译闭环：`OnSprintPressed` 分别检查 `InImmobilized`、
  `InDisableDash` 和 `SuperArmor`，三条 Center→Dash 转换还会再次检查 `InImmobilized`。
  Endaxis 直接消费生成的预定义标签表，逐项记录强制执行原因；缺少标签表时记录未验证，不能默认为合法。
- 闪避体力上限的账号来源已定位为服务端变量 `ServerGameVarDashEnergyLimit`（数值 100006），
  `_OnMaxDashCountSynced` 会把它除以 `GlobalConst.dashCostEnergyValue`。表内 480 / 60 给出最大容量 8，
  不等于当前账号容量；正式模拟没有账号同步值，只记录相对消耗与返还，不能阻止闪避或显示在界面中。
- 旧版标准空闪避块已接入转换器：只接受所属干员的完整 `<GAME_ID>_dodge` 身份，按旧开始时间生成
  同轨普通向前闪避标签，不再作为未映射技能遗漏；旧格式没有方向和极限闪避成功事实，因此不补成功收益。
- 9 份含旧闪避的公开轴已在 `tmp/dodge-conversion-validation-retimed-20260919` 串行重算：
  19 个旧块全部转换，闪避未映射问题为 0，19 个方案全部完成模拟；17 个标签随相邻技能顺延，
  2 个无需移动。报告新增 `dodgeMarkerAdjustments`，保留原帧、调整帧和相邻技能身份。
- Dash 执行、强制执行、局部模拟与极限闪避声明回执统一以 `dash:<markerId>` 动作身份指回时间轴标签；
  公共 Dash Buff 已沿用同一 `producedBy`，后续 Buff、技能和伤害来源图不会丢失标签入口。
- 全量数据重建连续两次被来源服务阻断：一次 AKEDB HTTP 570，一次本机 VFS 读取
  `abilityentity_interact_bomb_skill_2.json` 返回 HTTP 422。失败任务均未发布候选；本次所需的
  `chr_0032_lizhiyan` 生成差异已按新生成器的确定输出更新，并由投影测试和第 48 帧运行时测试覆盖。
