# 当前交接

更新：2026-09-15。Endaxis 分支为 `refactor/common-game-data`，本轮拉取后的基线为 `5cce2dab`；接续以实际 HEAD 为准。
接续时实际核对工作树和状态，不操作其他留有未提交内容的旧工作树。

## 当前工作

主线为[代码组织与冗余清理](code-organization-plan.md)，同时继续 T8 内存收束。
目录归位、切面状态补漏、31 个干员 .generated.ts 命名统一及生成数据出口合并已提交。
上游 Lieyuan621/main 的 0262ca07 已合入；保留当前编辑器和模拟路径。

此前数据清理已提交：

- 敌人分类改为重建期间的临时文件，生成成功或失败均清理；删除仓库副本和发布项。
- 删除被 TypeScript 替代的敌人分类 Python 提取器及其自测、无引用的投射物筛选夹具。
- 投射物黑板仍有旧版兼容消费者，移入 tools/game-data-compiler/legacy/evidence/。
- 正式运行时读取的 skill-setting.generated.json 保留。

本轮完成：删除 editor-demo 路由与演示组件；危机合约选择目录合入生成 TS，
中英文名称与说明进入独立语言文件并通过统一加载器读取。旧目录 JSON 与图标专用读取分支已删除。
同版本表重新生成后，46 个词条的双语内容、24 个已支持词条的计划及 49 个 Buff 定义保持一致。
面板自身的重置、删除、支持状态等 UI 文案归入公共 `src/i18n/locales` 中、英、俄资源，
生成的游戏语言文件只保存原生词条名称和说明。本机验证：相关 14 文件、56 项通过，
面板与语言加载相关 3 文件、20 项通过；应用非增量与转换器完整类型检查通过，生产构建通过
（仍有大 chunk 提示）；定义和语言产物的正式生成命令 --check 均通过。

GlobalBuff 模板目录已改为每轮在 tmp 生成，联合战斗定义编译完成后成功/失败均清理，
不再进入候选覆盖或正式发布。公共生成写入器的 staging、backup 与原子文件使用目标同卷的临时
目录，避免 Windows 跨盘符重命名失败，并在成功或失败后清理。无运行时消费者的能力实体 generated JSON、包装、自测和旧生成命令已删除；
当前整批转换仍直接读取同次 `AbilityEntityData`。[JSON 用途清单](json-file-inventory.md) 只剩旧兼容入口待逐项判断。

能力实体生成入口不再接受重复的 `--ability-entity-catalog` 参数，统一从当轮 `sourceRoot/AbilityEntityData`
读取；旧的 1.4.4 能力实体聚合 JSON、文件容器兼容解析和无消费者的 GameplayTag 来源清单已删除。
生成器全套 187 文件、2026 项通过、1 文件和 2 项跳过。测试同时发现公共写入器把 staging 固定放在
仓库 tmp 时，目标位于另一盘符会触发 `EXDEV`；现在原子发布目录创建在目标同级并在结束后清理，
跨盘符候选、装备目录、单文件和资源快照都不会再跨卷 `rename`。

文档只维护现状和专题结论。旧 archive、过期生成审计及装备报告已删除；原生逻辑研究已归 combat-spec。
旧开发过程查 Git 历史，不再用归档目录继续堆日志。

本轮拉取后又发现一个主线程整库入口：`editorSimulationService` 会在页面已经建立项目仓库后再次
静态导入完整游戏数据。现已改成由调用方显式传入页面仓库；需要完整正式数据的集成测试统一走
`application/simulation/testSupport`。同一真实长轴的 Vite SSR 分阶段测量中，模拟服务加载后的
稳定堆约从 124.6 MiB 降到 55.3 MiB；加载该轴实际引用的 4 名干员、4 把武器、15 件装备和 6 个
套装后为 66.9 MiB，进程最大 RSS 约从 391.9 MiB 降到 311.1 MiB。生产预览中的四人长轴已重新
加载并完成模拟，控制台无错误。具体边界见[性能分析](../research/combat/checkpoint-retiming-cost-analysis-2026-09-14.md)。

## 尚未完成

1. 复核运行时装配根、标准环境与时间轴页面的剩余职责；目录移动不等于职责已经合理。
2. 代码整理最终的同基线真实轴对照；不以源码结构测试代替行为对照。
3. 按 JSON 清单收束剩余投射物黑板旧兼容入口；能力实体聚合文件和旧 GameplayTag 来源清单已清理。
4. 切面 T8 的实际浏览器生产环境主线程/Worker 堆组成测量；当前浏览器控制接口不提供可靠堆统计，
   Vite SSR 数字不能代替它。
5. 尚无同时包含 afterCastId 与 customDefinition 的用户真实存档；派生组合验证不能冒充真实样本。
6. 三种尚无专用编辑器的动作仍是 UI 待办。更细的定义选择目录仅在数据规模需要时推进。

切面验收与性能任务见[切面计划](runtime-checkpoint-plan.md)，测量方法和已有结果见
[性能分析](../research/combat/checkpoint-retiming-cost-analysis-2026-09-14.md)。
项目按需加载定义、Worker 场景数据包已经接通，不能把后续测量写成这些功能尚未实现。

## 必须保持的边界

- 原生规则与同版本证据归 combat-spec；Endaxis 使用单敌人、无敌人主动行为、攻击命中的场景。
- SP 属于全队，终结技能量属于干员；不建立投射物私有能量账户。
- 回收顺序为结束当前技能 → reset 通知（来源仍可读）→ ClearSource。
- 切面先于新输入；技能、切人及人工标记都从输入边界提交，未来不能影响过去。
- 回执历史与瞬时状态分离，分支共享不可变前缀；固定程序与每次施放登记分离。
- 切面数据归 combat/state，模块依赖单向；实际对象图的必要循环与共享引用保留。
- 不手改生成技能文本，不重复实现公共结构，不提交中间产物、转储或本机环境。

## 最近验证边界

以下为已有记录，本次文档整理没有重新执行这些测试。

- Windows 全量 Vitest：781 文件、8337 项，初轮 8333 通过、1 项 symlink 夹具失败、3 项跳过。
  改用 Windows junction 后该文件 5 项通过；GC 额外测试 4 项、Python 测试 5 项通过。
  两项 HideUI 测试仍需显式原生快照。这不是单次全量全绿记录。
- 应用非增量、转换器完整、公共契约类型检查及生产构建通过；构建仍有大 chunk 提示。
- 上游 UI 合并：274 文件、1448 项初轮有 12 项旧结构断言失败，修正后的相关补跑通过；
  应用类型检查和构建通过。未重跑整个仓库全量测试。
- 当前数据清理：6 文件、70 项通过，覆盖临时文件成功/失败清理；转换器完整类型检查通过。
- 本轮生产目录中间产物清理：相关 12 文件、52 项及下载器 2 文件、12 项通过；
  跨 tmp 原子覆盖在 Windows 实测只留下最终文件。转换器全套初跑 185 文件、2026 项通过、
  1 项语言文件数量旧断言失败、2 项跳过；修正后该文件 20 项通过。应用与转换器类型检查、
  生产构建通过，仍有既有导入及大 chunk 提示；未重新执行整套以冒充单轮全绿。
- 切面曾通过真实轴 36 次固定帧续跑及 32 次事件边界续跑，状态、结果和有序回执一致。
  这是当时基线的验证，不代替当前代码整理后的真实轴对照。
- 当前拉取基线：核心与模拟 181 文件、1839 项通过；旧轴转换器 7 文件、55 项通过、1 项跳过；
  应用非增量类型检查和生产构建通过。
- 本轮主线程仓库注入：相关模拟集成与伤害分析 35 文件、2055 项通过；应用非增量类型检查和生产
  构建通过。生产预览重新加载真实四人长轴，伤害和状态正常生成，控制台无错误。
- 当前生成器来源清理：生成器全套 187 文件、2026 项通过、1 文件和 2 项跳过；生成器开发与生产
  类型检查、应用非增量类型检查和生产构建通过。构建仍只有既有动态/静态导入及大 chunk 提示。

重型检查串行、限制 worker。命令见[验证指南](../development/validation.md)，
本机路径见[本地环境](../development/local-environment.md)。
忽略目录中的真实轴和审计脚本不保证跨机器存在。
