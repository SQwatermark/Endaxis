# 2026-09-08：真实旧轴、拖动性能与公共 Buff 交接

## 当前任务与仓库

当前主线是真实旧轴复刻与伤害归因，编辑器由用户继续体验反馈。
用户浏览器实测确认拖动卡顿已很不明显，本轮性能问题按已解决收束。
不要继续将带 Profiler 的历史长帧作为阻塞，也不要修改技能块文本。

- Endaxis：`Endaxis-game-data-refactor`，`refactor/common-game-data`，推送同名 origin 分支。
- combat-spec：`combat-spec-operator-completion`，`refactor/operator-completion`，推送同名 origin 分支。
- VFS 本轮只用于原生证据分析，无正式代码变更；临时输出不提交。
- 旧版工作区只读。`tmp/`、原始项目、运行时二进制和解包资源不入库。

## 已完成：后台模拟与显示缓存

编辑器通过共享工厂使用原有模拟服务，在模块 Worker 中计算模拟及技能链规划。
客户端保留一个执行中请求和一个最新待执行请求；不能物理抢占正在计算的请求，
但通过版本、取消信号和发布代次拒绝失效结果。没有静默退回主线程的替代实现。
时间轴按完整的 scenario/run 快照发布，拖动期间允许完整中间结果，不分别拼接不同批次的数据。
命中效果按发布结果预计算并索引；资源曲线缓存几何，避免重复构造。

三条真实轴的 Worker 结果与同步服务 JSON 完全一致，计算期间主线程定时器能继续运行。
此前全量验证为 235 文件、1187 测试通过，应用类型检查及生产构建通过。
性能工具入口见 [性能复现说明](../../tools/performance/README.md)。工具只读取输入、输出计时，
不覆盖用户轴；没有受控的优化前后 FPS 对照，不能将 Profiler 数据包装成帧率提升比例。

## 真实轴基线与差异边界

**晚间台式机复现更正：**下表新版总数采用完整 3600 帧，不是全部采用存档结束线。
方案 6 配置结束线为 2231 帧，按该线为 264 条 / 1876431.868474918；完整时长额外包含
第 2237 帧诀连携能力实体结束子技能的 30146.527165223935，得到下表 265 条基线。
两种范围均已通过正式服务复现，未改输入或战斗规则。旧版数值的具体运行截止仍需回查，
不能把不同范围下的差额全部视为模拟问题。使用 `tools/legacy-timeline/auditSimulation.ts`
同时输出两种范围；不要依赖未提交的临时测量脚本。

使用已修正 arcana 映射的 `tmp/converted-real-axes-20260908-arcana/project.json`。
原始输入是用户 Downloads 下的 `Endaxis_Timeline_2026-08-31.json`，均为私有本地文件。

| 方案  | 旧版期望伤害 |       当前期望伤害 |
| ----- | -----------: | -----------------: |
| 方案4 |       653432 |  716503.8121548599 |
| 副本  |       311399 |  337938.8820469209 |
| 方案6 |      2126525 | 1906578.3956401418 |

共 102 个输入，新版伤害记录分别为 210 / 194 / 265 条。
旧版取正式模拟的期望伤害，新版汇总所有 DamageApplied.expectedDamage；不能漏掉没有 castId 的记录。
旧版仅供参考，不追平人工测量的几帧偏差，不为了总数相等复制旧版过期 Buff 或错误时钟。
赛希前两次连携直接命中与旧版差不到 1；末次旧 42593、新 25560.92，存在下述真实失衡承伤缺口。
旧版额外末次爆发也不能直接补：新版该次附着前无元素层，需追踪此前附着消耗。
细节归因继续维护 [映射与对照证据](../../tools/legacy-timeline/mapping-evidence.md)。

## 公共 Buff 二次核查

正式公共生成目录有 61 项。仓库运行时查询的 110 项还包括 49 项危机合约定义，不能混作生成数量。

| 范围                         | 结论                                                            |
| ---------------------------- | --------------------------------------------------------------- |
| 31 名正式干员的静态创建引用  | 47 个不同公共 ID，未发现缺定义                                  |
| 从实际源数据重新规划公共闭包 | 30 名通过，公共定义与正式目录无结构差异；Typhoeus 阻塞          |
| 五个缺失的默认子 Buff        | 当前调用均覆盖 child_buff_id 为已有定义，不是已证实的当前轴漏伤 |
| 四种元素附着                 | 已有旧 1.4.4 生成语义，不是完全未实现                           |
| 四种同元素爆发               | 公共编译支持已有，正式运行仍用兼容定义，尚未全量发布            |
| 十二种异元素组合             | 工厂查表已有生成依据，最终导电/冻结/燃烧/腐蚀状态仍含手工构造   |
| 失衡承伤 Buff                | 原生证据已闭合、根已登记，生成发布与运行时生命周期尚未完成      |

默认子项为 enhance_fire、vulnerable_fire、enhance_crystal、vulnerable_natural、speedup 的 default_child。
不据此保证自定义编辑器的默认调用可用。查询 ID、步骤 key 不是创建引用，不能按字符串搜索数量判漏项。
显式忽略的公共项是弧光战技 `buff_common_vfx_char_atk_up`，属于表现行为。

## 已证实但未实现：失衡承伤

唯一原生依据是 combat-spec 的 `docs/poise-break-buff.md`，不要复制成另一份原生规则文档。
固定 Buff 为 `buff_common_poise_break_damage_taken_scale`，源数据为无限寿命、
Defender/product 乘区、黑板 dmg_up=0.3。原生在 OnPoiseZero 事件前由 Modifier.source 施加，
保存实例句柄，ResetPoise 时按实例结束，不等待额外失衡标签窗口。
不能改 WeaknessDmgScalar，也不能硬乘最终伤害 1.3。

本轮只将该 ID 加入 systemBuffRoots，并验证独立公共编译与 10 项公共根测试。
完整 31 干员公共生成失败后没有覆盖正式目录，三轴基线不变。
阻塞不是文件未下载：`projectile_chr_0034_typhoea_archery_attack_05.json` 已存在，
但缺 entityBlackboard；当前融合来源及两份本地 VFS 副本均如此。
Typhoeus attack5 对应动作需要投射物黑板证据，不能以空黑板通过，也不能用子集覆盖公共目录。

## 下一步顺序

1. 查明并补齐 Typhoeus 投射物实体黑板来源，恢复全 31 名的公共闭包生成验证。
2. 在 combat-spec 和 Endaxis 按证据接入失衡 Buff 的创建/实例清理，复用公共 Buff 执行链，再重跑三轴。
3. 沿原生隐式路径收束四种爆发及十二种组合状态的正式转换，不再把“干员引用齐全”等同于“全部状态已转换”。
4. 继续逐事件归因赛希末次附着消耗及剩余真实伤害差额，以源数据和原生证据裁决新旧差异。

本轮 spec 的 Buff 生命周期、时间膨胀及关键词定向测试共 114 项通过；不将其表述为全仓测试全部通过。
