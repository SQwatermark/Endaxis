# 当前状态与路线图

> 状态日期：2026-08-17。短期精确状态以 `current-context.md` 为准，长期架构变化同步更新 `docs/next`。

> **最新基线提示**：当前工作已转到 `feature/next`，本文后续部分保留较完整的阶段背景，其中部分测试数字和旧 `main` 基线仅供历史参考。恢复开发时必须优先阅读 `current-context.md`，以实际 Git HEAD 和工作树为准。当前生成器已接入条件减速、根来源时间膨胀目标，并新增大潘作为第九名（不含首个佩丽卡样本）技能主体可完整转换的正式干员。

> **2026-08-24 公共数据编译器分支**：当前实际工作树为
> `C:\Users\sqwat\Projects\zmd\Endaxis-game-data-refactor`，分支 `refactor/common-game-data`。
> 新主线位于 `tools/game-data-compiler`，正在从干净主干以 TypeScript 重建干员、武器、装备共享的
> 来源 IR 和编译管线。下文旧 Python 生成器完成度只作迁移背景；最新数字、证据边界和下一任务见
> `current-context.md` 与 `tools/game-data-compiler/README.md`。

当前代码基线为 `b6a9b66f feat(next): add generated Da Pan operator`。本页第 1、2、4 节中的旧 `main` 提交表和历史测试数量不再作为恢复工作的依据，保留它们只是为了说明此前阶段的演进背景。

## 1. 当前 Git 基线

- 仓库：`C:\Users\sqwat\Projects\zmd\Endaxis`
- 分支：`main`
- 基线提交：`4e886dd6df3d8e1d499ab40682ff29c5b5259f29`
- 基线提交标题：`Refactor: 统一解析干员资源规则`
- 本交接文档正在 `docs/handoff` 中创建，尚未提交。
- 用户此前已经主动搁置另一批修改，因此不要把当前未出现的梨诺、新武器或脚本文件判断为意外丢失。

## 2. 近期关键提交

按由新到旧排列：

| 提交       | 意义                                                               |
| ---------- | ------------------------------------------------------------------ |
| `4e886dd6` | 资源上限和回复规则统一从已编译技能与同一构筑面板解析，减少重复事实 |
| `5a5057a6` | 光标辅助线恢复为鼠标悬停采样语义，与编辑落点光标分离               |
| `cffda4ac` | 26 名干员的终结技能量降费潜能批量生成                              |
| `da877f93` | 全局技力规则可编辑并进入新版场景                                   |
| `1d44aab4` | 干员养成效果可以补丁技能费用                                       |
| `ab440b63` | 每条轨道可配置初始终结技能量                                       |
| `97095f29` | 新版时间轴技能块连接                                               |
| `7062199d` | 潜能静态增伤进入面板/伤害编译                                      |
| `3e9b0411` | 标准玩家伤害场景贯通                                               |
| `ad02a771` | 新版静态面板计算贯通                                               |

## 3. 已经闭环或基本可用的能力

### 项目与编辑基础

- V2 项目 schema、创建、校验、序列化和迁移边界；
- 时间轴动作的稳定身份；
- 连续撤销/重做；
- 单选、多选、框选、批量删除和多动作移动；
- Ctrl 复制粘贴；
- 中键平移、滚轮缩放、吸附和轨道切换；
- 技能组放置，普攻链可展开；
- 时间轴伤害 hit 连接。

### 构筑

- 干员选择与基本养成输入；
- 武器和装备目录/适配的基础入口；
- 阵列属性、等级、突破和潜能静态属性；
- 静态面板和属性详情；
- 潜能静态伤害增益；
- 技能费用补丁；
- 敌人运行时输入和标准伤害环境。

### 模拟主干

- `ScenarioDocument -> compile -> runtime -> damage -> receipt` 的最小端到端链；
- 独立暴击随机源；
- 技力和终结技能量基础规则；
- 资源、Buff、状态、伤害和附着的核心类型与部分运行时；
- 从 receipt 生成资源/状态变化点和技能诊断的投影模块。

### UI

- `/next/timeline` 并行页面；
- 旧版布局风格的工作台、轨道头、标尺、技能库、技能块和工具栏；
- 干员、武器、装备、敌人等选择/编辑弹窗的基础版本；
- 主题注册和局部语义 token；
- 光标辅助线按真实鼠标位置采样，准备区钳制第 0 帧，标尺区隐藏。

## 4. 当前测试基线

最近一次完整验证：

- `npm.cmd run type-check:next`：通过；
- `npm.cmd exec vitest run src/next`：136 个测试文件、692 项测试通过；
- 生成器 Python 测试：202 项通过；
- 浏览器验证入口：`http://127.0.0.1:5173/next/timeline`。

测试数量只代表当前代码通过既有断言，不等于所有游戏机制已被证明。新增原生规则时仍需先查证据边界。

## 5. 当前 UI 差距

新版页面已经可操作，但离旧版 1:1 仍有差距：

- 阶段 A（模拟进 UI）已闭环：编辑自动重模拟、资源/敌人生命/失衡曲线、光标辅助线采样、技能块警告同源；
- 时间轴命中点已对齐旧版：底部红色菱形、hover 金色、条件命中未触发不显示、点击打开伤害详情弹窗；连线工具可拖到命中端点；
- 反应状态（感电/腐蚀等级与时长）已接入标准环境并进入命中点提示；反应伤害效果待数据闭环；
- 战斗日志、伤害详情、合法性诊断和分析面板尚未完整接入；
- 干员、武器、装备和敌人的完整编辑流程仍需逐项对齐旧版；
- 技能库图标、类型强调、轨道选中样式、头像点击范围等曾逐步修正，仍需整页回归；
- 顶部菜单、轨道上方按钮区、属性详情和筛选体验需继续对齐；
- 快捷键作用域已有 router，但仍可能存在临时 DOM 判断，应最终整理成显式作用域栈；
- 失衡曲线已从标准环境的连续真实账本投影；节点阈值已保留，节点事件和节点 Buff 尚未执行。

## 6. AKEDB 最新数据状态

2026-08-10 检查的 CDN 版本为 `1.4.4@9163343-11`。

### 梨诺

- CharacterTable：有；
- SkillPatch：12 个 `chr_0035_liino*` 条目；
- 天赋、潜能、文本：有；
- BuffData：72 项；
- 根 SkillData：0 项。

因此可确认基础成长、展示、逐级参数、潜能天赋和大量 Buff，但不能仅靠 AKEDB 生成完整帧序列和技能动作图。

### 曜夜

- ID：`wpn_lance_0014`；
- WeaponBasic/Item：有；
- SkillPatch：`wpn_attr_will_high`、`wpn_sp_attr_heal_high`、`sk_wpn_lance_0014`；
- 技能参数：主属性 `0.16 -> 0.448`，攻击增益 `0.035 -> 0.098`，持续 20 秒，最多 4 层；
- BuffData：2 项；
- 根 SkillData：0 项。

用户搁置的旧版更新应在需要恢复时先检查 shelf/stash，而不是重新抓取并覆盖。

## 7. 干员生成器状态

生成器目录：`scripts/generate_next_operators`。

已经具备：

- 结构化源模型和 schema；
- 根 Action 与递归 Skill/Buff 审计；
- 条件、目标和 Action payload 的模块化解析；
- 干员技能、天赋和潜能合并生成；
- 具名技能对象与可读 DSL 渲染；
- 等级值、百分比和 definition helper；
- 诀双形态归并；
- 佩丽卡、庄方宜等样本生成和对照；
- 全干员生成可行性、递归机制和养成覆盖报告；
- 终结技降费潜能的严格批量转换。

当前不足：

- 并非 26/27 名干员都能生成完整可执行 DSL；
- Aura、实体数量条件、条件投射物、部分嵌套引用和复杂 Buff 事件仍是共通阻塞；
- 宽松模式可以展示基本信息，但必须暴露不完整状态；
- 武器、装备和套装 DSL/生成器尚未达到干员同等成熟度；
- 原始数据缺 SkillData 时只能生成已证实部分。

## 8. C# Combat Spec 状态

Combat Spec 已建立相当广的模块和测试，包括：

- 固定 30 Hz 时钟和帧内 sequence；
- 技能可用性、冷却预占、延迟成本和技能结束；
- Timeline/Sequence/Conditional/Selector 等 Action；
- Blackboard、SkillPatch、天赋潜能和武器 CardSkill；
- Buff 生命周期、叠层、事件、周期和属性修改；
- 玩家主动伤害、抗性、防御、暴击、失衡和附着；
- 技力、终结技能量和回复限制；
- 普攻输入、连携和部分干员特殊状态；
- TableCfg/SkillData/BuffData 严格适配；
- 从完整构筑计算客户端 Deck 面板。

仍未达到“任意干员、任意构筑、任意操作序列完整模拟一场战斗”的最终状态。主要缺口是：

- 若干运行时条件/Action/处理器尚未证实或实现；
- 生产释放路径和延迟扣费失败仍有关键未知；
- 部分随机、护盾、格挡、转移和敌人受击系统；
- 新版本 IFix 与缺失 SkillData；
- 全干员配置适配覆盖。

## 9. 主线优先级

用户已明确新版功能优先级：

1. 干员、武器、装备、敌人实例的选择和编辑；
2. 时间轴编辑和调整；
3. 完整时间线模拟、事件记录、资源监控和战斗日志；
4. 投影和显示；
5. 技能实例编辑暂缓。

在当前阶段，近期最高价值工作是把已经存在的模拟流水线接进页面，让资源曲线、辅助线和诊断共用一个事实来源。

## 10. 建议的下一执行序列

### 阶段 A：模拟结果进入 UI

1. 应用层提供可取消/防竞态的异步模拟入口 —— **已闭环**：`ScenarioSimulationService`（按场景 revision 缓存、AbortSignal、冻结结果）；
2. 页面编辑后触发重新编译与模拟 —— **已闭环**：`useScenarioSimulation`（150ms 防抖、runId 防竞态、严格失败丢弃旧结果）；
3. 将技力、失衡、各轨道终结技能量和敌人生命投影为曲线 —— **已闭环**：`enemyHealthChangePoints/Curves`、`poiseCurves`、`curveSampling`，全部来自同一份回执；
4. 光标辅助线只采样这些曲线 —— **已闭环**：辅助线显示时间 + SP + 敌人生命 + 各干员终结技能量（失衡在标准环境为 0）；
5. 合法性警告从同一 receipt/diagnostic reducer 读取 —— **已闭环**：技能块红标 + tooltip 原因。

验收：黄线、警告、hover 数值和战斗日志对同一时刻给出一致结果。战斗日志展示仍属后续工作。

### 阶段 B：构筑编辑闭环

1. 补齐武器/装备筛选与选择；
2. 补齐词条等级、装备精锻和套装展示；
3. 确保每次编辑只改变存档，由编译器重算面板；
4. 用已确认 PanelSnapshot 回归；
5. 在 UI 明确标记宽松生成或机制缺失的干员。

### 阶段 C：战斗机制覆盖

1. 动态 Buff 和事件处理器；
2. 元素附着与反应（附着已接入标准环境；`applyElementalReaction` 仍待接入）；
3. 失衡、重击、处决（连续失衡执行/恢复已接入标准环境；处决和节点效果仍待接入）；
4. 连携窗口队列；
5. 强化/普通技能互斥与形态状态机；
6. 武器、装备、套装的持续效果；
7. 活动 mechanic。

### 阶段 D：持久化与切换

1. 完成 V2 导入导出、自动保存和分享；
2. 用真实旧项目 fixture 完成迁移；
3. 对比旧版布局、交互和模拟；
4. 建立性能基线；
5. 达到验收条件后切换正式入口。

## 11. 不应立即做的事

- 不要继续在旧版模拟器上做结构性重构；
- 不要先做复杂自动排轴；
- 不要为单敌人模型实现完整敌人 AI 和多目标几何；
- 不要在没有真实曲线前伪造辅助线资源值；
- 不要为性能提前引入复杂架构；
- 不要把缺少根 SkillData 的新干员假装生成完整；
- 不要把用户搁置的修改当成待重新实现需求。
