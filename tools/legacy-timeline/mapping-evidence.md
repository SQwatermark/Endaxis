# 三个本地旧轴：映射依据与运行检查

2026-09-08；来源为用户提供的 Endaxis_Timeline_2026-08-31.json。
配置保存对象身份及已核实的单动作变体，不提交完整用户存档及派生数据。

## 2026-09-08：27项中断诊断的九类归并

以每条SkillInputCannotInterruptCurrentSkill的currentSkillTimelineFrame为准，
不是两个输入时刻相减。现有原生门禁见combat-spec/docs/skill-interruption.md：
AllowedNextSkill窗口、优先级、严格passedTime > exclusiveFrame/30 + 1e-5。

| 当前→下个技能 | 次数 | 输入时局部帧 | 相关门槛/因素                                                   |
| ------------- | ---: | ------------ | --------------------------------------------------------------- |
| 弭弗A1→A2     |    1 | 7            | 允许接续17起；实际间隔仅8                                       |
| 洛茜A4→A5     |    1 | 31           | 首允许窗口36起；实际间隔32                                      |
| 诀A2→A3       |    8 | 12或13       | 允许窗口14起；实际间隔13或14                                    |
| 诀A3→A4       |    8 | 21           | 允许窗口22起；实际间隔22                                        |
| 诀A4→A5       |    2 | 1            | 允许窗口18起；队友汤汤连携膨胀使21全局帧远不足                  |
| 诀A5→A1       |    4 | 32或38       | 允许窗口40起；命中停帧/其他膨胀，42全局帧不能直接当局部42       |
| 诀终结技→A1   |    1 | 72           | exclusiveFrame=72，严格大于才能中断，无该允许窗口               |
| 诀连携→处决   |    1 | 16           | exclusiveFrame=23；期间汤汤连携替换全局膨胀，随后命中停帧       |
| 诀处决→秘仪   |    1 | 38           | exclusiveFrame=51；2146帧处决停帧，2160帧输入；允许窗口没有秘仪 |

合计27。新增8类正式短/长间隔对照，既有realAxisInterruptionRegression.test.ts覆盖
A4→A5队友膨胀以及A3→A4紧邻边界。秘仪用真实终结技先激活替换，不用基础终结技
高优先级替代检查；不手造inputWindows或直接设置替换Buff。
长间隔只作为隔离实验，原存档不被改动。固定步长输入先于本帧推进的边界仍是明确模型
约定，不能将这张表写成对原生渲染帧调度的完整证明，也不为清告警统一放宽一帧。
相关153项测试及应用vue-tsc通过，本轮无生产行为修改。

下一批公开候选已从官网只读接口取得，记录见current-context顶部；仅下载了5个响应，
尚未解码和运行，不能计入旧版无错误样本或新版模拟覆盖。此前三轴的已确认缺陷已经修复，
已解释差异与未闭环的细粒度原生帧序边界继续保留，转向不同干员的横向检验。

## 2026-09-08：卡蜜拉窗口缺口为公共提前消费事件遗漏（已修）

原始SkillData/chr_0028_wulfa_combo_3_skill.json，SHA256
`4D983C23E3388F6A4004134A55E0AB196B94FE71004F01EF7808D3AF8716A690`：
FinishBuffAdvanced为Tag HasAny(-1558844517)、finishAll=true、isFinishedEarly=true、
isAbsorbed=false、finishSource=Owner。生成FireInflict、Early均正确，无需改数据。
已有combat-spec/finish-buff-advanced.md专门包含洛茜这一真实样本，证明Early全量
Id/Tag复用消费入口；consume-buff-single.md证明结束完成后向finishSource发208消费事件。
这是既有规格在Endaxis中的遗漏，不需要另编卡蜜拉特例或重复研究机制。

两处缺陷共同造成窗口遗漏：

1. CombatBuffContainer.finishByIds/finishByTags没有向ConsumedObserver发布Early结束。
2. BuffOperationExecutor只读上下文actionSourceId/buffSourceId，主动技能没有这两个字段时
   未使用执行器已有的施法来源，使公共事件没有消费方身份。

修复限定于全量结束事件及已知来源交接；显式Buff/实体来源保持优先，Absorbed独立发布，
Other/无来源/重复结束不伪造消费。本次不声称新增消费保护、限层消费或原生Tag快照能力。
正式双人技能回归先复现无窗口，再验证火附着结束同帧开卡蜜拉窗口及后续合法消费。
容器Id/Tag回归校验事件原层数和发布时Buff已结束，避免仅使图上告警消失。

三轴两个截止范围重算：配置期望仍693530.8131581588 /337938.8820469209 /
1969138.5859327097，命中210/194/264；方案4窗口告警2→1，其余告警数不变。
私有报告tmp/early-consume-axis-audit.json。没有通过修改输入、生成配置或旧映射消除告警。

全量回归发现并去除重复生产者：applyPhysicalInfliction原有层数差推算onBuffConsumed，
公共Early事件恢复后会让大潘天赋重复消费同一层（旧断言1.06倍变成1.12倍）。
删除该推算及依赖注入回调，保留真实容器事件。大潘真实技能测试保持原期望通过，
不是修改断言接受重复增伤。最终三轴报告early-consume-final-axis-audit.json与上表一致。
最终全src 5396/5396通过，应用vue-tsc与diff检查通过；两种截止范围的伤害/命中均逐值不变。

## 2026-09-08：三项资源与七项窗口诊断的当前归因

资源表按输入前最后一条SpChanged和同castId的SkillCostApplied核对，费用取正式定义：

| 方案/干员/帧   |          支付前SP | 费用 |            支付后SP |
| -------------- | ----------------: | ---: | ------------------: |
| 方案4 弭弗493  | 21.13333333333471 |   50 |  -28.86666666666529 |
| 方案6 赛希818  |   91.133333333334 |  100 |  -8.866666666666006 |
| 方案6 赛希1602 |  85.2000000000001 |  100 | -14.799999999999898 |

这证明即时不足判定与实际支付一致，不代表原轴整条SP收支的每笔差异均已审完。
赛希nonReturnedSpCost=60不是技能费用变成60；combatResources.pay实际扣100，
其中40来自此前返还SP池，非返还部分才参与后续能量结算。
realAxisAvailabilityBoundaries.test.ts 的5项资源用例通过正式干员定义校验上述不足值
和恰好50/100；关闭自动恢复仅为了固定测试边界，不修改技能费用或输入检查。

窗口诊断（括号内为仍需保留的证据边界）：

| 位置                       | 已读取的事实                                                    | 结论                                             |
| -------------------------- | --------------------------------------------------------------- | ------------------------------------------------ |
| 方案4 卡蜜拉186            | 没有打开过窗口；13帧火能量碎片153帧early结束                    | 尚待核对FireInflict吸收/消费事件，不能视为已闭合 |
| 方案4 弭弗205              | 54帧破防1层，153帧2层；原生addedBuff要求NoGuard且破防>=3        | 本次输入前未满足层数条件                         |
| 方案6 汤汤382/1116         | 寒冷附着367/1100；冷却就绪370/1103                              | 触发早于冷却结束，不能自动补发窗口               |
| 方案6 佩丽卡2005、赛希2033 | 窗口1843打开，1994过期                                          | 输入时已过期；不按旧统一膨胀延长窗口             |
| 方案6 诀2063               | 2057打开汤汤候选；失败回执expectedOperatorId=汤汤；2071汤汤消费 | 当前可释放顺序不匹配，仍执行指定诀连携           |

汤汤原生条件为takeDamage中的法术爆发标签或beforeTakeInfliction中的cryo。
新增2项正式对照：实际赛希连携在冷却前3帧/后1帧施加寒冷，只有后者开窗；
另2项由同样真实附着开窗，经过150帧仍可消费、151帧到期告警，均不阻止放置的技能。
测试在双人场景定位实际冷却时刻，不能沿用单人时间推断队友施法后的时钟。
本节未重新证明所有原生时钟内部顺序；只补足本批次事件与回执的可复现对照。

审计CLI现输出两种截止范围各自的正式三类诊断及引用回执，不另写一套原因映射。
三轴报告tmp/availability-official-audit.json总数/命中数不变；38项相关测试通过。

卡蜜拉待查的精确入口：火附着正式定义applyTags确为FireInflict；153帧回执432~434为
洛茜comboSkill3的readBuffStackCount→finishBuffsByTag→BuffFinished(reason=early)。
生成对应动作也显式early；卡蜜拉监听的是buffAbsorbed/buffConsumed，不能把任意结束
当作消费。需对原始chr_0028_wulfa_combo_3_skill.json和结束事件发布链继续核实。

## 2026-09-08：弭弗连携后第一段战技身份差异

方案4：连携205帧，第一段战技406帧。映射配置将旧 segmentIndex=1 保留为
battleSkill1，不能因运行时槽位改变而把映射改成第二段。

原始资源（本地忽略目录 tmp/game-data-sources-hybrid-20260905）：

- SkillData/chr_0031_mifu_combo_skill.json：27帧 CreateBuffAction 向 Owner 创建
  buff_chr_0031_mifu_normalskill_2；SHA256
  `943D8C091FE5660B54112949A8CABB8782D3813DAAD6E85DE05936E39E015B90`。
- BuffData/buff_chr_0031_mifu_normalskill_2.json：Limited，duration=15，
  useTimeDilationDt=false；DuringBuffEnable 的 ChangeSkillAction 改到第二段，
  specificRevertedSkillId=true，恢复 chr_0031_mifu_normalskill_1，不继承原冷却进度；SHA256
  `731EF27E8CBAAAEE86E4BD3EA7B35C57A74B901DC1CE52118387865B291739EE`。
- 生成 mifu.operator.generated.ts 的 comboSkill scheduledSequences 与
  skillSlotReplacement 已表达这些数据，没有本轮新造规则或修改生成产物。

正式回归 mifuComboSkillRoute.test.ts 通过真实连携产生替换：200帧第一段得到
skillInputMismatch / actualSkillId=battleSkill2；同刻第二段没有身份告警；900帧第一段
没有身份告警，并验证同一替换Buff以lifetime结束。三例均 accepted=true、实际有指定
施法的伤害，场景文档不变。这里只断言身份告警，未把连携窗口等其他告警隐藏掉。
结论：这是原存档操作与当前原生路由的差异，不是导入时丢失段数，也不应自动替换。

另重新读取全部中断回执 currentSkillTimelineFrame：诀 A3→A4 在
49/160/271/782/893/1004/1615/1800帧均读到局部21帧，生成 allowedNextSkills 从22帧开始。
既有 realAxisInterruptionRegression.test.ts 对照已覆盖此边界及队友膨胀下A4提前截断。
这是当前调度规则的可复现解释，不等于原生同帧输入/推进顺序已取得完整证据。
仍须核对其余中断、3项资源及7项窗口告警，不能因这一条身份差异闭合就宣布三轴全部无问题。

## 2026-09-08：汤汤多水体矩阵、诀八次追击分账

快照回执修复后重算同一第三轴：汤汤三次战技分别17/41/41次，倍率和为480/1080/1080%，
不再出现误导性的1147.8897/1093.5779%。后两次各三组水体12×25%+直伤5×36%。
新增正式行为矩阵（拓展 tangtangWaterDamage.test.ts）：预先通过0/1/2次真实连携积累水体，
随后战技分别生成1/2/3组，每组独立12击，总480/780/1080%；全部3例通过。
生成定义 sharedActionSequence7/8 与原生分支对应 water_cnt=0、1、>=2；没有手填黑板。
这验证的是木桩模型下全部水体命中同一个敌人的结构，不宣称游戏实战空间覆盖也必定全中。

诀八次36%派生追击，新旧数量一致。实际比较按两组四次进行，不使用旧显示时间作为
同帧结算依据：旧回执四次都显示66.4秒、另一组都显示76.483秒，但后一组增幅已不同。

| 追击                           | 旧期望 |         新期望 |                        已确认因素换算后 |
| ------------------------------ | -----: | -------------: | --------------------------------------: |
| 第一组三次（1855/1859/1863帧） | 各6914 |  各6914.879454 |                                取整差异 |
| 第一组末次（1912帧）           |   6914 |    6286.254049 |                去掉1.1脆弱后6285.454545 |
| 第二组首次（2129帧）           |  11368 |   10795.860878 | 扣到期动火用/落草、补失衡后10795.345257 |
| 第二组其余（2133/2137/2141帧） | 各9928 | 各10795.860878 |   再将增幅1.65408→1.89408后10795.830201 |

第一组第四击时，脆弱1877帧已到期。第二组全在失衡Buff1726→2221帧范围内，
动火用/落草2026/2027帧已到期。天赋增幅真实实例105为2117→2160帧，包含第二组全部四击；
原生 ultimate_skill_listener_owner 从 talent1.enhance_rate 创建子Buff，当前等级值0.24。
旧 arcane.ts 则按 arcane-gloompurger-array 状态门控 ampBonus，旧回执第二组只有首击含0.24。
这里证实新旧参与乘区不同；尚不据旧合并显示时间推断其内部回调具体先后，更不能强行
把新后三击削去0.24。上述换算残差均小于1，与旧整数输出量级一致。

没有发现新的水体重复伤害或追击漏失。剩余审计仍需避免将“结构/几组乘区通过”表述为
三份完整存档每项数值均已逐笔验收。官网不同干员轴为后续覆盖方向，当前没有下载新轴。

重新通过正式服务导出完整时长诊断（tmp/input-status-current.json）：方案4可用性3/窗口2，
副本1/0，方案6为27/5，三者 executionDiagnostics 均为空。102个输入全部执行是编辑器
自由编辑规则，不等于校验全部通过。尚需优先检查方案4弭弗406帧 battleSkill1 的
skillInputMismatch（actualSkillId=battleSkill2）；文末556帧为原点修正前历史坐标。
本轮多水体3例、应用vue-tsc、格式检查通过；未修改生产逻辑，不重复跑全量宣称新数字。

## 2026-09-08：汤汤水体数量与攻击快照回执（修复详情，不改变伤害）

当前三个轴仍未整体验收。完成本批遗留项后才进入官网分享页的新干员样本，不把回归全绿
等价为新旧轴逐项无问题。

第一段战技：旧日志 16 次有效伤害=5×36% 直伤+11次水体均分300%（每次27.272727%）；
新版17次=5×36%+12×25%，总倍率都480%。原始
chr_0027_tangtang_normal_skill_water_projhit.json 的持续检测 triggerInterval=0.26，
Hp DamageUnit.takeAtkSnapshot=true；生成子技能的有效时段覆盖12次，不能改成旧0.3秒/11次。
新增正式单水体回归验证12次快照、5次直伤及固定25%/36%，均为寒冷伤害。

后两次战技当前各41次=三水体各12次+5次直伤。原始 _water_projhit_2 同为0.26秒，
同样 takeAtkSnapshot=true。检查时发现回执将部分25%写成27.262991%，并非原生动态倍率：
原 baseValue 使用冻结攻击，但输出 skillMultiplierPercent 时除以实时攻击。
例如冻结5855与实时5369，`25×5855/5369=27.262991`，会误导技能倍率汇总与命中详情。

已修复：动作上下文快照由单数值扩充为 attack/attackScale/baseValue，仍保存原本的基础
计算值，真正伤害路径不变；回执使用冻结攻击与冻结倍率，另保留 currentAttack 和
usesAttackSnapshot。非快照路径不改变。既有执行器回归在冻结100后将实时攻击改999，
断言扣血仍100，回执仍攻击100/倍率100%，而不是攻击999/倍率约10%。
不向游戏公共定义增加展示字段，不从旧轴的伤害数值倒推倍率。

正式 CLI 重算三轴仍 693530.8131581588 / 337938.8820469209 / 1969138.5859327097，
第三轴完整时长1996820.2804914764；命中数仍210/194/264（完整265）。
输出 tmp/attack-snapshot-receipt-audit.json，私有输入不变。
本次仅关闭快照详情缺陷和单水体数量依据；三水体伤害总量、各实例快照时机与诀追击
剩余差异还要继续核对，不因数量组合能对上就宣称全部行为正确。

## 2026-09-08：汤汤末次连携伤害差额闭合，清波独立时钟回归

上一节待查的旧 74.90 秒 24130 与新 2097 帧 46245.491513，已拆到同一次连携直接命中。
两边攻击 6720、倍率 240%、防御 0.5、抗性 1.208、暴击期望 1.025、失衡 1.3 相同。
原生伤害类型 Cryst，而旧回执 physical，导致以下差异：

| 项目             |   旧物理命中 |     新寒冷命中 | 来源与边界                                     |
| ---------------- | -----------: | -------------: | ---------------------------------------------- |
| 落草静态寒冷增伤 |            0 |          0.288 | wpn_pistol_0011 技能 3 等级 5 的静态 cryo 词条 |
| 清波技能增伤     |          0.4 |            0.2 | 旧版保留两层，新版前一层已经到期               |
| 清波手甲 / 长息  | 0.299 / 0.16 |           相同 | 普通区都保留                                   |
| 赛希寒冷增幅     |            0 | 0.454079985619 | 物理不消费寒冷增幅                             |
| 导电法术承伤     |            0 | 0.258434116840 | 物理不消费法术承伤                             |

所以攻击普通区为旧 1.859、新 1.947，而非仅增加寒冷增伤：
`24130 × (1.947 / 1.859) × 1.454079985619 × 1.258434116840 = 46244.774293`。
新版 46245.491513，残差约 0.717，符合旧版先取整非暴击/暴击伤害再计算期望的误差量级。
本次实际七区间修正逐项记录的乘积为 4.631568422417232；没有另补法术爆发或暴击。

清波旧正式 operatorLog：53.667 秒触发的独立层 expiresAt=75.583，基础时长 15 秒实际
延长为 21.916 秒；74.033 秒再次触发后，74.90 秒命中仍有两层。
新版同一前次施放的实例 28 为 1460→1909 帧，末次实例 49 为 2071 帧施加，2097 帧
只有后者。原始 buff_equipsuit_combo_cd01_spellup.useTimeDilationDt=false，旧 qingbo.ts
虽也是独立层和 15 秒，但交给旧 effectDispatch 统一延寿。保留新版，不将前一层续到末击。

新增 tangtangComboAndQingbo.test.ts：正式汤汤三次连携、清波三件套与佩丽卡终结技，
验证三次 240% 直接命中均为 cryo，清波三次施加各自独立 449 帧差到期，时间膨胀实际发生，
前两层先重叠、随后全部到期再施放第三次；不手工注入 Buff，也不从旧期望数生成模拟输入。
原生连携源 SHA256：AC7FD43C346B3D2496493757727B09B7D1C4264FC2936A0F8A0E07ABEC3BB413。

诊断在忽略目录 tmp/compare-axis-damage.mjs 的 Vite 内存变换中只观察属性快照与累加器
modify 调用，不修改磁盘生产代码、输入或公式；tmp/tangtang-damage-trace.json 的该命中
期望与未插桩回执一致。旧 operatorLog 仍由只读工作树正式 store 生成。
本轮无生产模拟或生成定义变化；只闭合这次连携，不能外推到汤汤所有水体、战技与终结技。

## 2026-09-08：诀末段连携、处决与秘仪差异闭合

名称纠正：当前中文资源 arcane.name 为“诀”（chr_0032_lizhiyan），arclight.name 才是“弧光”。
此前口头接续及当前快照误把 arcane 写成弧光，不涉及转换身份或生产定义的修改。

上轮攻击槽位修复后，这三组伤害的攻击均为 7839，与旧回执相同；下文曾记录的 7893
已被修正，不再是待研究的小幅面板差异。用同一实际旧回执与当前新回执比较：

| 命中                          | 旧期望 |        新期望 | 从旧值移除/补入已确认差异后的期望 |
| ----------------------------- | -----: | ------------: | --------------------------------: |
| 连携 80%，新 2103 帧          |  32842 |  23990.801951 |                      23990.467771 |
| 处决，新 2117~2146 帧六击合计 | 193624 | 175514.366989 |                     175513.911959 |
| 秘仪 1152%，新 2218 帧        | 363793 | 345467.548093 |                     345467.191843 |

三组共同移除旧版延寿保留的动火用自然增伤 0.5、落草法术承伤 0.108。
秘仪和连携攻击普通乘区 2.4174666667→1.9174666667；处决因不使用终结技词条，
为 2.0586666667→1.5586666667。防御侧普通乘区均 1.3664341042→1.2584341168。
连携旧/新均有失衡承伤 1.3，处决和秘仪旧回执 staggerMult=1，新版为 1.3。
后者由此前已接入的原生失衡 Buff 生命周期解释：实际实例 89 存活 1726→2221 帧，
包含上述全部命中，不在处决或另一个技能输入时提前结束。

例如秘仪：`363793 × (1.9174666667/2.4174666667) × (1.2584341168/1.3664341042) × 1.3`。
处决使用对应的攻击普通乘区比例；连携不再额外乘 1.3。余差均小于 0.5，为旧整数输出。
动火用当前实例 78 在 2026 帧结束，落草敌方实例 78 在 2027 帧结束，均先于末段命中。
实例 ID 只在对应容器内有效，这两个相同编号不是同一个实例。

处决数量差不代表重复伤害。旧版把 720% 计作一击；原始
SkillData/chr_0032_lizhiyan_power_attack.json 使用 BreakingAttackCalculation，
三个独立 0.1、一个 0.5，加上持续检测的 0.1（maxCountPerTarget=2，间隔 0.067 秒），
合计五个 0.1 + 一个 0.5。生成定义按此保留；当前回执五个 17551.436547、一个 87757.184254。
源 SHA256：454AEC9DFA596E1EF05035B76A273CCB4384765105E6C6F4700A089CF0B92EE3。
新增正式入口 arcaneFinisherHits.test.ts 验证六次命中、同一施法、持续检测两次、最后一击比例，
不为贴近旧版合并命中或修改原生序列。本轮只添加回归和证据，不修改生产模拟逻辑。

仍未完成整轴逐击归因：诀的其余普攻/派生追击及汤汤等继续核对，尤其不能因这三组
闭合就把所有追击增幅、反应时序、旧日志合并规则视为已解释。

下一项汤汤线索：旧 74.90 秒的 240% 连携期望 24130，旧回执 element=physical；
新 2097 帧同倍率、同攻击 6720 的直接命中为 cryo，期望 46245.491513。
旧 tangtang.ts 的 comboSkill.damageGroups 未填写元素；实际旧回执已证明确实按物理结算。
同批原始 chr_0027_tangtang_combo_skill.json 的 Hp DamageUnit 明确 damageType=Cryst、
atkScale=atk_scale；当前生成定义对应步骤是 cryo，保留新行为。
新步骤 chr_0027_tangtang_combo_skill:/scheduledSequences/1/sequence/steps/0/whenTrue/steps/1，
这是连携直接命中，不是 240% 法术爆发。元素差异会影响增伤、承伤和增幅，但完整乘区
归因尚未完成，不仅凭元素不同就宣布整个差额闭合。

## 2026-09-08：佩丽卡四次命中差异全部归因，补充原生效果时钟回归

在上一轮攻击槽位修复之后，后两击的攻击、倍率、防御、抗性、暴击、失衡和攻击侧增伤
均能对应。剩余差异来自两个旧版被冻屏延寿的状态，不是新版漏算效果：

- 第 1991 帧终结技：汤汤法术脆弱实例 80 已于 1877 帧结束，落草实例 78 尚未结束。
  新伤害乘区 `1.46 × 1.108 × 1.3 = 2.102984`，旧版额外乘脆弱 1.1。
  `79460 / 1.1 = 72236.3636`，新版为 72236.4066，残差 0.043。
- 第 2029 帧连携：落草实例 78 也已于 2027 帧结束；导电仍存在。
  新伤害乘区 `1.66 × 1.2584341168 × 1.3 ≈ 2.7157008241`。
  `19927 / 1.1 × (1.2584341168 / 1.3664341042457715) = 16683.6483`，
  新版为 16684.1168，残差 0.469，仍在旧整数输出范围内。

具体回执：脆弱 `buff_common_affixes_vulnerable_spell_default_child` 为 1428→1877，
落草 `buff_wpn_pistol_0011_valid2` 为 1428→2027，结束原因均为 lifetime。
这对应源时长 15/20 秒（创建当帧计入 tick，帧号差为 449/599）。两份原始 Buff JSON
均 useTimeDilationDt=false、onlyUseSelfTimeDilation=false；旧时间规则及实际到期回执见下文。
前两击取整差异已在上一轮闭合，因此佩丽卡四击均已解释；**不是整轴差异全部解决**。

新增 tangtangDebuffClock.test.ts 通过正式入口执行汤汤连携生成水体、战技触发效果、
佩丽卡终结技产生真实 TimeDilationStarted。检查脆弱/落草各只有一个施加实例、同实例
以 lifetime 结束，帧号差严格为 449/599；不手工注入 Buff，不用旧版结果强制覆盖时钟。

审计 CLI 现在同时输出每个截止范围的 sources 分账。使用完整原始 sourceId，保留零伤害、
无 castId、无 sourceId（null）和能力实体来源；不按 ID 后缀强行归并，不把旧日志中的
零伤害占位与新版伤害回执数量差异直接解释成漏 hit。真实输出位于 tmp/debuff-clock-source-audit.json，
三轴总伤害与上一轮相同。本轮没有修改生产模拟逻辑或生成数据。

## 2026-09-08：佩丽卡对照暴露静态攻击槽位烘焙缺陷（已修）

方案 6 同范围逐干员拆账后发现佩丽卡四次命中都存在，前两次伤害各乘区基本一致但攻击不同。
新旧构筑四维均为 91/93/624/297、干员基础攻击 303、武器 411，没有版本属性变动。
静态武器加成 0.312，动态骑士精神 0.126，潜能 3 连携增益 0.2。
旧连携攻击 `floor(714 × (1 + .312 + .126 + .2) × 4.714) = 5513`；
修复前新版 `floor(714 × 1.312 × (1 + .126 + .2) × 4.714) = 5855`。
终结技没有潜能连携增益，同理错误的 4972 应为 4840。

证据：combat-spec/docs/attribute-modifiers.md 已由 Attributes 的运行时代码确认 BaseMultiplier
同槽加算；生成器 buildAttributeProjection.ts 显式把 Atk/baseMultiplier 映射为 attackPercent、
Atk/baseFinalAddition 映射为 attackFlat；当前佩丽卡 potential_3_atkup 定义也写 BaseMultiplier。
因此这是 Endaxis 构筑到运行时交接失真，不是需要重研究的新机制，不改 combat-spec 公式。
正式面板新增 attackBase，保留 rawValue、baseMultiplier、baseFinalAddition，在同一属性容器中
与 Buff 聚合；展示明细保持独立。静态贡献来源仍由构筑 receipt 保存，本次在容器中恢复聚合
Deck 槽位，不宣称已逐项恢复所有静态来源掩码；四维等其他静态属性的同类交接另需核对。

| 佩丽卡命中（新版帧） | 旧整数期望 | 修复前新版 | 修复后新版（约） |
| -------------------- | ---------: | ---------: | ---------------: |
| 连携 415             |       8704 |    9244.06 |          8704.10 |
| 连携 1148            |      12558 |   13337.69 |         12558.61 |
| 终结技 1991          |      79460 |   74206.49 |         72236.41 |
| 连携 2029            |      19927 |   17719.12 |         16684.12 |

前两击仅余旧整数输出差，后两击仍需按状态时钟和乘区归因，不标记为已解释。
这一通用修复也减少其他干员的攻击，不能为了接近旧总数保留原错误：
三轴新总数 693530.8131581588 / 337938.8820469209 / 1969138.5859327097（第三轴 2231 帧）；
完整第三轴 1996820.2804914764，2237 帧尾伤 27681.694558766594。数量不变，不增删命中。
私有诊断 tmp/axis-damage-comparison-after.json、正式审计 tmp/attack-slot-real-axis-audit.json；
项目 SHA256 与上文一致。5375 项 src 回归通过，包含正式构筑+生成潜能+真实连携的回归，
以及静态/动态倍率同槽聚合、平加不受倍率重复影响、动态修正移除后还原的单测。

## 2026-09-08：重新运行旧版，闭合赛希第三次连携差异

只读工作树 `4dadc55f` 的正式 timelineStore 经 fetchGameData + importProject 读取原始存档，
重新得到方案 6 期望总伤害 2126525、结束线 79.36666666666666 秒（含 5 秒准备）。
对应新版配置截止 2231 帧，而不是完整 3600 帧；正确同范围新版总数为 2082008.8836663882。
隔离 Node 脚本只适配 FileReader、内存 localStorage 和 eager glob/CJS 打包，不改旧源码或存档；
输出在 tmp/audit-old-store.mjs、tmp/old-store.log，不能把该脚本当新生产模拟器。

**更正下节“旧默认 Infinity”的初步解释：本轴实际附着覆盖为 20 秒，差异是旧版统一延寿。**
旧 enemyLog：汤汤在 52.634 秒施加寒冷，effectiveDuration=20，expiresAt=81.133，实际 28.499 秒；
赛希 73.567 秒命中时仍有附着，74.567 秒输出寒冷爆发。新版相应附着 1428→2027 帧到期，
2057 帧只产生新附着。旧 EffectCryo 默认值不是本轴的实际时长，不能用默认 Infinity 替代回执。

| 赛希第三次直接命中的因素 |                旧版 |                 新版 | 核对结果                                                  |
| ------------------------ | ------------------: | -------------------: | --------------------------------------------------------- |
| 攻击 / 技能倍率          |         5014 / 450% |          5014 / 450% | 相同                                                      |
| 终结技增幅               |             1.45408 |              1.45408 | 相同                                                      |
| 防御 / 抗性 / 期望暴击   | 0.5 / 1.208 / 1.025 | 相同（浮点误差除外） | 相同                                                      |
| 失衡承伤乘区             |                 1.3 |                  1.3 | 本轮前已补齐                                              |
| 汤汤法术脆弱             |                 10% |                    0 | 新版 1428→1877 帧自然到期；旧 52.634→74.55 秒延寿         |
| 落草法术承伤             |               10.8% |                    0 | 新版 2027 帧到期；旧保留到 81.133 秒                      |
| 赛希天赋寒冷承伤         |                 10% |                    0 | 原生先检查已有附着/冻结，再施加本次附着；新版检查时已到期 |
| 直接命中期望             |               42593 |   33229.191666180865 | 上述三项解释差额                                          |

旧防御侧普通乘区 1.4664341042457716，扣除落草 0.108 与天赋 0.1 后为 1.2584341042457716；
再去掉 1.1 脆弱：`42593 × (1.2584341168 / 1.4664341042457716) / 1.1 = 33228.7096`，
与新值相差约 0.48（旧版整数输出），不需要额外伤害倍率补丁。

原生 `chr_0011_seraph_combo_skill_projhit` 同帧动作先 IfElse 检查天赋/目标标签，后 SpellInfliction，
再 DamageAction；生成定义 sharedActionSequence5 顺序一致。汤汤脆弱及落草 Buff 的源文件
useTimeDilationDt 均 false。新增 xaihiAttachmentPrerequisite.test.ts 通过三次真实连携覆盖
首次无前置、已有附着时天赋先于附着生效、附着到期后不触发天赋/爆发，不修改模拟规则。
这只闭合此命中和对应爆发的差异，并非整轴剩余 44516.1163 差额均已解释。

## 晚间续接：失衡承伤已接入，赛希末次附着到期

Endaxis `6a75cd89` 后：第三轴配置截止 2231 帧期望 2082008.8836663882，
完整 3600 帧 2112155.410831612；命中仍 264/265，前两轴不变。
下面旧基线只作历史记录，不能继续把 1906578.40 当当前值。

正式入口读取同一转换项目，事件链为：

|   帧 | 事实                                                        |
| ---: | ----------------------------------------------------------- |
| 1428 | 汤汤施加寒冷附着实例 75，previousElement=null               |
| 2027 | 实例 75 BuffFinished，reason=lifetime                       |
| 2057 | 赛希施加新寒冷附着实例 93，previousLayers=0，attachmentOnly |
| 2057 | 赛希直接命中期望 33229.191666180865，没有额外爆发           |

源 `BuffData/buff_common_energy_shard_attached_cryst.json` SHA256
`03B023473D2C3C424F2F7352BED155D2DE0DAC15E23788F8F0F44E8940941F01`，
duration 读取黑板 duration=20，useTimeDilationDt=false、onlyUseSelfTimeDilation=false。
旧只读工作树 `4dadc55f` 的 src/simulation/effects/types.ts::ElementCryo 则设为 Infinity，
同元素矩阵在已有寒冷附着时生成寒冷爆发。这解释了可见差异的方向；本次没有重新实跑旧轴，
旧轴是否有逐 hit 时长覆盖仍需核对，不能把来源默认值直接当成旧运行回执。
新版已经有明确的附着到期事实，不补发第 2057 帧爆发，也不延长 Buff 追平旧数。
临时事件审计脚本及回执 tmp/audit-xaihi-attachments.mjs、tmp/xaihi-attachments.json 不提交。

另修复洛茜天赋回归夹具：原始 SkillData SHA256
`AA6A4DDF0648B2ACBE08A6639AA082A8E86C6DDE9EBA2B353E926A88ABD5F2F4`，
timelineActions[25] 检查 NoGuard BuffCount>=1，不是 PoiseBroken。夹具通过第一轮真实战技
产生破防，第二轮才验收流血；没有修改游戏规则。全 src 5372 项通过。

## 2026-09-08 台式机接续：更正统计范围说明

当前代码 `428a26a2` 重新转换 Downloads 的同名旧存档，102 个输入、零转换问题。
经 `auditSimulation.ts` 复现，下面历史三轴基线使用完整战斗时长 3600 帧，**不是全部采用存档结束线**。
方案 6 存档结束线为 2231 帧，此范围内为 264 条 / 1876431.868474918；完整时长为
265 条 / 1906578.3956401418。差额恰好来自第 2237 帧诀连携能力实体结束子技能的
120% 命中（cast:45），期望 30146.527165223935。其余两轴相应数值均与白天一致。

本次未修改游戏规则、映射、生成定义或源存档。前一轮怀疑的机器间伤害缺失已排除。
下表旧版总数的截止口径仍需重新核对；不能直接把不同范围相减后的全部差额当成模拟缺陷。
后续报告同时列出配置结束帧与完整时长，尾部伤害单列。转换项目 SHA256：
`1fad653fba332f1212d256d445bfcf9983e368f92770a81f9b408736cb618437`。

## 2026-09-08 性能验收后恢复对照：当前基线与失衡承伤线索

使用当前正式编辑器模拟装配入口 `createEditorSimulationService`、项目定义库及
`converted-real-axes-20260908-arcana/project.json` 原输入重新计算，未移动技能或修改敌人：

| 方案          | 旧版期望伤害 | 当前期望伤害 | 当前减旧版 |
| ------------- | -----------: | -----------: | ---------: |
| 方案 4        |       653432 |    716503.81 |  +63071.81 |
| 方案 4 (副本) |       311399 |    337938.88 |  +26539.88 |
| 方案 6        |      2126525 |   1906578.40 | -219946.60 |

旧版口径仍是只读参照工作树正式 store 重算的 `hitData._expectedDamage`，新版为全部
`DamageApplied.expectedDamage`，不是血量扣减或随机暴击实伤。结果与 SourceFinder 修复后基线一致。
按 cast 汇总仅用于定位，不视为实际技能效果归属相同：诀两次战技的后续伤害仍归原连携，
四条没有 castId 的伤害也必须纳入总账，不能因缺少 castId 丢掉。

### 赛希连携的具体差额

- 前两次直接命中：旧 10609 / 15481，新 10609.211264 / 15481.639942，面板与倍率基本一致。
- 这两次爆发在新回执中用 sourceActionId 保留原 cast 身份，期望 3772.164005 / 5504.583091；
  不属于漏执行。旧版分别 6773 / 9716，另含下文已经记录的等级系数和不同增益时序，不能直接补差。
- 第三次直接命中：旧 42593，新 25560.916666。旧明细含失衡乘数 1.3；新同帧
  PoiseApplied 明确 hasPoiseBrokenTag=true、inPoiseRecovery=true，却未接入下述原生失衡承伤 Buff。
- 第三次额外爆发：旧有 20018，新无；新第 2057 帧附着回执为 previousElement=null、
  previousLayers=0、attachmentOnly。后续应对照此前附着/冻结的消耗与到期，不能直接补发爆发，
  也不能用敌人死亡解释它（附着仍实际执行，死亡对全轴的早期隔离检查见交接历史）。

### 原生失衡承伤 Buff：内容与挂接已闭合，尚未实现

本轮后续已从匹配元数据解出固定字符串，并重新分析恢复清理函数。
唯一完整原生依据为 combat-spec `docs/poise-break-buff.md`：来源是 Modifier.source，
失衡事件发布前添加、保存实例句柄，恢复失衡值时逐实例结束，不等标签额外窗口。
下述地址保留为首次发现记录，其中“字符串未解析”状态已被该权威依据替代。

本地同批原始 `BuffData/buff_common_poise_break_damage_taken_scale.json`：Infinity、
默认黑板 dmg_up=0.3，Defender DamageScaleProcessor 将其加到 ProdCalcZone；没有可见图标。
当前正式数据/运行时没有该 ID。它应走既有 Buff 伤害处理器，不应冒充
攻击者 WeaknessDmgScalar，亦不能在最终伤害公式手加一个失衡专用常量。

1.4.4 `runtime-1/poise-controller.analysis.json`（两方法均未截断）已有以下证据：

- OnPoiseZero 在 0x06CC39C6 读取 m_buffsDuringZeroPoise（+0x40），0x06CC39F0
  调用 AddBuff(source, string, true)，随后将句柄加入该集合；字符串参数从 RIP 槽读取，
  **本轮尚未解析槽中具体字符串，不能声称已证明它就是上述 ID**。
- 0x06CC3A28..0x06CC3A95 又读取 AbilitySystemData.buffDuringZeroPoise（+0x40），
  循环调用 AddBuff(self, BuffInput)。这与前一条固定字符串路径是两条入口，不可合并猜测。
- ResetPoise 在 0x03FBF7A4 调用 _RemovePoiseBreakBuff；仍须核查各分支与结束窗口，
  不先假定恢复计时到点就是所有 Buff 的统一结束时刻。

已在生成器 systemBuffRoots.json 登记身份，公共编译器从原始 Buff 得到 defender/product、
addition.blackboardKey=dmg_up、默认0.3；无新增伤害处理器。公共根10项回归通过。
完整31干员公共目录候选生成失败于 Typhoeus attack5 的
projectile_chr_0034_typhoea_archery_attack_05：投射物文件存在，但缺少实体黑板字段。
另两份本地 VFS 副本同样缺字段，不属于单纯没传目录；不能将缺失认作空黑板。
正式目录未被替换，战斗运行时未改，三轴基线仍不变。下一项补齐这份源证据后发布完整目录，
再接失衡生命周期；不跳过泰丰或用小范围目录覆盖全部公共 Buff。

## 2026-09-08 伤害对照：诀的终结技变体与后续伤害归属

- 旧版正式导入/模拟显示 `sc_zpm5ozw/0/39` 使用144%起手，而 `/0/47` 在
  `arcane-gloompurge-arcana-ready` 状态下使用1152%秘仪。存档两次身份均为 ultimate，
  不能仅按身份字段映射。当前定义明确区分 ultimate 与 arcana（源 SkillData
  chr_0032_lizhiyan_ultimate_skill2），故配置只对 `/0/47` 添加显式 actions 覆盖。
  不采用“第二次一律强化”的规则，不让运行时自动替用户替换已放置技能。
- 重转换输出 `tmp/converted-real-axes-20260908-arcana/project.json`，102个输入保留原坐标，
  0转换问题；旧 verified-enemies 目录是修前映射，后续对照请使用新项目。
- 重跑该轴期望总伤害1390671.92→1541379.73，旧2126525。最后一次终结技
  36392.85→193435.69，倍率144%→1152%；其余差异未定性，不能用统一倍率补差。
- 两次战技表面“旧7条/新1条”不等于少了6次伤害：新版四次60%和一次260%的后续
  均按连携原始cast归属，旧版归触发的战技；需按来源链/时间合并比较。普通120%状态伤害
  及反应伤害亦有独立归属，仍应逐项比较倍率与增益，不以日志条数直接判断漏算。

## 2026-09-08 末次秘仪：伤害乘区差异闭合，不追平旧版冻屏延寿

只读旧版正式 store 的 operatorLog/enemyLog 与 Next 回执对照：

| 项目                  | 旧版（含5秒准备）           | Next（不含准备）      |
| --------------------- | --------------------------- | --------------------- |
| 动火用自然增伤50%     | 62.533→78.949，实际16.416秒 | 57.533→67.533，10秒   |
| 落草法术承伤增加10.8% | 52.634→81.133，实际28.499秒 | 47.600→67.567，约20秒 |
| 秘仪命中              | 78.583秒                    | 73.933秒              |

两个 Buff 的原始同批 JSON（`buff_equipsuit_fninflict_01_poisedamageadd`、
`buff_wpn_pistol_0011_valid2`）均 `useTimeDilationDt=false`、`onlyUseSelfTimeDilation=false`。
现有初始化覆盖时长分别10、20秒，与旧定义的基础时长一致。原生时钟依据为 combat-spec
`docs/time-dilation.md` 的 Buff 时钟选择和默认TimeManager分支；它们不应随技能膨胀延寿。
旧 `simulation/events/effectDispatch.ts` 则对未设置 ignoreTimeShift 的效果统一用
`ctx.getShiftedTime(time,duration)` 求到期时间。此处差异不修成旧版行为。

“长息16%缺失”已排除：它是伤害处理器，不在属性快照内；Next攻击侧普通乘区为
1.9174666667 = 1 + 装备0.7574666667 + 长息0.16。Next防御侧普通乘区为1.2584341168。
旧版对应为2.4174666667、1.3664341042，差额恰为上述0.5、0.108。
双方增幅区1.89408（含筹谋）、易伤1.04、腐蚀抗性倍率1.208、倍率1152%、暴击期望1.025一致。
攻击力旧7839、新7893，另有小幅面板差异，尚不在此宣称原因已查明。

将旧期望363793按两项已到期效果扣除并替换攻击力：
`363793 × (1.9174666667/2.4174666667) × (1.2584341042/1.3664341042) × (7893/7839)`
=267574.60，新版267574.88，相差约0.28。该击倍率差异已闭合；不是整条轴差额已解释。
新增正式定义应用回归覆盖动火用十秒内遇到汤汤终结技膨胀，寿命仍约300帧。
后续转查其他命中的归属、次数和状态消耗，不继续为该击补套装/武器倍率。

## 映射

### 2026-09-08 腐蚀剩余倍率拆分及赛希长息来源线索

两次反应的Next攻击侧普通区均1.7186666667，旧均2.3786666667，差0.66；
分别为旧长息0.16、动火用0.5。防御侧普通区分别1.2584341168/1.3664341168，
增强区均1.2、脆弱区分别1.04/1.14，与旧明细匹配。旧攻击6927、新6748；
旧反应另乘1.4540816326530612（`computeLevelCoefficient`的1+(90-1)/196）。

反算：旧60166/71612分别除该等级系数，乘1.7186666667/2.3786666667、6748/6927，
得29123.95/34664.50；Next29124.62/34664.90，差均不足1。不是已证明这几项都该加回。

- **长息来源有具体偏差。** 原始
  `BuffData/buff_chr_0011_seraph_potential_1_atkup.json` DuringBuffEnable是
  `EnhancedAction`，source.targetSource=Source、target.targetSource=Owner、subType=Spell。
  宿主来自ability-entity:3并挂到主控诀；7.033/10.733秒创建的法术增幅在Next回执中
  却是诀→诀。生成动作没有source，使用执行器默认来源。规格`KeywordBuffAction.TryExecute`
  明确独立解析source与targets并传给ApplyKeywordBuff，不能将source折叠成受益者。
  下一步检查`keywordBuffProjection.ts`、Buff生命周期执行器和AbilitySystem来源解析，
  用晶体→受益者→赛希outputBuff监听端到端回归验证；不手改生成文件或长息倍率。
- **动火用的创建在本击之后。** 首击序号1098为DamageApplied，1100发布腐蚀Buff，
  1101才添加增伤；第二击同样先伤害再新增。其监听是outputBuff，不能用同帧视作已生效。
  正式生命周期先启用/执行启动动作再发布Buff事件；是否有不同原生外层包装顺序仍应按源端核查。
- **等级系数尚无原生依据。** 旧computeReactionDamage.ts显式定义等级线性因子；
  当前规格ReadSkillSettingData只查列与PhysicalAndSpellInflictionEnhance增强，
  本次AtkScaleCalculation与最终公式证据没有自动等级因子。不照搬旧值，后续有新原生/IFix
  证据再修订，不能把未找到当作已证明任何版本都不存在。

后续实施更正：最终修复是公共Buff施加入口未指定source时读取本次动作上下文来源，
不再直接使用宿主执行器默认ID；不是手写关键词定义或给赛希加特例。原生成物无需改动。
正式赛希战技＋主控诀第五段普攻回归证明，法术增幅保持宿主Buff的ability-entity来源；
修前同一回归错误归因arcane。显式来源不变，回调actionSourceId优先于buffSourceId。
晶体来源恢复后仍不等于赛希outputBuff已收到：父干员的事件路由尚需原生证据，
不得把所有子实体事件一律转发。该来源修复没有改变三轴总数；后续SpellBurst公共入口修复见下。

### 2026-09-08 拘禁、腐蚀与终结技追击身份核对

此前“拘禁120%/240%”是错误配对：旧版240%的 `triggeredBy` 是 `reaction:corrosion`；
同一时刻另有拘禁120%命中。新版两次战技也分别有拘禁120%，不能将它翻倍修成240%。
新旧终结技36%追击均8次（两组各4次）；旧日志归属触发普攻/处决，Next归属创建实体的终结技。
仅按cast分类会错误报告缺失。末次连携到期后的120%命中仍需独立核查寿命，未在此宣称完全一致。

真正的新缺口位于公共法术异常伤害消费入口：

| 命中               | 旧时刻/期望    | Next时刻/期望     |
| ------------------ | -------------- | ----------------- |
| 第一次腐蚀初始伤害 | 19.617 / 60166 | 14.667 / 10790.05 |
| 第二次腐蚀初始伤害 | 43.934 / 71612 | 39.000 / 10790.05 |

Next回执攻击6748、倍率312%（工厂已包含强化参数）、damageScaleMultiplier=1，
两次伤害完全相同。不能把312%误读成拘禁层数；旧240%另乘法术强度1.3，倍率展示口径也不同。
`standardPlayerDamageEnvironment.#onBuffDamageTriggered` 直接构造最终公式输入，绕过
PlayerDamageOperationExecutor/PlayerDamageContext 的准备事件、双方Modifier和伤害区属性注入；
同一入口还被导电/冻结/燃烧初始伤害、燃烧周期使用，不能只给腐蚀补倍率。
原始 `buff_common_natural_cryst_triggered_wrapper` 创建 `buff_common_natural_triggered`，
后者Hp DamageAction明确 `simpleCalculation=false`、`AtkScaleCalculation(atk_scale)`、
`takeAtkSnapshot=false`、无额外unit processors；不是被允许省略乘区的固定伤害。
规格依据见 combat-spec `docs/buff-and-damage.md` 的DamagePackData与双方Modifier顺序。

本轮已实现：该兼容目录入口复用 PlayerDamageOperationExecutor，正式技能与Buff共用
伤害准备装配端口。保留Buff身份，来源SkillCastInfo取自Buff，不从攻击者当前技能猜测；
独立身份通过执行器依赖传递，不伪造带有技能黑板的操作上下文。非暴击伤害不推进随机数。
回归覆盖双方伤害处理器和电伤属性乘区得到2.21、来源身份与技能黑板不变，6文件125项通过，
补来源身份断言后环境57项再次通过。

同一输入实轴重跑：两次腐蚀分别29124.620738/34664.904163，伤害乘区
2.6992103079/3.2126724492；第三轴总伤害1650072.05→1692281.47，命中数仍265。
另两轴仍716503.81/337938.88，命中数210/194。距旧第三轴2126525尚差434243.53。
接下来检查腐蚀剩余差额的等级/法术反应参数与增益口径，不照抄旧版乘系数。
后续 `executeSpellBurst` 也已删除独立公式/扣血路径，使用同一公共伤害执行器与战场端口。
第三轴升至1694429.1140017707（增加2147.6432313667），其2次爆发合计8238.603231366757；
另两轴及三轴命中数不变。回归覆盖双方处理器、护盾、技能身份和零暴击随机数边界，
8文件249项及vue-tsc通过。当前爆发伤害标签仍为空，后续需按原始DamageUnit转换分类和处理器，
不能宣称所有爆发语义已经完整，也不能按元素/技能名称猜测标签。

- 旧定义只读依据：Git 提交 9ec608cc 的 src/data/operators 和 src/data/gearpieces；
  当前依据：正式 generated-definitions、equipment 目录及武器展示身份表。
- 干员八个在用 ID 保持原值。武器 11 个旧 ID 按 weaponPresentationSlugs 与当前
  定义 assetSlug 联合匹配，输出原生 wpn ID；包含旧存档实例库中未装配的武器。
- 装备按旧定义明确的图标路径找候选，**不是根据图片相似度**。唯一候选记录为显式配置；
  共用图标的两处再用旧定义名称/档位与当前本地化区分：
  Bonekrusha Poncho T1 → attri01_body_04，Eternal Xiranite Gloves T1 → usp02_hand_02。
  后者图标为 hand_01，不能把图标 basename 直接当作定义 ID。
- 普攻按旧段号与当前基础攻击链对应。洛茜旧连携 1/2 段分别映射当前 comboSkill2/3；
  弭弗战技 1/2/3 分别映射 battleSkill1/2/3，第三段旧 skillId 为 mifu-world-splitter。
  卡缪旧单段连携映射当前 comboSkill1；第二条原生技能不是旧输入里自动新增的一次操作。
- 其他在用动作按同组基础技能映射。运行时路由可能不同，转换不为消除告警改放置技能。
  “同身份映射”不证明旧存档没有直接编辑过动作内容，后续对照需继续审查差异。

## 当前结果

**以下为原点/敌人映射修复前的历史首轮结果，不能作为当前正确基线。**
浏览器对照发现绝对时刻漏减战前准备时长，以及敌人旧 gameId 未匹配当前 id。
最新转换同时修复这两项；旧临时产物保留以追查，不继续用于对照。

转换使用源 60 FPS → 当前 30 FPS，三条 7200 源帧均为 3600 当前帧。
219 个搬运时间字段中 94 个发生取整，最大误差约 1/60 秒（包括重复记录的逻辑起点，
不是 94 个技能块）。旧版毫秒显示取整不作为中间步骤。

| 方案          | 输入/已启动 | 运行结束帧 | 不可中断告警 | 连携窗口不可用 | 技能冷却不可用 |
| ------------- | ----------- | ---------- | ------------ | -------------- | -------------- |
| 方案 4        | 23/23       | 3600       | 2            | 1              | 0              |
| 方案 4 (副本) | 11/11       | 3600       | 1            | 0              | 0              |
| 方案 6        | 68/68       | 3600       | 25           | 6              | 3              |

三个项目方案已通过当前正式项目/游戏数据校验。模拟使用当前正式模板、SkillSetting，
以及与现有公开轴回归一致的资源运行端口配置；不表示所有旧系统常量已逐项等价验证。
没有因告警删改任何输入。回执含实体、Buff 等因果后代，不能将伤害事件数当旧 hit 数。

两处待调查的输入路由差异：

1. 方案 4：弭弗第 10 个输入（下标 9），556 帧放 battleSkill1，当前路由为 battleSkill2。
2. 方案 6：诀第 48 个输入（下标 47），2310 帧放 ultimate，当前路由为 arcana。

这些目前只是线索，不判定哪边错误，也不直接调整映射掩盖差异。
尤其方案 6 的连续普攻有较多不可中断告警，需要核查旧输入时间、
当前自然寿命/允许衔接窗口及时间膨胀，不能只靠“全部启动”判通过。

本机结果在 tmp/converted-real-axes-20260908；project.json 可在当前应用打开，
report.json 保留量化和身份改写，simulation.json 与各方案 receipts 文件是临时运行证据。
未进行旧版浏览器重放、最终屏幕坐标或伤害总账对照。

## 本机独立对照环境

2026-09-08 已建立只读参照工作树 `../Endaxis-legacy-reference`，固定在
`4dadc55f2b54cfe621ed8641fc4be9e1e4269dfc`（detached HEAD）。这是此前公开轴
时间探针使用的基线，不代表用户存档生成时的精确版本。旧版依赖按自身 lockfile
通过 `npm ci --ignore-scripts` 安装，没有修改旧版源码。

- 旧版：在参照工作树运行 `npm run dev -- --host 127.0.0.1 --port 5174 --strictPort`。
- 新版：在当前工作树运行 `npm run dev -- --host 127.0.0.1 --port 5175 --strictPort`。
- 两端页面均为 `/timeline`。此前误写新版 `/next/timeline`，该路由不存在。
- 5175 专用于对照，避免覆盖用户在 5173 上的编辑状态；不同端口的浏览器存储隔离。
- 已在两端导入存档并打开方案 6，屏幕确认新版最初多出约 5 秒空白，原点修复后消失。

本轮确认并修复：绝对时刻必须先减原始 prepDuration 再取整；保留 activeScenarioId；
`eny_0051_rodin` / `eny_0071_sandb` 按当前定义 gameId 显式映射到连字符 id，
恢复 boss / elite 分类，不能回退 mob。血量、防御、抗性、霸体、处决倍率与失衡配置
此前已有搬运，修复后继续保留旧存档覆盖，不用新版默认面板替换用户数值。

最新临时证据：`tmp/converted-real-axes-20260908-verified-enemies`。
11项转换测试通过。三轴仍102/102输入启动、结束帧3600；方案6的中断/连携/冷却
告警仍为25/6/3，另外有2次资源不足。已在新版页面重新导入并确认斧甲兽Lv90。
这不证明伤害总账一致。

衔接调查尚未结论：输入诊断探针读到 A3→A4 边界 localFrame=21，输入时刻差为22；
另有取整与汤汤连携全局时间膨胀影响。尚未根据原生 Tick 顺序判定边界是否为错误，
不得直接 +1 或移动输入掩盖。首轮 A4 无伤害点的独立定位见下文。

### 方案 6 首轮 A4：已定位的模型差异

- 旧版 `4dadc55f` 的 `compileTimeline.ts/getNominalFreezeDuration` 将普通连携冻屏
  近似为固定0.5秒；连携相邻时还按间距裁剪。对三个原始方案运行只读时间探针，
  102个动作的 realStartTime 与保存起点差均不超过1毫秒（旧编译器显示精度），
  没有发现转换器漏搬加载后大幅平移的证据。探针清空旧hits，不是旧伤害重算。
- 本机 `tmp/game-data-hybrid-full-20260903/SkillData/chr_0027_tangtang_combo_skill.json`
  的 `serverActionIndex=64` 明确为 Global、0.867000043秒、ComboSkill曲线，
  ignoreTargets只有Owner与OwnerSpawnedEntityFinder(AbilityEntity)，finishByAction=false。
  当前生成的 `tangtangComboSkill` 保留同一配置；诀不是忽略对象。
- Combat-spec `docs/time-dilation.md` 说明全局缩放参与未豁免实体的技能时钟，
  与旧版固定冻结近似不同。原轴A4后两帧触发汤汤连携，到重击输入时A4局部时钟仅1帧，
  首伤害调度在2帧；强制输入重击中断A4，因而没有伤害。这不能通过补发伤害解决。
- 新增 `src/application/realAxisInterruptionRegression.test.ts`：移除其他养成/配装干扰，
  对照有无汤汤连携；无连携时A4有伤害，有连携时无伤害，重击仍在指定帧执行，
  场景输入不变。测试通过。这是当前数据与模拟的因果验证，不声称已经实机验证1.5。
- 另一个问题仍未定性：A3→A4的允许接续窗口起点正好压边界，而输入先于技能Tick。
  Combat-spec `docs/skill-interruption.md` 仍将ComboController与Timeline Tick严格先后列为未闭环；
  `docs/skill-cast-frame.md` 又明确要求施放当帧零增量。不得撤销零增量保护或盲目+1。

2026-09-08续查：新增22/23帧边界最小回归，确认当前输入阶段分别读到21/22局部帧，
原输入仍按指定帧执行。`SkillInputCannotInterruptCurrentSkill` 回执现在附带
`currentSkillTimelineFrame`，不再需要临时替换原型才能审计。
本轮13项转换/真实轴最小回归通过。combat-spec的 `skill-interruption.md` 已补充
原生子功能Tick早于技能循环的调用地址；完整输入消费链仍待核实，未修改模拟调度。

2026-09-08用户收束：旧版技能时长为人工测量，几帧误差正常；只要确认新版时长正确生成，
不再以旧轴边界告警追究一帧调度差异。上述探针保留事实，不作为继续追踪原生消费链的要求。

下一步优先核对方案 6 诀第二次终结技的实际表现，再核对方案 4 的弭弗战技路由，
并检查生成时长与当前源数据一致；缺 hit 等实质差异仍应区分冻屏模型与行为遗漏。
保留输入位置，对差异分别记录版本差异、模型问题或证据不足，不以消除告警为目标。
